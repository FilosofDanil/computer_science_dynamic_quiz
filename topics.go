package main

// Topic represents a single learning item from the CS Foundations Pyramid.
type Topic struct {
	Category    string // used for the topic-selection menu
	Layer       string // displayed after answering for context
	Name        string // the correct answer option shown to the user
	Overview    string // authoritative one-sentence definition, shown after answering
	Question    string // beginner-friendly scenario question shown during the quiz
	Explanation string // 2-3 sentence plain-English explanation shown after answering
}

// AllTopics contains every topic in layer order.
var AllTopics = []Topic{

	// ── Layer 1: CPU & Hardware ───────────────────────────────────────────────

	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "ARM instruction set",
		Overview: "A CPU can only fetch, store, add, compare, branch, and shift — everything else is combinations of these.",
		Question: "A CPU looks incredibly powerful, yet at the deepest level it only knows a handful of basic operations. Which concept describes this minimal set of things a processor can actually do?",
		Explanation: "Think of a CPU like a very fast, very simple calculator: it can load a number, save a number, add, compare two values, jump to another instruction, or shift bits. Everything your computer does — playing a video, running a game, showing a webpage — is just millions of these tiny steps chained together every second. ARM keeps this list as short as possible (RISC design), which is why iPhone chips use far less battery than the busier x86 chips in older laptops.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "Registers vs memory",
		Overview: "Registers are the CPU's handful of scratch variables; RAM is millions of times slower to access.",
		Question: "When a CPU needs to add two numbers together, it keeps them somewhere ultra-fast while it works. But that fast storage is tiny — only a handful of slots. What is the name of this super-fast CPU-side storage?",
		Explanation: "Registers are like the small whiteboard right next to you — instantly reachable, but only a few slots (maybe 16–32). RAM (main memory) is like filing cabinets across the room — way more space, but it takes time to walk over. Accessing a register takes less than 1 nanosecond; fetching from RAM takes 60–100 nanoseconds. That sounds tiny, but at CPU speeds, it feels like being asked to wait several minutes between each step.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "CPU cache hierarchy (L1/L2/L3)",
		Overview: "Data already in L1 cache is ~100× faster to access than data in RAM — locality of reference is the single biggest performance lever.",
		Question: "Iterating through an array from start to finish is ~100× faster than jumping to random positions in it, even though you touch the same number of bytes. What CPU mechanism explains this huge difference?",
		Explanation: "Your CPU secretly pre-loads nearby memory into a small, super-fast storage called cache — like a waiter bringing a whole bread basket instead of one slice at a time. L1 cache (tiny, very fast, sits right on the chip) is checked first; L2 is a bit bigger and slower; L3 is shared between all cores and biggest. When you read an array in order, almost everything is already in cache ('cache hit'). When you jump around randomly, the CPU must constantly fetch from slow RAM ('cache miss') — that's the 100× slowdown.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "Cache lines & false sharing",
		Overview: "The CPU loads memory in fixed 64-byte chunks, so nearby data comes for free and unrelated data sharing a chunk causes invisible slowdowns.",
		Question: "Two threads run on different CPU cores and update completely different variables. Yet the program runs slower in parallel than single-threaded. No locks, no race conditions — what hidden CPU behavior is the culprit?",
		Explanation: "CPUs don't load one byte at a time — they always grab a 64-byte chunk called a cache line, like picking up a whole egg carton instead of one egg. If two unrelated variables happen to share the same 64-byte chunk, every time Thread A changes its variable, Thread B's copy of that chunk is marked stale and must be re-fetched from slower memory — even though B's variable didn't change. This 'false sharing' can actually make a parallel program slower than a single-threaded one.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "Memory alignment",
		Overview: "Misaligned data reads can require two cache-line fetches instead of one, silently halving throughput.",
		Question: "You define a struct with a 1-byte field followed by an 8-byte field. sizeof() returns 16 instead of the expected 9. The compiler added invisible bytes you didn't ask for. What is this behavior called?",
		Explanation: "CPUs are happiest when data starts at an address that's a multiple of its size — an 8-byte number should start at address 8, 16, 24… not 7 or 9. If it doesn't, the CPU may need two memory reads and some stitching to get the full value. The compiler automatically inserts invisible 'padding' bytes between struct fields to keep everything lined up — like adding spacers in a toolbox to keep tools from rattling. You can save memory by ordering struct fields from largest to smallest.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "Branch prediction",
		Overview: "The CPU speculatively executes the next instruction before a condition resolves — unpredictable branches flush the pipeline and cost dozens of cycles.",
		Question: "A simple loop contains an if/else whose outcome is random every iteration. Despite simple logic, performance is surprisingly bad. Which CPU feature is failing here, causing wasted work on every iteration?",
		Explanation: "Modern CPUs don't wait to know the result of an if/else — they make a guess and start running the likely next instruction early, like a quarterback throwing before the receiver even turns around. When the guess is right (predictable data), it's much faster. When it's wrong, the CPU throws away everything it speculatively started and must redo it — wasting 15–20 cycles. That's why sorting data before branching on it can make code 6× faster: sorted data creates predictable patterns.",
	},
	{
		Category: "CPU & Hardware",
		Layer:    "Layer 1 — CPU & Hardware Primitives",
		Name:     "SIMD / vector instructions",
		Overview: "A single instruction can operate on 4–16 values in parallel when data is laid out contiguously, which is why NumPy beats a Python loop by 100×.",
		Question: "NumPy multiplies two arrays of 1,000 numbers in microseconds; a plain Python for-loop takes milliseconds for the same work. What special CPU feature does NumPy exploit that pure Python cannot?",
		Explanation: "Normal CPU instructions process one number at a time. SIMD (Single Instruction, Multiple Data) instructions can process 4, 8, or even 16 numbers with a single instruction — like stamping 16 envelopes at once instead of one at a time. NumPy calls pre-compiled C code that uses these SIMD instructions for its inner loops, which is why it's 100× faster. To use SIMD, data must be stored side-by-side in memory so the CPU can grab a whole 'vector' in one shot.",
	},

	// ── Layer 2: Memory & Concurrency ────────────────────────────────────────

	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Stack vs heap",
		Overview: "The stack is automatic, fast, and scoped to a function call; the heap is manual (or GC-managed), slower, and lives as long as you keep a reference.",
		Question: "You create a variable inside a function. When the function returns, the variable is automatically gone — you didn't write any cleanup code. What memory area did it live in?",
		Explanation: "The stack is like a pile of trays in a cafeteria: when a function starts, a new tray is placed on top with all its local variables; when the function ends, the tray is lifted off and everything on it disappears automatically. The heap is like renting a storage unit: you request space when you need it, and you (or a garbage collector) must explicitly return it when done. The stack is faster and automatic; the heap is flexible but requires management.",
	},
	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Pointers & references",
		Overview: "A pointer is just a number — an address in virtual memory — and every \"object reference\" in any language is ultimately this.",
		Question: "In Python, you pass a list to a function. The function modifies the list. Your original list changes too — you never returned anything. What mechanism causes this behavior?",
		Explanation: "Every object in memory lives at a specific address — like a house number on a street. A pointer (or reference) is simply that house number written on a piece of paper. When Python passes a list to a function, it hands over the house number, not a copy of the entire house. So the function can find and modify the original. In lower-level languages like C or Go, you work with these addresses directly — `p := &x` stores the address of `x`; `*p` means 'go to that address and look'.",
	},
	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Memory ordering & reordering",
		Overview: "CPUs and compilers reorder instructions for speed, so without explicit barriers, two threads can observe writes in different orders.",
		Question: "Thread A writes data to a buffer, then sets a 'ready' flag. Thread B checks the flag and reads the buffer — but sometimes reads garbage values even though the flag was set. No race condition on the flag itself. What is happening?",
		Explanation: "To run faster, your CPU and compiler are allowed to silently reorder instructions — like a chef preparing parts of a dish out of order for efficiency. This is usually fine for a single thread, but with multiple threads it means Thread B might see the 'ready' flag set before the actual buffer data has been written to memory. You fix this with memory barriers (or `sync/atomic` in Go), which act as a checkpoint: 'don't reorder anything across this point'.",
	},
	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Atomic operations",
		Overview: "A single indivisible read-modify-write is the primitive that makes lock-free data structures possible.",
		Question: "Two goroutines both run `counter++` on a shared integer simultaneously. Sometimes the final count is wrong — one increment gets lost. What single operation replaces this broken pattern?",
		Explanation: "The problem is that `counter++` is secretly three steps: read the value, add 1, write it back. If two goroutines both read the value before either writes, they both write the same incremented result — one increment disappears (a 'race condition'). An atomic operation performs all three steps as one indivisible action — like a safe that physically can only be opened by one person at a time. Go's `sync/atomic.AddInt64` uses a special CPU instruction that guarantees this single-step behavior.",
	},
	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Mutexes & condition variables",
		Overview: "A mutex serialises access to shared state; a condition variable lets a thread sleep until that state changes.",
		Question: "Multiple goroutines need to read and write the same map. Without protection, writes from two goroutines simultaneously will corrupt the map and crash the program. What Go primitive do you add to prevent this?",
		Explanation: "A mutex (mutual exclusion lock) is like a bathroom key: only one person can have it at a time. When a goroutine wants to touch the shared map, it 'locks' the mutex first; if another goroutine already holds the lock, the newcomer waits outside. When done, it unlocks, letting the next goroutine in. A condition variable goes one step further — it lets a goroutine sleep until some condition changes (like 'wake me when there's something in the queue to process').",
	},
	{
		Category: "Memory & Concurrency",
		Layer:    "Layer 2 — Memory Model & Concurrency Primitives",
		Name:     "Memory safety: dangling pointers, use-after-free",
		Overview: "Accessing memory after it has been freed is undefined behaviour — the source of most serious security vulnerabilities in C/C++ code.",
		Question: "A C program frees a chunk of memory, but another part of the code still has a pointer to it and reads from it seconds later. The data looks plausible but is wrong — or the program crashes mysteriously. What class of bug is this?",
		Explanation: "Imagine renting a storage unit, returning the key, but keeping the unit's address written down. The unit might now be rented to someone else — if you go back and look inside, you'll see their stuff or corrupt it. Use-after-free is this exact bug with memory: the allocator may have given those bytes to a completely different object. Attackers can exploit this to plant a fake object at a known address and take control of the program — it's behind most browser and OS security vulnerabilities.",
	},

	// ── Layer 3: Systems Programming ─────────────────────────────────────────

	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "Compilation pipeline (preprocessor → AST → IR → machine code)",
		Overview: "Source code passes through four distinct transformations before becoming the bytes that the CPU actually executes.",
		Question: "When you click 'Run' or execute `go build`, your source code undergoes four distinct transformation stages before the CPU can execute it. Which concept describes this multi-stage process?",
		Explanation: "Stage 1: The preprocessor expands `#include` and `#define` macros — like a find-and-replace across your file. Stage 2: The compiler checks types and builds a tree (AST) of your program's structure. Stage 3: An optimizer rearranges and simplifies the code in an intermediate form. Stage 4: The back-end translates to specific machine code for your CPU (x86, ARM, etc.) and writes out a binary file. Running `clang -v hello.c` lets you see every step happening.",
	},
	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "Linking: static vs dynamic",
		Overview: "Static linking bakes library code into your binary; dynamic linking loads it at runtime and lets multiple processes share one copy in memory.",
		Question: "Your program uses OpenSSL. In one approach, the OpenSSL code is baked directly into your binary — self-contained but large. In another, the binary records a dependency on a shared file loaded at runtime. What are these two approaches called?",
		Explanation: "Static linking is like photocopying a recipe into your own cookbook — you're self-contained and work anywhere, but the file is bigger and you won't get the author's future corrections. Dynamic linking is like keeping a reference to a shared cookbook on a communal shelf — your binary is smaller, and if the shared library gets a security update, all programs using it benefit automatically. The downside: if the shared library is missing or the wrong version, your program refuses to start — the classic 'DLL not found' error.",
	},
	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "File I/O and buffering",
		Overview: "Reading a file in one large buffered call is orders of magnitude faster than calling read() one byte at a time, because each syscall is expensive.",
		Question: "Reading a file byte-by-byte is dramatically slower than reading it in large chunks, even when the total data read is identical. What concept explains this performance gap?",
		Explanation: "Every `read()` call crosses a boundary into the OS kernel — like asking a librarian to fetch one page at a time from a back room. Each trip costs about 100 nanoseconds of overhead regardless of how much data you get. Buffering means reading 8,000 bytes at once and storing them locally, then handing you one byte at a time from that local stash — far fewer trips to the librarian. Go's `bufio.Scanner` does this automatically, making line-by-line file reading orders of magnitude faster.",
	},
	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "errno, syscalls, and the kernel boundary",
		Overview: "Every interaction with hardware or the OS goes through a system call — a controlled jump into kernel mode with a measurable overhead.",
		Question: "Your program wants to read a file. It can't touch the disk directly — it must ask the OS to do it on its behalf through a controlled interface. What is this controlled request mechanism called?",
		Explanation: "A system call (syscall) is a knock on the OS's door: your program says 'please do this privileged thing for me'. The CPU switches into kernel mode, the OS performs the operation (read disk, send network packet, etc.), and control returns to your program with the result. The tool `strace` on Linux shows every syscall your program makes — try `strace ls` and you'll see ~100 calls for a simple directory listing. `errno` is a small number the kernel sets when something goes wrong, telling you what failed.",
	},
	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "struct layout and padding",
		Overview: "The compiler inserts invisible padding between fields to satisfy alignment requirements, so struct size is not just the sum of its members.",
		Question: "You define a struct with a 1-byte char followed by a 4-byte int. You expect sizeof to be 5, but it returns 8. The compiler silently added 3 extra bytes you didn't write. What is this phenomenon called?",
		Explanation: "CPUs are most efficient when multi-byte data starts at an address that's a multiple of its size — a 4-byte int should start at address 4, 8, 12… not 2 or 3. If it doesn't, the CPU may need two reads and some gluing to get the full value. The compiler inserts invisible 'padding' bytes between fields to keep everything aligned — like spacers in a toolbox. You can reduce wasted space by putting the largest fields first in your struct.",
	},
	{
		Category: "Systems Programming",
		Layer:    "Layer 3 — C & Systems Programming",
		Name:     "Manual memory management (malloc/free)",
		Overview: "Understanding explicit allocation makes every GC language's behaviour and pauses intuitive.",
		Question: "In C, you request memory with malloc() and must return it with free(). What happens over time if you keep calling malloc() but never call free(), and what tool detects this?",
		Explanation: "Not calling `free()` is a memory leak — like renting storage units but never returning the keys. Your program uses more and more memory until it crashes or the OS kills it. The allocator (malloc) stores bookkeeping info alongside your data, which is why even a 1-byte allocation uses at least 16–32 bytes. Tools like Valgrind and AddressSanitizer track every malloc/free pair and report at the end which allocations were never freed — essential when writing C or C++.",
	},

	// ── Layer 4: Runtimes & Compilers ────────────────────────────────────────

	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Compiled without runtime (C, Rust)",
		Overview: "The binary runs directly on the CPU with no intermediary — what you wrote is what executes.",
		Question: "A Rust binary starts in microseconds, has zero garbage-collection pauses, and uses completely predictable memory. What property of Rust makes this possible — and what does 'no runtime' mean?",
		Explanation: "When you compile Rust, the output is pure machine code with no background helper program running alongside it. Memory cleanup is handled by the compiler itself: it inserts `drop` calls at the end of every block, freeing memory the instant it's no longer needed — no garbage collector ever pauses your program. This predictability makes Rust perfect for firmware, operating systems, and real-time systems where a random half-millisecond pause could be catastrophic.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Compiled with runtime (Go, Java, C#)",
		Overview: "A runtime ships alongside your code to manage goroutines/threads, GC, and reflection — fast, but with GC pauses and a startup cost.",
		Question: "A Go 'hello world' binary is ~4 MB even though your code is tiny. And the program occasionally pauses briefly during execution. What is bundled inside the binary that causes this?",
		Explanation: "Go bakes a runtime into every binary — a small background system that manages goroutines, runs the garbage collector, and handles growing stacks. The garbage collector (GC) automatically finds and frees memory you're done with, so you never call free() yourself — but it occasionally needs a brief pause (usually under 1 ms) to do its bookkeeping. The trade-off is worth it for most programs: automatic memory management in exchange for a small overhead.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Interpreted (Python, Ruby)",
		Overview: "Each source line is decoded at runtime by an interpreter written in C — convenient but 10–100× slower for CPU-bound work.",
		Question: "A matrix multiplication in pure Python is 100× slower than the same math in NumPy, even though both are running Python. Why is Python code so much slower for heavy computation?",
		Explanation: "Python doesn't compile your code directly to machine code — it translates each line into a simpler form at startup, then a C program (the interpreter) runs those instructions one by one. Every single operation (even `a + b`) involves type checking and multiple C function calls under the hood. NumPy skips all of this overhead by calling pre-compiled C code for the actual math — the Python layer only executes once, then raw C runs at full speed. PyPy is an alternative Python that compiles hot code paths, often reaching 2–5× of C speed.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "JIT compilation (V8, JVM HotSpot, PyPy)",
		Overview: "A just-in-time compiler profiles hot code paths at runtime and compiles them to native instructions, narrowing the gap with static compilers.",
		Question: "Node.js starts slow but measurably speeds up the longer it runs. A JavaScript function called millions of times eventually runs as fast as C code. What mechanism is responsible?",
		Explanation: "V8 (the JavaScript engine in Node.js and Chrome) watches which code runs most often ('hot paths') and compiles those specific functions to fast native machine code — a process called JIT (Just-In-Time) compilation. The first few runs use a simple interpreter; once V8 has profiling data, it optimises aggressively. If you later pass a different data type than V8 expected, it 'deoptimises' back to the slower path — which is why keeping variable types consistent in JavaScript helps performance.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Garbage collection algorithms",
		Overview: "GC strategies (mark-and-sweep, generational, reference counting) make different trade-offs between throughput, latency, and memory overhead.",
		Question: "Your Go server occasionally freezes for a fraction of a millisecond even though no code you wrote causes this pause. What background process is responsible, and what are the different ways it can work?",
		Explanation: "A garbage collector (GC) automatically finds and frees memory your program is finished with — like a cleaning crew that comes in periodically. Go's GC works mostly in the background but needs two very brief 'stop-the-world' moments per cycle where all goroutines freeze (usually under 1 ms in modern Go). Different GC strategies exist: Java's generational GC optimises for throughput; Go's concurrent GC optimises for low latency; Python uses reference counting, freeing objects instantly when the last reference disappears.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Green threads vs OS threads",
		Overview: "OS threads are scheduled by the kernel and expensive to create; green threads (goroutines, async tasks) are scheduled by the runtime and cheap enough to have millions of.",
		Question: "A Go server handles 100,000 simultaneous connections without running out of memory. An equivalent Java server using one OS thread per connection would crash before reaching 10,000. Why?",
		Explanation: "Each OS thread reserves about 8 MB of memory for its stack (the kernel requires this). 100,000 OS threads = ~800 GB of RAM — impossible. Go's goroutines start with just 2–8 KB of stack that grows only if needed, so 100,000 goroutines use only a few GB. The Go runtime plays scheduler, assigning goroutines to a small pool of OS threads and automatically parking goroutines that are waiting for network data. It's like one experienced waiter managing 20 tables efficiently vs. one waiter standing at each table doing nothing.",
	},
	{
		Category: "Runtimes & Compilers",
		Layer:    "Layer 4 — Runtimes, Compilers & Language Models",
		Name:     "Async / event loops (Node.js, asyncio)",
		Overview: "A single OS thread can handle thousands of concurrent I/O operations by registering callbacks and never blocking — the tradeoff is that CPU-bound work blocks everyone.",
		Question: "Node.js handles thousands of simultaneous web requests using just one OS thread — it never creates new threads for each request. What mechanism allows it to serve many users without waiting for one to finish before helping the next?",
		Explanation: "Instead of blocking (doing nothing while waiting for network data), Node.js registers a callback and immediately moves on to handle other requests. The OS (via `epoll` on Linux) notifies Node.js the moment data arrives, and the callback is triggered. It's like a waiter taking many orders and checking back when the kitchen signals each dish is ready — rather than standing at one table until the food comes. The catch: any heavy computation (like parsing a huge JSON file) freezes the whole event loop, delaying every other request.",
	},

	// ── Layer 5: Operating Systems ────────────────────────────────────────────

	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "Kernel vs userspace",
		Overview: "The kernel has unrestricted hardware access; user processes live in a protected sandbox and must ask the kernel for resources via syscalls.",
		Question: "Your program tries to directly access a hardware register to talk to the network card. It immediately crashes with a permission error, even though you're the admin. What fundamental OS design prevents user programs from touching hardware directly?",
		Explanation: "CPUs have privilege levels (like security clearance tiers). The kernel runs at the highest level with full hardware access. Your program runs at the lowest level — it literally cannot access hardware or another program's memory. If it tries, the CPU itself intercepts the attempt and crashes the program (not the whole OS). This isolation means a buggy app crashes itself but doesn't take down your entire computer — the kernel's protected boundary is what makes that possible.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "Processes & process isolation",
		Overview: "Each process has its own virtual address space — one process cannot read another's memory without the kernel's explicit permission.",
		Question: "Chrome crashes spectacularly. Your text editor, running at the same time on the same hardware, keeps working fine and your document is safe. What OS feature ensures one program's crash can't destroy another's data?",
		Explanation: "Each process lives in its own completely private 'bubble' of memory — a virtual address space. When Chrome accesses its memory at address 0x1000, it gets its own private slice of RAM; when your text editor accesses 0x1000, it gets a completely different physical slice. Neither can see or touch the other. When Chrome crashes and corrupts its bubble, the OS simply destroys Chrome's bubble — your editor's bubble is untouched, as if Chrome never existed.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "Virtual memory & paging",
		Overview: "The OS maps each process's logical addresses to physical RAM pages, allowing programs larger than RAM and making ASLR possible.",
		Question: "Your computer has 8 GB of RAM but all running programs together need 12 GB. The computer doesn't crash. How does the OS make this work?",
		Explanation: "The OS uses virtual memory: every program thinks it has a large, private chunk of memory all to itself, but the OS maps those 'virtual' addresses to real physical RAM in 4 KB chunks called pages. Pages that haven't been used recently get quietly written to disk (swapping), freeing up real RAM. When a program accesses a swapped-out page, the OS fetches it back from disk — called a page fault. Disk is ~10,000× slower than RAM, which is why heavy swapping makes your computer crawl.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "Scheduling (preemptive, CFS)",
		Overview: "The kernel's scheduler decides which process runs on which CPU core at any microsecond — fairness and latency are competing goals.",
		Question: "You're running a game, a music player, and a browser at once, all seeming to run simultaneously on 4 CPU cores. Which OS component decides which program gets to use the CPU at each moment?",
		Explanation: "The scheduler is the OS component that divides CPU time fairly among all running programs. Linux's CFS (Completely Fair Scheduler) tracks how much CPU time each process has actually used; the process that has used the least gets to run next. A program hogging the CPU gets 'penalized' and pushed back; your music player, which barely uses the CPU, jumps to the front the instant it has audio data ready. 'Preemptive' means the OS can interrupt a running program mid-task — no program can hog the CPU forever.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "File system abstractions (VFS, inodes)",
		Overview: "\"Everything is a file\" works because the kernel presents sockets, pipes, and devices through the same read/write interface as disk files.",
		Question: "In Unix, you can use the same `read()` function call to read from a disk file, a network socket, or a pipe between processes. What OS abstraction makes this unified interface possible?",
		Explanation: "The Virtual File System (VFS) is a layer that presents everything — disk files, sockets, hardware devices, pipes — as if they were files you can open, read, write, and close. An inode is the metadata record behind each file: it stores size, permissions, and disk location, but NOT the filename (filenames are stored in directories that point to inodes). This is why you can have two different filenames pointing to the same file (hard links) — they both point to the same underlying inode.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "Signals and inter-process communication (pipes, sockets, shared memory)",
		Overview: "Processes communicate via well-defined kernel-mediated channels — signals for async notification, pipes for streaming data, shared memory for performance-critical paths.",
		Question: "When you press Ctrl+C, your running program immediately stops. When you run `kill 1234` from another terminal, that process also stops. What OS mechanism delivers these instant 'stop' notifications to running processes?",
		Explanation: "Signals are instant notifications the OS delivers to a process — like a tap on the shoulder. Ctrl+C sends SIGINT ('please interrupt'); `kill` sends SIGTERM ('please shut down gracefully'); `kill -9` sends SIGKILL which literally cannot be ignored or caught. Programs can write custom handlers for most signals (e.g., SIGTERM → save your work → exit cleanly). Pipes (`|` in shell) and sockets let processes exchange data in a stream; the kernel buffers bytes between them and handles flow control.",
	},
	{
		Category: "Operating Systems",
		Layer:    "Layer 5 — Operating Systems & the Kernel",
		Name:     "cgroups and namespaces",
		Overview: "These two Linux kernel features are the entire foundation of containers — cgroups limit resources, namespaces isolate visibility.",
		Question: "Docker containers run as isolated processes sharing the same Linux kernel — no virtual machines involved. What two Linux kernel features provide the process isolation and resource limiting that make this work?",
		Explanation: "Namespaces make a process think it's alone on its own machine: a container gets its own private process list, its own network stack, its own filesystem view, and its own hostname — all provided by the kernel without running a separate OS. cgroups (control groups) put hard limits on resource usage: a container with `--memory=512m` literally cannot use more RAM — the kernel will kill a process inside it before allowing the container to exceed its budget. These two features together are the entire technical foundation of Docker.",
	},

	// ── OS Fundamentals (new) ─────────────────────────────────────────────────

	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "What is an Operating System",
		Overview: "An OS is the software layer that manages hardware resources and provides services (files, processes, networking) to applications through a defined API called system calls.",
		Question: "Why can't you just write a program that directly reads from a disk drive or sends data over the network — why must you always go through an intermediary?",
		Explanation: "The OS is the manager of all shared hardware — disk, network, screen, keyboard. Without it, every program would need its own disk driver, its own network code, its own screen rendering — and they'd constantly crash into each other. The OS provides a safe, uniform interface (system calls) that all programs use to request resources. Think of the OS like a hotel manager: guests don't rewire the hotel's electricity themselves — they call the front desk, and the manager handles it.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "BIOS / UEFI",
		Overview: "BIOS is firmware stored in a chip on the motherboard that runs before any OS and initialises hardware; UEFI is its modern replacement with a GUI, network stack, and Secure Boot support.",
		Question: "When you press the power button on a computer, it starts up from scratch. Something runs before the OS, checks the hardware is working, and hands control to the operating system. What is this called?",
		Explanation: "BIOS (or its modern replacement UEFI) is a tiny program permanently stored in a chip on the motherboard, not on your disk. It runs the instant you power on, checks that the CPU, RAM, and keyboard are present and working (called POST — Power-On Self Test), then finds the bootable drive and passes control to the bootloader. UEFI is the modern version: it has a nicer interface, supports larger disks, and can verify the bootloader hasn't been tampered with (Secure Boot).",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Bootloader (GRUB)",
		Overview: "A bootloader is the first program the firmware hands control to after POST; it finds the OS kernel on disk, loads it into memory, and jumps to its entry point.",
		Question: "After BIOS/UEFI finishes its hardware check, what small program takes over next — the one that reads your disk, shows a boot menu, and actually loads the Linux kernel into RAM?",
		Explanation: "The bootloader (commonly GRUB on Linux) is the link between firmware and operating system. GRUB reads its config file, optionally shows you a menu (useful if you have multiple OSes), then decompresses the Linux kernel from disk into RAM. It also loads a small temporary filesystem (initrd) that contains the drivers needed to mount your actual disk — like a spare key kept outside the house to open the front door. On Windows, the Windows Boot Manager plays the same role.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Shell (bash, zsh, fish)",
		Overview: "A shell is a command-line interpreter that reads user commands, expands globs and variables, and uses fork/exec to create child processes.",
		Question: "When you type `ls -la | grep .go` in a terminal, what program actually reads that text, understands the pipe `|`, creates two new processes, and connects them together?",
		Explanation: "The shell is an interpreter that turns your typed commands into running processes. When you type `ls -la | grep .go`, the shell finds the `ls` and `grep` programs on disk, creates two new processes (via fork + exec), and connects them with an invisible pipe before either program starts. Shell scripts are just text files full of these commands — the shell reads and executes them line by line. bash, zsh, and fish are all shells; they differ in features and syntax but do the same fundamental job.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Terminal emulator and pseudo-terminal (pty)",
		Overview: "A terminal emulator (iTerm2, Windows Terminal) is a GUI app that creates a pseudo-terminal (pty) device and renders the text output of a shell, interpreting ANSI escape codes for colours and cursor movement.",
		Question: "What is the difference between a 'terminal', a 'terminal emulator', and a 'shell' — and what does the terminal app actually do with the text your shell outputs?",
		Explanation: "A shell (bash, zsh) is the program that interprets commands. A terminal historically was physical hardware — a keyboard+screen box connected by cable. A terminal emulator (like Terminal.app, iTerm2, or Windows Terminal) is a modern GUI app that pretends to be that hardware. It creates a virtual connection (pseudo-terminal) to the shell, sends your keystrokes to it, and converts the shell's text output into rendered characters on screen. ANSI codes like `\\033[32m` tell the terminal to display text in green — the same codes this program uses.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "File descriptors",
		Overview: "A file descriptor (fd) is a small integer that the kernel associates with an open resource (file, socket, pipe, device); all I/O in Unix is done by passing an fd to read(), write(), or ioctl().",
		Question: "In Unix, you read from files, network sockets, and pipes using the same `read()` function. What small token does the OS give you when you open any of these, so `read()` knows where to get the data?",
		Explanation: "A file descriptor is just a small number (like 3, 7, or 15) that the OS hands you when you open anything readable or writable. When you call `open('/etc/hosts')`, the OS finds the file and returns a number; you pass that number to `read()` and the OS knows which file to read from. This works identically for network sockets and pipes. Processes inherit their parent's open file descriptors when they start — that's how shell pipes work: the shell opens a pipe and gives one end to each process.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "stdin, stdout, stderr",
		Overview: "Every Unix process starts with three open file descriptors: stdin (0) for input, stdout (1) for normal output, and stderr (2) for errors; they default to the terminal but can be redirected independently.",
		Question: "You run `./myprogram > output.txt`. The normal output goes to the file, but error messages still appear on your screen. How does the OS support this split — one destination for normal output and another for errors?",
		Explanation: "Every process starts life with three pre-opened 'channels': stdin (fd 0) for reading input, stdout (fd 1) for normal output, and stderr (fd 2) for errors. By default all three connect to your terminal screen. `> output.txt` changes where fd 1 points — but fd 2 still points at your terminal. Programs write errors to stderr specifically so they're not accidentally piped into the next command or hidden in log files. To capture both: `./prog > out.txt 2>&1` redirects stderr (fd 2) to the same place as stdout (fd 1).",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Symlinks vs hard links",
		Overview: "A symbolic link stores a target path string; a hard link is a second directory entry pointing to the same inode, so both names refer to the exact same file data.",
		Question: "Two filenames both point to the same file on disk. When you delete one filename, the file still exists under the other name — no data was lost. What type of filesystem link are these?",
		Explanation: "Hard links are like two signposts pointing to the same house — the house (file data) is only demolished when the last signpost is removed. Each hard link is just a directory entry pointing to the same underlying inode number. A symlink (symbolic link) is different — it's more like a sticky note that says 'the real file is over there'. If you delete the original file, the symlink becomes broken. Hard links can't cross filesystem boundaries or point to directories; symlinks can do both.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Package managers (apt, brew, npm, pip)",
		Overview: "A package manager automates downloading, verifying, installing, and removing software packages along with their declared dependencies.",
		Question: "You need to install nginx, which depends on libssl, zlib, and pcre. Instead of hunting down and installing each library manually, one command handles all of it. What kind of tool does this?",
		Explanation: "A package manager is like an app store for software libraries and programs. It knows about thousands of packages and their dependencies: `apt install nginx` figures out that nginx needs libssl, zlib, and pcre, downloads all of them in the right order, and installs everything. Packages are cryptographically signed so the manager can verify they haven't been tampered with in transit. npm and pip work the same way for JavaScript and Python projects — `npm install` reads `package.json` and installs every listed dependency.",
	},
	{
		Category: "Operating Systems",
		Layer:    "OS Fundamentals",
		Name:     "Environment variables",
		Overview: "An environment variable is a key-value string in a process's environment block, inherited by child processes via fork/exec, used to pass configuration without hardcoding values.",
		Question: "Your program needs a database password, but you don't want to hardcode it in your source code (which goes into git). How do you pass secret configuration to a program securely at runtime?",
		Explanation: "Environment variables are key=value pairs the OS passes to a program when it starts — like a set of sticky notes handed to a new employee on their first day. `DATABASE_PASSWORD=secret123` is invisible to anyone reading your source code, can be different on your laptop versus the production server, and is never accidentally committed to git. Access them in Go with `os.Getenv(\"DATABASE_PASSWORD\")`, in Python with `os.environ['DATABASE_PASSWORD']`. Tools like `.env` files and `direnv` load them automatically per project.",
	},

	// ── Layer 6: Containers ───────────────────────────────────────────────────

	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "Hardware virtualisation (hypervisors, VMs)",
		Overview: "A hypervisor traps privileged instructions and emulates hardware so multiple OS kernels can run on one physical machine with near-native performance.",
		Question: "A cloud provider runs 500 customer virtual machines on one physical server, each with their own OS and seemingly exclusive access to hardware — yet performance is near-native. What technology makes this possible?",
		Explanation: "A hypervisor (like VMware ESXi or KVM) is software that tricks each guest operating system into thinking it has its own hardware. Modern CPUs have special hardware support (Intel VT-x, AMD-V) that lets normal guest instructions run at full speed — only the rare privileged operations (like talking to hardware) get intercepted by the hypervisor. Type 1 hypervisors (used in cloud data centers like AWS, GCP) run directly on hardware; Type 2 (like VirtualBox on your laptop) run inside a host OS.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "Linux namespaces (pid, net, mnt, uts, ipc, user)",
		Overview: "Each namespace type hides a different slice of the system from a process — together they create the illusion of an isolated machine.",
		Question: "A Docker container has its own process list, its own network interface, and its own filesystem — yet it's just a regular process on the same Linux kernel. What kernel feature provides these separate, isolated 'views' of the system?",
		Explanation: "Linux namespaces are like one-way mirrors for different system resources. A PID namespace makes a container think its first process is PID 1 (as if it's alone); the host can still see the real PID. A network namespace gives the container its own private IP and ports. A mount namespace gives it its own filesystem tree. Together, a container gets all six namespace types — creating the convincing illusion of a completely separate machine, with zero hypervisor overhead.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "cgroups v2 for resource limits",
		Overview: "cgroups enforce CPU, memory, and I/O budgets per process group — the mechanism behind --memory and --cpus in Docker.",
		Question: "You run a Docker container with `--memory=512m --cpus=0.5`. If the container tries to use more, it gets killed or throttled. What Linux kernel feature actually enforces these limits?",
		Explanation: "cgroups (control groups) are a kernel feature that puts hard resource budgets on groups of processes. Docker writes your limits into special files in `/sys/fs/cgroup/`: `512m` into `memory.max` and CPU quota into `cpu.max`. If the container exceeds its memory limit, the kernel OOM-kills a process inside it; if it tries to use more CPU than its quota, the kernel throttles it. It's like giving a restaurant table a spending limit — the kitchen tracks the tab and stops serving when the budget is hit.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "OCI images and layers",
		Overview: "A container image is a stack of read-only filesystem layers expressed as tarballs — the running container adds one writable layer on top.",
		Question: "When you pull a Docker image, it downloads several separate 'layers'. Deleting a large file in a Dockerfile RUN command doesn't shrink the image. Why not — and what are these layers?",
		Explanation: "Each Dockerfile instruction that changes the filesystem creates a new layer — a snapshot of only what changed. Layers stack on top of each other; lower layers are permanently read-only. 'Deleting' a file in a new layer just adds a 'whiteout' marker saying 'ignore this file from lower layers' — the original bytes in the lower layer still exist in the image. To actually shrink the image, you must delete a file in the same `RUN` command that created it, so both the add and delete happen within one layer.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "Container runtime (runc, containerd)",
		Overview: "containerd is the daemon; runc is the low-level binary that actually calls clone(2) and execve(2) to start a container process.",
		Question: "When Kubernetes starts a new Pod, what chain of software — from the orchestrator down to the actual Linux system calls — actually creates the container process?",
		Explanation: "Kubernetes calls containerd (a daemon that manages container lifecycle). containerd prepares the container's layered filesystem, sets up networking, and then calls runc — a small binary that does the actual OS work. runc calls the Linux `clone()` system call to create new namespaces, joins the container to its cgroup, and then `execve()`s the container's start command. The whole process takes a few hundred milliseconds — much faster than booting a VM, which would need minutes.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "Virtual networking (veth pairs, bridges, overlay networks)",
		Overview: "Each container gets a virtual Ethernet interface connected to a software bridge — packets cross namespace boundaries via veth pairs.",
		Question: "Two Docker containers on the same host can reach each other by IP address. How does network traffic travel between them without going through a physical network card?",
		Explanation: "Docker creates a virtual cable pair (veth pair) for each container — like two ends of a virtual Ethernet cord. One end goes inside the container, the other connects to a software bridge (docker0) on the host. Both containers connect to the same bridge, so packets travel: container A's virtual NIC → bridge → container B's virtual NIC — entirely in software, never touching a physical cable or NIC. For containers on different servers, Kubernetes overlay networks wrap packets in UDP tunnels to carry them across the real network.",
	},
	{
		Category: "Containers",
		Layer:    "Layer 6 — Virtualisation & Containers",
		Name:     "Kubernetes primitives (Pod, Service, Deployment)",
		Overview: "A Pod is the scheduling unit (one or more containers sharing a network namespace), a Service is stable DNS/IP in front of dynamic Pods, a Deployment keeps a desired replica count.",
		Question: "You have 3 replicas of a web server in Kubernetes. Pods get replaced constantly and their IPs change. Clients need one stable address that always works. What Kubernetes object provides this stable address?",
		Explanation: "A Service is a stable name and IP address (ClusterIP) that Kubernetes keeps in front of a group of Pods. Kubernetes programs load-balancing rules so requests to the ClusterIP are automatically forwarded to any healthy Pod. Pods can be replaced, rescheduled, or scaled up/down — the Service address never changes. A Deployment manages the desired count: if a Pod dies, the Deployment controller creates a replacement automatically, like a manager who always maintains a target headcount.",
	},

	// ── Layer 7: Networking (original) ───────────────────────────────────────

	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "Ethernet & MAC addressing (L2)",
		Overview: "Two machines on the same network segment exchange frames identified by 48-bit hardware addresses, resolved via ARP.",
		Question: "Two computers are on the same WiFi network. Before they can exchange any data, they need each other's hardware address — not IP address. What protocol resolves an IP address to a hardware address on the local network?",
		Explanation: "Every network card has a permanent 48-bit MAC address (like aa:bb:cc:dd:ee:ff) burned in at the factory. ARP (Address Resolution Protocol) is the 'who has this IP?' broadcast: your computer asks the whole local network 'who has 192.168.1.5?' and that device replies with its MAC address. Your OS caches these mappings in an ARP table — run `arp -a` to see it. VLANs let one physical switch carry traffic for multiple isolated networks by tagging frames with a network ID.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "IP addressing & routing (L3)",
		Overview: "IP provides best-effort packet delivery across networks using 32- or 128-bit addresses; routers forward packets hop-by-hop based on the destination prefix.",
		Question: "Your request to google.com travels through many routers across the internet before reaching Google. How does each router know which direction to forward your packet — without knowing the full path?",
		Explanation: "Each router has a routing table: a list of network address ranges and which 'next hop' router to send packets toward. When a packet arrives, the router finds the most specific matching range — like a postal sorting office choosing the right truck based on the zip code prefix. Routers learn each other's routes using protocols like BGP (used across the internet) or OSPF (used inside a company network). No single router knows the full path — it just knows the next step.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "TCP: three-way handshake, flow control, congestion control",
		Overview: "TCP converts an unreliable packet network into a reliable ordered byte stream by numbering every byte and requiring acknowledgement.",
		Question: "You're downloading a large file. Individual packets can be lost or arrive out of order over the internet, yet the final file is always complete and correct. Which protocol provides this reliability guarantee?",
		Explanation: "TCP numbers every byte it sends; the receiver must acknowledge each chunk. If an acknowledgement doesn't arrive within a timeout, TCP automatically retransmits. It also adjusts how fast to send based on network conditions — slowing down when packets are lost ('congestion control') and speeding up when the path is clear. Before any data flows, TCP does a three-way handshake (SYN → SYN-ACK → ACK) to set up the connection — this one round-trip is the minimum latency cost of any TCP connection.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "UDP: when and why",
		Overview: "UDP sends datagrams with no delivery guarantee and no connection overhead — the right choice when latency matters more than reliability (DNS, video, games).",
		Question: "A DNS lookup, a live video call, and an online game all prefer to lose occasional packets rather than wait for retransmission. Which protocol do they use, and why is that better than TCP for these cases?",
		Explanation: "UDP (User Datagram Protocol) is a minimal wrapper: send a packet and move on — no connection setup, no acknowledgements, no automatic retransmissions. This makes it very fast with very little overhead. For live video, a frame arriving too late is useless anyway — better to skip it than retransmit and delay everything behind it. For DNS, a query and response each fit in one small packet, and TCP's connection setup would double the lookup time. The application itself decides how to handle lost packets.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "TLS / mutual TLS",
		Overview: "TLS negotiates symmetric encryption on top of TCP using asymmetric crypto for key exchange and certificates for identity — the 's' in 'https'.",
		Question: "Your browser shows a padlock next to a website URL. The connection is encrypted so nobody on the network can read the data. Which protocol provides this encryption and identity verification?",
		Explanation: "TLS (Transport Layer Security) works in two phases. First, your browser checks the site's certificate — a signed document from a trusted Certificate Authority saying 'this public key belongs to google.com'. Second, browser and server use the public key to agree on a temporary shared secret for encrypting actual data. Without TLS, anyone on the same coffee-shop WiFi could read everything you send and receive. mTLS (mutual TLS) adds the reverse: the server also verifies a certificate from the client, used in service-to-service security.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "DNS: resolution chain",
		Overview: "A hostname is resolved by walking a hierarchy of authoritative servers, with aggressive caching at every step.",
		Question: "You type 'github.com' in your browser. Before the browser can connect, it needs github.com's IP address. What system translates domain names like 'github.com' into IP addresses?",
		Explanation: "DNS (Domain Name System) is the internet's phonebook. Your computer first checks its local cache and the `/etc/hosts` file. If not found, it asks a DNS resolver (e.g., 8.8.8.8). The resolver walks a hierarchy: ask a root nameserver 'who handles .com?', ask the .com server 'who handles github.com?', ask that server for github.com's IP. Results are cached at every step. The whole uncached process takes 50–100ms across 3–4 round trips; subsequent lookups take <1ms from cache.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "HTTP/1.1 → HTTP/2 → HTTP/3 (QUIC)",
		Overview: "HTTP/1.1 serialises requests; HTTP/2 multiplexes streams over one TCP connection; HTTP/3 replaces TCP with QUIC to eliminate head-of-line blocking at the transport layer.",
		Question: "A webpage loads 50 assets (images, CSS, JS). HTTP/1.1 browsers open 6 parallel connections to speed this up. HTTP/2 improves on this, but still has a problem. HTTP/3 fixes it. What is the progression of improvements?",
		Explanation: "HTTP/1.1: one request at a time per connection (like a single-lane road). Browsers open 6 connections as a workaround. HTTP/2: one connection, many requests at the same time (multiple lanes) — but a single lost packet freezes ALL requests until TCP recovers. HTTP/3 replaces TCP with QUIC (which runs over UDP): each request has independent recovery, so a lost packet only delays that one request. Mobile users on unreliable networks see the biggest improvements with HTTP/3.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "WebSockets & Server-Sent Events",
		Overview: "WebSockets upgrade an HTTP connection to a full-duplex channel; SSE is a simpler one-way push over plain HTTP.",
		Question: "You're building a chat app. The server needs to push new messages to the browser instantly as they arrive. Repeatedly polling the server every second is wasteful. What two protocols enable real-time server-to-browser messaging?",
		Explanation: "WebSockets start as a normal HTTP request, then 'upgrade' to a persistent two-way channel — like switching from sending letters to having a phone call. Once connected, server and browser can send messages any time in either direction, with very little overhead. SSE (Server-Sent Events) is simpler: the server keeps an HTTP response open and streams text events indefinitely, but only in one direction (server → browser). Use WebSockets for chat, games, and live collaboration; SSE for notifications and live dashboards.",
	},
	{
		Category: "Networking",
		Layer:    "Layer 7 — Network Protocols",
		Name:     "Load balancing (L4 vs L7)",
		Overview: "L4 load balancers forward TCP connections; L7 load balancers inspect HTTP and can route, rewrite, and terminate TLS.",
		Question: "Your web service has 10 backend servers. Something must decide which server handles each incoming request. This device operates at two possible levels — TCP or HTTP. What is this called?",
		Explanation: "A load balancer distributes incoming requests across multiple servers so none gets overwhelmed. L4 (Layer 4) load balancers see only IP addresses and ports (TCP level) — very fast, but can't make smart routing decisions. L7 (Layer 7) load balancers understand HTTP — they can read URLs and headers to route `/api/*` to API servers and `/static/*` to a CDN, terminate TLS, and do canary deployments. nginx, Envoy, and AWS ALB are common L7 load balancers; AWS NLB is a common L4 one.",
	},

	// ── Networking (new topics) ───────────────────────────────────────────────

	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "Subnet mask / CIDR notation",
		Overview: "A subnet mask defines which bits of an IP address are the network prefix; CIDR notation expresses this as a slash and prefix length, e.g., /24 means the first 24 bits are the network.",
		Question: "An IP address like 192.168.1.0/24 has a '/24' at the end. What does this number mean, and how many individual devices can this network address?",
		Explanation: "An IPv4 address is 32 bits total. The '/24' means the first 24 bits identify the network, leaving 8 bits for individual devices. 8 bits = 256 combinations, but two are reserved (network address and broadcast), leaving 254 usable host addresses (192.168.1.1 to 192.168.1.254). A /16 gives 65,534 hosts; a /30 gives just 2 (useful for point-to-point links). This 'slash notation' is called CIDR and replaced the older, less flexible A/B/C address class system.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "NAT (Network Address Translation)",
		Overview: "NAT rewrites IP packet headers as they cross a router, allowing many private-IP hosts (RFC 1918) to share a single public IP by tracking connection state in a translation table.",
		Question: "Your home router has one public IP address from your ISP, yet your phone, laptop, and smart TV can all browse the internet simultaneously. How does the router manage this with just one IP?",
		Explanation: "NAT (Network Address Translation) is built into your home router. When your laptop (192.168.1.5) sends a request, the router replaces the source IP with its own public IP and assigns a temporary unique port number. When the response comes back to that port, the router looks it up in its translation table and forwards it to your laptop's private address. Hundreds of devices can share one public IP this way. The downside: servers behind NAT can't be reached directly from outside without port forwarding.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "Firewall / iptables / nftables",
		Overview: "A firewall filters network traffic by matching packets against a ruleset (source/destination IP, port, protocol, connection state) and either accepting, dropping, or rejecting them.",
		Question: "You want your Linux server to accept only SSH (port 22) and HTTPS (port 443) from the internet, and reject everything else. What tool and what concept allow it to automatically allow replies without opening all ports?",
		Explanation: "iptables (or the newer nftables) is a Linux firewall that matches incoming packets against rules and decides ACCEPT or DROP. The key concept is stateful filtering: the firewall tracks connection state. `--state ESTABLISHED,RELATED` allows return traffic for connections your server initiated (like downloading packages) without having to open every possible outbound destination port. Cloud firewalls (AWS Security Groups) work on the same idea but are configured in the cloud console rather than on the server itself.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "DHCP (Dynamic Host Configuration Protocol)",
		Overview: "DHCP automatically assigns an IP address, subnet mask, default gateway, and DNS server to a newly-connected device using a four-message DORA exchange.",
		Question: "You plug your laptop into a new network (or connect to WiFi) and it immediately gets an IP address, a gateway, and DNS server — without any manual configuration. What protocol does this automatically?",
		Explanation: "DHCP (Dynamic Host Configuration Protocol) automates network setup. When you connect, your device broadcasts 'Does anyone have an IP for me?' The DHCP server (usually your router) replies with an available IP, subnet mask, gateway, and DNS server — plus a lease duration (e.g., 24 hours). Before the lease expires, your device must renew or it loses the address, which is why your IP can change when you reconnect. Servers usually get fixed IPs via 'static DHCP leases' that tie a specific MAC address to a fixed IP.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "ARP (Address Resolution Protocol)",
		Overview: "ARP maps a known IPv4 address to an unknown MAC address by broadcasting a query on the local L2 segment; the target host replies with its MAC, and the result is cached in the ARP table.",
		Question: "Your computer knows the IP of your router (192.168.1.1) but needs its hardware address to actually send an Ethernet frame to it. What protocol broadcasts a 'who has this IP?' question on the local network?",
		Explanation: "ARP (Address Resolution Protocol) is the IP-to-MAC translator for your local network. Your computer broadcasts 'Who has 192.168.1.1?' to every device on the network; the router replies with its MAC address. Your OS caches this in an ARP table (check with `arp -a`). ARP only works on the local network — once traffic leaves the router, IP routing takes over. ARP spoofing is an attack where a malicious device sends fake ARP replies to redirect your traffic through itself.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "ICMP / ping / traceroute",
		Overview: "ICMP is a network-layer protocol for error reporting and diagnostics; ping sends ICMP Echo Request messages and measures round-trip time from the Echo Reply.",
		Question: "You run `ping google.com` to check if a server is reachable and measure round-trip time. You run `traceroute` to see every router your packets pass through. What protocol do both tools use?",
		Explanation: "ICMP (Internet Control Message Protocol) is a diagnostic tool built into the IP layer. Ping sends an 'Echo Request' packet; the target replies with 'Echo Reply' — the round-trip time shows network latency. Traceroute is cleverer: it sends packets with TTL (Time To Live) starting at 1, increasing by 1 each time. Each router decrements TTL; when it hits 0, the router sends back an ICMP 'Time Exceeded' with its own address, revealing itself. Mapping all these responses shows the full hop-by-hop path across the internet.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "VPN / tunnelling (WireGuard, IPSec)",
		Overview: "A VPN encrypts traffic between endpoints and encapsulates it inside another protocol, creating an encrypted overlay network that appears as a direct private connection.",
		Question: "You're working from home and need to access internal company servers that aren't accessible from the internet. What technology creates a secure, encrypted 'tunnel' through the public internet to the company's private network?",
		Explanation: "A VPN (Virtual Private Network) encrypts your packets and wraps them inside other packets to send over the public internet. Your device gets a private IP address in the company's network; from then on, traffic to internal servers flows through the encrypted tunnel. WireGuard is a modern VPN protocol that's fast and simple (~4,000 lines of code vs. OpenVPN's 100,000+). Site-to-site VPNs connect entire office networks permanently; client VPNs connect individual remote workers to the company network.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "Forward proxy vs reverse proxy",
		Overview: "A forward proxy forwards requests on behalf of clients (hiding the client's IP); a reverse proxy forwards requests on behalf of servers (hiding server topology).",
		Question: "A company routes all employee internet traffic through a security appliance for monitoring. nginx sits in front of web servers and routes incoming user requests to backends. These are two types of the same concept — what is it, and what are the two directions?",
		Explanation: "A proxy is a middleman for network traffic. A forward proxy speaks on behalf of clients: all outbound traffic goes through it (corporate monitoring, VPNs, content filtering). The internet sees the proxy's IP, not your laptop's. A reverse proxy speaks on behalf of servers: incoming traffic hits the proxy first, then gets forwarded to a backend. Users see one address; the proxy decides which backend handles each request. nginx, HAProxy, and Cloudflare are commonly used as reverse proxies; Zscaler and Squid as forward proxies.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "CDN (Content Delivery Network)",
		Overview: "A CDN is a geographically distributed network of edge servers that caches content close to users, reducing latency and origin server load.",
		Question: "A website serves users in Asia, Europe, and North America. A single server in the US gives overseas users high latency. What technology puts copies of content in servers worldwide so users get served from nearby?",
		Explanation: "A CDN (Content Delivery Network) has hundreds of servers ('edge nodes') globally. When a user in Tokyo requests an image, they get it from a nearby Tokyo server with ~5ms latency instead of ~150ms to a US origin server. The origin only gets requests when the edge doesn't have a cached copy. CDNs also absorb DDoS attacks by spreading traffic across thousands of nodes — one server being overwhelmed doesn't bring down the whole site. Cloudflare, AWS CloudFront, and Fastly are major CDN providers.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Fundamentals",
		Name:     "Port numbers and sockets",
		Overview: "A port is a 16-bit number that identifies a specific service within a host; a socket is the combination of IP address and port that uniquely identifies one endpoint of a network connection.",
		Question: "A web server and an SSH server both run on the same machine with the same IP address. When a packet arrives, how does the OS know which program should receive it?",
		Explanation: "Port numbers (0–65535) distinguish services on the same IP address. Port 80 = HTTP, 443 = HTTPS, 22 = SSH by convention. When a packet arrives, the OS reads the destination port and delivers it to whichever program has 'bound' (reserved) that port. Ports 0–1023 are reserved ('well-known ports') and require root/admin to bind. Your browser opens a random high-numbered port (e.g., 54321) for each connection; the server listens on the fixed well-known port and responds back to your random port.",
	},

	// ── Security & Crypto ─────────────────────────────────────────────────────

	{
		Category: "Security & Crypto",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Public-key cryptography & PKI",
		Overview: "RSA and elliptic-curve crypto let two parties establish a shared secret without ever meeting — certificates bind a public key to an identity.",
		Question: "Two strangers on the internet want to communicate securely. They've never met before and share no secret. How can they establish an encrypted channel without an attacker being able to decrypt it?",
		Explanation: "Public-key cryptography uses a key pair: a public key you share with everyone, and a private key you never share. Anything encrypted with your public key can only be decrypted with your private key — like a padlock anyone can snap shut, but only you have the key to open. PKI (Public Key Infrastructure) is the system of Certificate Authorities (CAs) that vouch for who owns which public key. Your browser comes pre-installed with ~150 trusted CAs; when a website shows a padlock, one of them has confirmed 'this public key really belongs to this domain'.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Common vulnerability classes (buffer overflow, injection, IDOR, SSRF)",
		Overview: "Most real-world vulnerabilities are variations of three root causes: trusting input, confusing data and code, and broken access control.",
		Question: "An attacker types `' OR '1'='1` as a username in a login form and gets into the database without knowing any password. What class of vulnerability is this, and what is the root cause?",
		Explanation: "This is SQL injection — the attacker's input is treated as SQL code rather than as data. The fix is parameterised queries: you pass the user input separately so the database driver never interprets it as code. SSRF (Server-Side Request Forgery) tricks a server into making requests to internal systems on the attacker's behalf — often used to reach internal APIs or metadata services like AWS's `169.254.169.254`. IDOR (Insecure Direct Object Reference) is simply changing an ID in a URL (e.g., `/orders/1234` → `/orders/1235`) to access another user's data — pure missing access control.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Supply chain trust (SBOMs, signing, reproducible builds)",
		Overview: "Modern attacks target your dependencies, not your code — knowing what you ship and verifying its provenance is now a baseline expectation.",
		Question: "The SolarWinds attack injected malicious code into a legitimate software update that passed all code reviews. The update was even cryptographically signed. What modern practices would have made this detectable?",
		Explanation: "An SBOM (Software Bill of Materials) is a complete ingredient list of every library and version in your software — like food nutritional labels. With an SBOM, you can automatically scan for known vulnerabilities in every dependency. Reproducible builds mean that given the same source code and build environment, the output binary is byte-for-byte identical — making it detectable if a build server secretly inserts malicious code. Sigstore's `cosign` tool signs container images with keys tied to verified CI/CD identities, creating a public audit trail.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "OAuth 2 / OIDC",
		Overview: "The standard protocol for delegated authorisation and federated identity — understanding the token flows prevents the most common auth bugs.",
		Question: "When you click 'Sign in with Google' on a third-party website, Google handles authentication but the website never receives your Google password. What protocol makes this secure delegation possible?",
		Explanation: "OAuth 2 lets you authorise a website to act on your behalf without giving it your password — like giving a valet a key that only opens the car door, not your house. The website redirects you to Google; Google authenticates you in Google's own system; Google sends the website a temporary 'access token' proving you authorised it. OIDC (OpenID Connect) adds an ID token — a signed document (JWT) with your name and email — so the website knows who you are. Your Google password never leaves Google.",
	},

	// ── Security & Crypto (new topics) ────────────────────────────────────────

	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Symmetric encryption (AES)",
		Overview: "Symmetric encryption uses the same key for both encryption and decryption; AES (Advanced Encryption Standard) is the modern standard operating on 128-bit blocks.",
		Question: "Your browser and a web server use the same key to encrypt and decrypt HTTPS traffic between them. Both sides 'lock' and 'unlock' with the identical key. What type of encryption is this?",
		Explanation: "Symmetric encryption uses one shared key for both locking (encrypting) and unlocking (decrypting) — like a padlock where both parties have identical copies of the key. AES (Advanced Encryption Standard) is the modern standard: it scrambles data using a 128-, 192-, or 256-bit key through multiple rounds of mixing. It's so fast that modern CPUs have dedicated hardware instructions for it (AES-NI). The challenge: how do you securely share the key in the first place? That's what asymmetric key exchange (like Diffie-Hellman) solves before TLS sessions start.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Cryptographic hash functions (SHA-256, MD5)",
		Overview: "A cryptographic hash function maps arbitrary-length input to a fixed-size digest, designed to be one-way and collision-resistant — changing one bit completely changes the output.",
		Question: "A download site shows a SHA-256 checksum next to a file. After downloading, you compute the same checksum and compare. What property of this function makes it suitable for detecting tampering — and why is MD5 no longer safe for this?",
		Explanation: "A cryptographic hash function takes any input (a file, a password, a message) and produces a fixed-length fingerprint. The same input always produces the same fingerprint; even a 1-bit change completely scrambles it. This lets you verify integrity: if the checksum matches, the file is unmodified. MD5 is broken: researchers can craft two completely different files with the same MD5 hash (a collision), so an attacker could replace a legitimate file with malicious content and show the same checksum. SHA-256 has no known practical collisions.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Digital signatures (RSA-PSS, ECDSA, Ed25519)",
		Overview: "A digital signature is created by hashing a message and encrypting the hash with a private key; any holder of the corresponding public key can verify the signature, proving authenticity and integrity.",
		Question: "You receive a software update that claims to be from Mozilla. How can you mathematically verify it was actually created by Mozilla's private key and hasn't been tampered with since?",
		Explanation: "Mozilla hashes the file (SHA-256), then encrypts that hash with their private key — the result is the signature. To verify: decrypt the signature with Mozilla's widely-published public key, hash the file yourself, and compare. If they match, only someone with Mozilla's private key could have produced that signature — no one else can fake it. This same mechanism is used in code signing, SSH host verification, git commit signing, and TLS certificates. Ed25519 is a modern algorithm that produces small (64-byte) signatures very quickly.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "HMAC (Hash-based Message Authentication Code)",
		Overview: "HMAC combines a secret key with a hash function to produce a message authentication code, proving both integrity and authenticity.",
		Question: "GitHub sends your server a webhook. In the HTTP header is a signature you can verify against the payload. This proves the webhook is genuine and wasn't tampered with in transit. What algorithm is commonly used for this?",
		Explanation: "HMAC (Hash-based Message Authentication Code) proves that a message was created by someone who knows a shared secret key — and that the message wasn't modified in transit. The sender computes `HMAC-SHA256(secret_key, message_body)` and sends it as a header. You compute the same thing independently; if they match, the message is authentic and unmodified. GitHub, Stripe, and Twilio all use HMAC-SHA256 for webhook signatures. Unlike a regular hash, an HMAC can't be forged without knowing the secret key.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Password hashing / KDFs (bcrypt, PBKDF2, Argon2)",
		Overview: "A key derivation function deliberately slows computation and adds memory hardness to make brute-force attacks expensive; bcrypt, PBKDF2, and Argon2 are the standard choices.",
		Question: "A database of user passwords is stolen. Passwords stored as plain SHA-256 hashes get cracked in minutes. Passwords stored with Argon2 are still safe weeks later. What makes Argon2 resistant to mass cracking?",
		Explanation: "SHA-256 of a password takes microseconds — a GPU can try billions of guesses per second, cracking common passwords almost instantly. Argon2id is intentionally slow AND requires a lot of memory (64+ MB) per hash attempt, making GPU cracking impractical. A 'salt' (unique random string added before hashing) ensures two users with identical passwords get different hashes, preventing lookup tables. The current recommendation is Argon2id with high memory settings — if you see bcrypt in existing code, that's good too; if you see plain SHA-256 for passwords, that's a problem.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Perfect forward secrecy (PFS / ECDHE)",
		Overview: "PFS ensures that compromising a server's long-term private key does not expose previously recorded encrypted sessions, because each session uses an ephemeral key pair discarded after the handshake.",
		Question: "An attacker records all HTTPS traffic to a website for years. Later they steal the server's private key. Which TLS feature — if enabled — means they still cannot decrypt those old recorded sessions?",
		Explanation: "Perfect Forward Secrecy (PFS) means past sessions stay secret even if the server's permanent key is later stolen. It works by generating a fresh temporary (ephemeral) key pair for each new connection using Diffie-Hellman math: both sides compute the same session key independently, then throw those temporary values away. The session key is never transmitted or stored anywhere. TLS 1.3 (2018) made PFS mandatory — all its cipher suites use ephemeral key exchange (ECDHE). This is why keeping TLS 1.3 enabled matters more than just 'having HTTPS'.",
	},
	{
		Category: "Security & Crypto",
		Layer:    "Security & Cryptography",
		Name:     "Certificate Transparency (CT logs)",
		Overview: "CT is a public, append-only log of all issued TLS certificates; browsers require a Signed Certificate Timestamp (SCT) proving a certificate was logged, making mis-issued certificates detectable.",
		Question: "A rogue Certificate Authority secretly issues a fake TLS certificate for your domain. Before Certificate Transparency, you might never find out. How does CT allow domain owners to detect this within minutes?",
		Explanation: "Certificate Transparency (CT) is a public, append-only audit log of every TLS certificate ever issued. When a CA issues a certificate, it must submit it to at least two CT logs and include proof (an SCT) in the certificate — Chrome and other browsers reject certificates without a valid SCT. Domain owners can monitor CT logs (e.g., via crt.sh) and receive alerts if any unexpected certificates are issued for their domain. This has effectively ended the era of silent fake-certificate attacks — any rogue issuance quickly becomes visible to the world.",
	},

	// ── Observability ─────────────────────────────────────────────────────────

	{
		Category: "Observability",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Structured logging",
		Overview: "Machine-readable JSON logs with consistent field names are the difference between grep and a query — crucial when you have more than one server.",
		Question: "Your application logs millions of lines per day across 50 servers. Finding all errors for a specific user requires searching through gigabytes of text. What logging approach makes this instant instead of painful?",
		Explanation: "Structured logging formats each log line as JSON with consistent field names: `{\"level\":\"error\",\"user\":\"alice\",\"service\":\"payments\"}`. A log aggregator (Datadog, Loki, Elasticsearch) indexes every field and lets you query `user='alice' AND level='error'` across billions of lines in milliseconds. Unstructured text (`ERROR: alice failed`) requires slow regex. Adding a `trace_id` field to every log line lets you collect all log lines for one user's request across 20 services — like using a tracking number to follow a package.",
	},
	{
		Category: "Observability",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Distributed tracing (OpenTelemetry)",
		Overview: "A trace ID propagated through every service call lets you reconstruct the full path of a request across dozens of microservices.",
		Question: "A user's API request takes 3 seconds. Your own service responds in 50ms — the rest is wait time somewhere in the chain of microservices. How do you find the slow service without manually adding logs to every service?",
		Explanation: "Distributed tracing follows a request end-to-end across all services. Each service creates a 'span' (operation name + start/end time) and passes a trace ID in HTTP headers to the next service. All spans from one request share that trace ID. A trace viewer (Jaeger, Honeycomb, Tempo) shows them as a timeline: your 50ms span is tiny; a 2.8-second span from the database service reveals the culprit. OpenTelemetry is the standard SDK for adding tracing to any language — you add it once and immediately see where time is actually spent.",
	},
	{
		Category: "Observability",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Metrics & the RED method (Rate, Errors, Duration)",
		Overview: "Rate, Errors, and Duration per service tell you immediately whether it is healthy — everything else is context you look at after an alert fires.",
		Question: "You want to know at a glance whether your web service is healthy or degraded — without looking at CPU, memory, or disk. What three metrics give the clearest picture?",
		Explanation: "The RED method is: Rate (how many requests per second is the service handling?), Errors (what fraction of requests are failing?), and Duration (how long do requests take — P50, P95, P99?). These three numbers immediately tell you if something is wrong: rate drop = traffic lost; error spike = something is broken; duration increase = performance regression. Prometheus collects these as counters and histograms; Grafana visualises them. Alert when these breach your agreed limits — not on CPU/memory, which are causes, not user-visible symptoms.",
	},
	{
		Category: "Observability",
		Layer:    "Bonus Layer — Security & Observability",
		Name:     "Alerting vs dashboards",
		Overview: "Alerts should fire on user-visible symptoms (SLO breaches), not causes — dashboards are for diagnosis after an alert fires, not primary monitoring.",
		Question: "Your team has beautiful dashboards but still gets surprised by outages — nobody is watching dashboards at 3am. What is the right approach to monitoring that wakes someone up only when a real problem occurs?",
		Explanation: "Dashboards are reactive — you look at them after you've been told something is wrong. Alerts are proactive — they notify you the moment a problem starts. Good alerts fire on user-visible symptoms (e.g., 'P99 latency exceeded 1s for 5 minutes') not on causes ('CPU is 80%' — might be completely fine). The SRE approach is to define an SLO (e.g., '99.9% of requests complete under 500ms'), track how fast you're burning through that budget, and alert only when the burn rate threatens to exhaust it — minimising false alarms.",
	},

	// ── HTTP & Web APIs ───────────────────────────────────────────────────────

	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP methods (GET, POST, PUT, PATCH, DELETE)",
		Overview: "HTTP methods (verbs) declare the intended action: GET fetches, POST creates/submits, PUT fully replaces, PATCH partially updates, DELETE removes; HEAD and OPTIONS are used for metadata and capability checks.",
		Question: "You're designing a REST API. A client wants to update only a user's email address without touching anything else. Which HTTP method is designed for partial updates — and how does it differ from PUT?",
		Explanation: "PUT replaces the entire resource: if you PUT a user object, you must send all fields or the missing ones get wiped. PATCH updates only the fields you send — ideal for 'just change the email'. GET is safe and idempotent (calling it ten times changes nothing). POST is for creating new resources or submitting forms; the server decides the new resource's URL. DELETE removes a resource. OPTIONS is used by browsers in CORS preflight requests to ask the server what methods are allowed.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP status codes (2xx, 3xx, 4xx, 5xx)",
		Overview: "Status codes are 3-digit numbers grouped by class: 2xx success, 3xx redirect, 4xx client error, 5xx server error — the most important ones are 200, 201, 204, 301, 400, 401, 403, 404, 429, 500, 502, 503.",
		Question: "Your API returns a 403 when a logged-in user tries to access another user's data. A colleague says it should be 401. What is the exact difference between 401 Unauthorized and 403 Forbidden?",
		Explanation: "401 means 'I don't know who you are — please authenticate first' (missing or invalid credentials). 403 means 'I know exactly who you are, but you are not allowed to do this' (authenticated but not authorised). Other essentials: 200 = OK, 201 = Created (new resource), 204 = No Content (success with no body, e.g., DELETE), 400 = Bad Request (malformed input), 404 = Not Found, 429 = Too Many Requests (rate limited), 500 = Internal Server Error (your bug), 502 = Bad Gateway (upstream server failed), 503 = Service Unavailable (overloaded/down).",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP request and response structure",
		Overview: "An HTTP request has a request line (method + URL + version), headers, a blank line, and an optional body; the response has a status line, headers, a blank line, and a body.",
		Question: "You open your browser's DevTools Network tab and inspect a request. You see the method, URL, a list of key-value pairs, and the actual data being sent. What are the four parts that make up an HTTP request?",
		Explanation: "Every HTTP request starts with a request line: `POST /api/users HTTP/1.1`. Next come headers — key-value pairs like `Content-Type: application/json` and `Authorization: Bearer abc123`. A blank line separates headers from the body, which contains the actual data (e.g., a JSON object). The response mirrors this: a status line (`HTTP/1.1 200 OK`), response headers (including `Content-Type` of the response), a blank line, and the response body (the HTML, JSON, or file). You can see all of this raw using `curl -v https://example.com`.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "CORS (Cross-Origin Resource Sharing)",
		Overview: "CORS is a browser security mechanism that blocks JavaScript from making requests to a different origin (domain/port/protocol) unless the server explicitly permits it via Access-Control-Allow-Origin headers.",
		Question: "Your React app at `app.example.com` makes a `fetch()` call to `api.example.com`. The browser blocks it with a CORS error even though both are your domains. Why does this happen and how do you fix it?",
		Explanation: "Browsers enforce the Same-Origin Policy: JavaScript can only make requests to the exact same origin (same scheme, domain, and port) as the page it runs on. This prevents a malicious website from silently making requests to your bank using your stored cookies. CORS lets a server relax this policy: `Access-Control-Allow-Origin: https://app.example.com` in the response tells the browser 'this other origin is allowed'. For non-simple requests (PUT, DELETE, or custom headers), the browser first sends a preflight OPTIONS request to check permissions before the real request.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP cookies and session management",
		Overview: "HTTP is stateless — each request carries no memory of previous ones; cookies are small key-value strings the server sets and the browser resends automatically on every request to maintain state like login sessions.",
		Question: "HTTP is stateless — the server forgets you between requests. Yet websites remember you're logged in across dozens of page loads and even after closing the browser. What mechanism enables this?",
		Explanation: "When you log in, the server creates a session and sends back a `Set-Cookie: session_id=abc123; HttpOnly; Secure` header. Your browser stores this cookie and automatically includes it in every subsequent request to that domain: `Cookie: session_id=abc123`. The server looks up `abc123` in its session store and knows who you are. `HttpOnly` prevents JavaScript from reading the cookie (protects against XSS theft). `Secure` means the cookie is only sent over HTTPS. `SameSite=Strict` prevents the cookie from being sent in cross-site requests, blocking CSRF attacks.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP caching (Cache-Control, ETags, Last-Modified)",
		Overview: "HTTP caching lets browsers and CDNs store responses and reuse them, controlled by Cache-Control directives; ETags and Last-Modified enable conditional requests to avoid re-downloading unchanged content.",
		Question: "A user visits your website twice in 5 minutes. The second visit loads the CSS and images instantly from the browser cache — no network requests. Which HTTP headers tell the browser how long it can cache a response?",
		Explanation: "`Cache-Control: max-age=3600` tells browsers to use the cached copy for up to 1 hour without asking the server. After expiry, the browser sends a conditional request with `If-None-Match: <etag>` (a fingerprint of the content). If the file hasn't changed, the server replies `304 Not Modified` with no body — fast and bandwidth-free. `Cache-Control: no-store` disables caching entirely (for sensitive pages like account settings). CDNs also honour these headers, serving cached copies to users worldwide without hitting your origin server.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "REST API design principles",
		Overview: "REST (Representational State Transfer) organises APIs around resources identified by URLs and manipulated using standard HTTP methods, with stateless requests and predictable conventions.",
		Question: "You're designing an API to manage users and their orders. How do you structure the URLs and choose HTTP methods so any developer can intuitively understand what each endpoint does — without reading documentation?",
		Explanation: "REST uses URLs to name things (nouns) and HTTP methods to describe actions (verbs): `GET /users` lists all users, `POST /users` creates one, `GET /users/42` fetches user 42, `PATCH /users/42` updates it, `DELETE /users/42` removes it. Nested resources: `GET /users/42/orders` lists user 42's orders. Stateless means every request contains all information needed — no server-side session. Responses use standard status codes: 200 for success, 201 for created, 404 for not found. This predictability means any developer familiar with REST immediately knows how your API works.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP authentication (Basic Auth, Bearer tokens, API keys)",
		Overview: "HTTP authentication passes credentials with each request: Basic Auth encodes username:password in Base64, Bearer tokens send a signed token in the Authorization header, and API keys use a custom header or query parameter.",
		Question: "Your API needs to verify who is making each request without asking for a username and password every time. What are the three most common ways to authenticate HTTP API requests?",
		Explanation: "Basic Auth sends `Authorization: Basic base64(username:password)` — simple but requires HTTPS since Base64 is trivially reversible. Bearer tokens send `Authorization: Bearer <jwt_or_opaque_token>` — the token is typically issued after login and expires; JWTs are self-contained (the server verifies the signature without a database lookup). API keys are long random strings sent in a header (`X-API-Key: abc123`) or query param — simple for server-to-server calls but must be kept secret. For user-facing apps, always prefer tokens (short-lived, revocable) over long-lived API keys.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "Idempotency in HTTP",
		Overview: "An idempotent operation produces the same result no matter how many times it is repeated; GET, PUT, HEAD, and DELETE are idempotent; POST is not — repeating it creates duplicates.",
		Question: "A client sends a DELETE request. The network times out before the response arrives. The client retries. Is it safe to retry DELETE? What HTTP property determines whether a method is safe to retry?",
		Explanation: "Idempotent means 'doing it multiple times has the same effect as doing it once'. DELETE /users/42 ten times and user 42 is still gone after the first call — the retries are harmless. GET never changes data. PUT replaces a resource to the same final state each time. POST is NOT idempotent: POST /orders ten times creates ten orders. This matters for retry logic in networks, proxies, and message queues. For POST, use idempotency keys: send a unique `Idempotency-Key: <uuid>` header and the server deduplicates retries — this is how Stripe prevents double-charging.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP redirects (301, 302, 307, 308)",
		Overview: "Redirect responses (3xx) tell clients to resend the request to a different URL; 301/308 are permanent (cached by browsers and search engines), 302/307 are temporary; 307 and 308 preserve the original HTTP method.",
		Question: "Your website moves permanently from HTTP to HTTPS. You want browsers to update their bookmarks and search engines to update their index to the new URL forever. Which redirect code do you use — and which code should you avoid for a permanent move?",
		Explanation: "301 is a permanent redirect: browsers and search engines cache it and update stored URLs — use this for permanent moves like HTTP→HTTPS. 302 is temporary: clients re-check on every visit, nothing is cached — use this for temporary redirects (e.g., during maintenance). The catch: historically, browsers changed POST to GET on 301 and 302, breaking form submissions. 307 (temporary) and 308 (permanent) fix this by explicitly preserving the HTTP method — a POST to a 307 URL stays a POST to the new URL. When in doubt for permanent redirects, use 308 if you care about method preservation.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "Content-Type and Accept headers",
		Overview: "Content-Type declares the format of the body being sent; Accept declares the formats the sender is willing to receive — together they enable content negotiation between clients and servers.",
		Question: "A client POSTs JSON data to your server. Later, a different client sends form data to the same endpoint. Your server needs to know which format to parse. Which HTTP header tells the server what format the request body is in?",
		Explanation: "`Content-Type: application/json` tells the server the request body is JSON; `Content-Type: application/x-www-form-urlencoded` means it's HTML form data; `Content-Type: multipart/form-data` means a file upload. The `Accept` header works in reverse: `Accept: application/json` tells the server the client wants a JSON response (not HTML or XML). If you forget `Content-Type: application/json` when sending JSON, most servers will refuse to parse it and return 400 Bad Request. You can verify these headers in the browser's DevTools under the Network tab.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTP keep-alive and connection reuse",
		Overview: "HTTP keep-alive (persistent connections) allows multiple request-response pairs to share a single TCP connection, eliminating the TCP handshake overhead for each request.",
		Question: "A webpage loads 50 resources (HTML, CSS, JS, images) from the same server. Opening a new TCP connection for each one would be very slow. What HTTP feature allows these to reuse the same connection?",
		Explanation: "Each TCP connection setup (three-way handshake + TLS handshake) costs at least one round trip — say 50ms. Multiplied by 50 resources, that's 2.5 seconds just in handshakes before any data arrives. HTTP/1.1 made keep-alive the default: the connection stays open after a response, ready for the next request. HTTP/2 goes further with multiplexing: dozens of requests can be in-flight simultaneously on one connection, removing the need for the browser's workaround of opening 6 parallel connections. Connection pools (in Go's `http.Client`, nginx, databases) reuse persistent connections for performance.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "Rate limiting and the 429 status code",
		Overview: "Rate limiting restricts how many requests a client can make in a time window to protect server resources; 429 Too Many Requests signals that the limit has been exceeded, with Retry-After indicating when the client may try again.",
		Question: "Your public API is receiving 10,000 requests per second from one client's buggy retry loop, starving all other users. What HTTP mechanism signals 'slow down' to clients, and what headers communicate when they can retry?",
		Explanation: "When a client exceeds its rate limit, the server returns `429 Too Many Requests` with a `Retry-After: 30` header (wait 30 seconds) or `X-RateLimit-Reset: <timestamp>`. Good APIs also send `X-RateLimit-Limit: 1000` (total allowed) and `X-RateLimit-Remaining: 0` (left in the window) on every response so clients can self-throttle before hitting the limit. Algorithms for rate limiting include token bucket (smooth burst allowance) and sliding window (precise per-minute counts). API gateways like Kong, AWS API Gateway, and nginx can apply rate limiting before requests even reach your code.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "JSON and HTTP API data formats",
		Overview: "JSON (JavaScript Object Notation) is the de-facto standard for HTTP API request and response bodies — lightweight, human-readable, and natively supported by every language; alternatives include XML, Protocol Buffers, and MessagePack.",
		Question: "When building a REST API, clients and servers need to agree on how data is formatted in request and response bodies. What format do most modern web APIs use, and what are its trade-offs vs alternatives?",
		Explanation: "JSON uses text: `{\"name\":\"Alice\",\"age\":30}`. Every language can parse it, it's human-readable in DevTools and logs, and you need no special tooling. The downsides: it's verbose (field names repeat in every object), doesn't have a schema by default, and parsing large JSON is slower than binary formats. Protocol Buffers (protobuf) and MessagePack are binary alternatives: 3–10× smaller and faster to parse, but not human-readable — used in high-throughput internal services. GraphQL and gRPC are entire API paradigms built on JSON and protobuf respectively, solving different problems like over-fetching and strong typing.",
	},
	{
		Category: "Networking",
		Layer:    "HTTP & Web APIs",
		Name:     "HTTPS and HTTP Strict Transport Security (HSTS)",
		Overview: "HTTPS is HTTP tunnelled through TLS; HSTS is a response header that instructs browsers to only ever connect to a domain over HTTPS, even if the user types http://, preventing downgrade attacks.",
		Question: "A user types `http://yourbank.com` (without the 's'). Without any protection, an attacker on the same network could intercept that first plain HTTP request. What header makes the browser always use HTTPS for a domain, forever?",
		Explanation: "`Strict-Transport-Security: max-age=31536000; includeSubDomains` tells the browser: 'For the next year, never connect to this domain over plain HTTP — always use HTTPS automatically'. The browser caches this and enforces it even before making a connection. HSTS preloading goes further: browsers ship with a hardcoded list of domains that must always use HTTPS, so even the very first ever visit is secure. Without HSTS, an attacker can intercept `http://` requests (SSL stripping) before the server redirects to HTTPS — you never know you've been downgraded.",
	},

	// ── Networking — Additional ───────────────────────────────────────────────

	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "BGP (Border Gateway Protocol)",
		Overview: "BGP is the routing protocol that connects autonomous systems (ISPs, cloud providers, companies) on the internet, exchanging reachability information so that every router knows how to reach every IP prefix.",
		Question: "The internet is made of thousands of separate networks (your ISP, AWS, Google, Cloudflare). How do these networks exchange routing information so that a packet from your laptop can find its way to a Google server across the globe?",
		Explanation: "Each major network operator is an Autonomous System (AS) with a unique ASN number. BGP is how ASes tell each other 'I can reach these IP address ranges'. When AWS announces `3.0.0.0/8` via BGP, every internet router learns that packets for 3.x.x.x addresses should head toward AWS. A BGP misconfiguration (called a BGP hijack) can accidentally or maliciously redirect large parts of internet traffic — this has happened with real incidents like Pakistan Telecom accidentally redirecting YouTube's IP space. BGP is one of the most critical and fragile pieces of internet infrastructure.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "IPv6 basics",
		Overview: "IPv6 uses 128-bit addresses (vs IPv4's 32-bit), written in eight groups of hex digits, providing effectively unlimited addresses and eliminating the need for NAT.",
		Question: "IPv4 addresses are running out — there are only ~4 billion possible addresses. What is the replacement, and what does an address in this protocol look like?",
		Explanation: "IPv6 uses 128-bit addresses written in hex: `2001:0db8:85a3:0000:0000:8a2e:0370:7334`. That's 2^128 ≈ 340 undecillion addresses — enough to give every atom on Earth its own IP. With IPv6, every device can have a globally unique, publicly routable address, making NAT unnecessary. The double-colon `::` shorthand compresses consecutive zero groups: `2001:db8::1`. IPv6 also simplifies routing (fewer NAT traversal headaches), improves multicast, and has built-in IPsec. Adoption is over 40% globally; most cloud providers and mobile networks support it natively.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "Network latency vs throughput",
		Overview: "Latency is the time for one packet to travel from source to destination (milliseconds); throughput is the volume of data transferred per second (Mbps/Gbps) — they are independent and often trade off against each other.",
		Question: "You have a 1 Gbps fiber connection but an HTTP request to a server 200ms away still takes 600ms to complete. High bandwidth didn't help. What networking concept explains why speed and distance are separate problems?",
		Explanation: "Latency is the round-trip time — limited by the speed of light across cables (~1ms per 100km). A 200ms round-trip means 200ms minimum wait regardless of bandwidth. Throughput is how much data flows per second once a connection is established. High-latency connections (satellite: 600ms RTT) are terrible for interactive requests even with high bandwidth, because every request waits for the round trip. This is why CDNs cache content near users (reduce latency) and why HTTP/2 multiplexing helps (reduces round trips). The classic analogy: a truck carrying 1 TB of hard drives has enormous throughput but terrible latency.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "TCP keep-alive and heartbeat",
		Overview: "TCP keep-alive sends periodic empty probes on an idle connection to detect if the remote end has disappeared without sending a FIN; application-level heartbeats serve the same purpose at the protocol layer.",
		Question: "A server keeps a database connection open. The database server crashes without sending a proper close signal. Hours later, the server tries to use the connection and hangs forever. What mechanism prevents this?",
		Explanation: "TCP keep-alive sends small empty probe packets on idle connections after a timeout (default: 2 hours on Linux, configurable). If no response comes after several retries, the OS declares the connection dead and returns an error to the application. You can set this to seconds in code: `net.Dialer{KeepAlive: 30 * time.Second}` in Go. Application-level heartbeats (like WebSocket ping/pong or gRPC keepalive) are more reliable because they work across NAT and firewalls that may silently drop TCP keep-alive probes. Connection pools always set short keep-alive timers to avoid reusing dead connections.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "Anycast routing",
		Overview: "Anycast assigns the same IP address to multiple servers in different locations; the network routes each client to the topologically nearest server, providing automatic failover and geographic load balancing at the IP layer.",
		Question: "Cloudflare's DNS resolver `1.1.1.1` is reachable from anywhere in the world with low latency — yet it's one IP address. Users in Tokyo don't connect to a server in the US. How does routing a single IP to the nearest of many servers work?",
		Explanation: "Anycast works by having multiple servers in different cities announce the same IP prefix via BGP. The internet's routing system naturally selects the 'closest' (lowest cost) path for each client — a Tokyo user's packets get routed to a Tokyo Cloudflare server, while a London user's go to a London server. This is how Cloudflare, Google (8.8.8.8), and root DNS servers handle global traffic with one IP. It also provides automatic failover: if the Tokyo datacenter goes down, BGP withdraws that announcement and traffic automatically reroutes to the next-nearest city.",
	},
	{
		Category: "Networking",
		Layer:    "Networking — Advanced",
		Name:     "MTU and packet fragmentation",
		Overview: "MTU (Maximum Transmission Unit) is the largest packet a network link can carry (typically 1500 bytes for Ethernet); packets larger than the path's MTU must be fragmented or the sender must use Path MTU Discovery to find the right size.",
		Question: "A VPN is working fine for small requests but large file transfers fail mysteriously. Ping works, but downloading a 10 MB file hangs. What networking concept involving packet size is likely the culprit?",
		Explanation: "Ethernet's MTU is 1500 bytes; VPN adds a header (~60 bytes), reducing the effective MTU to ~1440 bytes. Large packets that don't fit must either be fragmented (split into smaller pieces) or the sender must be told to send smaller packets via ICMP 'Fragmentation Needed' messages. If a firewall blocks ICMP (a common but wrong configuration), Path MTU Discovery breaks, causing large packets to be silently dropped — small packets (like ping) work fine, but large transfers hang. The fix is setting the TCP MSS (Maximum Segment Size) on the VPN interface to match the reduced MTU.",
	},

	// ── Linux ─────────────────────────────────────────────────────────────────

	{
		Category: "Linux",
		Layer:    "Linux — Filesystem",
		Name:     "Linux filesystem hierarchy (FHS)",
		Overview: "The Filesystem Hierarchy Standard defines where files live: /bin and /usr/bin for executables, /etc for config, /var for variable data (logs, caches), /home for users, /tmp for temporary files, /proc and /sys for live kernel data.",
		Question: "You install nginx on Linux. Its binary ends up in /usr/bin, its config in /etc/nginx, its logs in /var/log/nginx, and its web files in /var/www. What standard defines this predictable layout across all Linux distros?",
		Explanation: "The FHS (Filesystem Hierarchy Standard) is a convention that all major Linux distributions follow, so sysadmins always know where to look. Key directories: `/etc` (short for 'et cetera') holds all system configuration files; `/var` holds data that changes over time like logs and mail; `/tmp` is cleared on reboot; `/home` holds user files. `/proc` and `/sys` are special virtual filesystems — they look like directories but are really a live window into the running kernel, not files on any disk.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Filesystem",
		Name:     "Linux file permissions (chmod, chown, rwx)",
		Overview: "Linux permissions use three triplets (owner/group/others), each with read (r=4), write (w=2), execute (x=1) bits; chmod changes permissions, chown changes ownership, and octal notation like 755 means rwxr-xr-x.",
		Question: "You run `ls -la` and see `-rwxr-xr--` next to a file. Reading those nine characters, who can execute this file and who can only read it?",
		Explanation: "The nine characters after the file type split into three groups of three: `rwx` (owner can read/write/execute), `r-x` (group members can read and execute but not write), `r--` (everyone else can only read). Numbers work the same way: `chmod 755` = `rwxr-xr-x` because 7=rwx, 5=r-x. `chmod 600` = `rw-------` — only the owner can read/write, nobody else sees it (good for SSH private keys). `chown alice:developers file` changes both the owner to alice and the group to developers.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Filesystem",
		Name:     "Linux special permissions (setuid, setgid, sticky bit)",
		Overview: "Setuid makes an executable run as its owner (not the caller) — used by sudo and passwd; setgid on a directory makes new files inherit the directory's group; the sticky bit on /tmp means only the file's owner can delete it.",
		Question: "The `passwd` command lets ordinary users change their own password, which requires writing to /etc/shadow — a root-owned file. How can a normal user run passwd and write to a file they have no permission to touch?",
		Explanation: "The setuid bit (shown as `s` in `ls -la`) makes an executable run with its owner's privileges, not the caller's. `passwd` is owned by root with setuid set: when you run it, it temporarily becomes root — enough to update /etc/shadow — then exits. This is why `ls -la /usr/bin/passwd` shows `-rwsr-xr-x`. It's powerful and dangerous; vulnerabilities in setuid programs are prime targets because exploiting them gives an attacker root. The sticky bit on `/tmp` (shown as `t`) prevents users from deleting each other's temp files even though everyone has write access to the directory.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Users & Processes",
		Name:     "Linux user and group management",
		Overview: "Linux isolates services and users via UIDs and GIDs; useradd/usermod/userdel manage users, groupadd manages groups, and /etc/passwd, /etc/shadow, and /etc/group are the backing files.",
		Question: "On a production Linux server, nginx, postgres, and redis all run as separate unprivileged system users, not as root. Why — and what commands create these service accounts?",
		Explanation: "Running services as dedicated users limits blast radius: if nginx gets compromised, the attacker gains nginx's permissions (e.g., read web files) but cannot touch postgres's data directory, which is owned by the postgres user. `useradd --system --no-create-home nginx` creates a system account with no login shell and no home directory. `/etc/passwd` maps usernames to UIDs; `/etc/shadow` stores hashed passwords (readable only by root). The `id` command shows your current UID, GID, and group memberships.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Users & Processes",
		Name:     "sudo and privilege escalation",
		Overview: "sudo lets authorised users run specific commands as root (or another user) without knowing the root password; the /etc/sudoers file controls exactly who can run what, audited via syslog.",
		Question: "On Ubuntu, the root account is locked by default. When you need to install a package or edit a system config file, how do ordinary users perform these administrative tasks?",
		Explanation: "`sudo apt install nginx` runs the command as root for one invocation — your own password is required as a confirmation that you're present at the keyboard. The `/etc/sudoers` file (edited with `visudo`, never directly) defines rules like `alice ALL=(ALL:ALL) ALL` (alice can do everything) or `deploy ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart nginx` (the deploy user can only restart nginx, without a password prompt). Every sudo invocation is logged to syslog or journald, creating an audit trail — unlike logging in as root, where all actions are anonymous.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Users & Processes",
		Name:     "Linux processes (ps, top, htop, kill, nice)",
		Overview: "Every running program is a process with a PID, owner, CPU/memory usage, and state (running, sleeping, zombie); ps lists processes, top/htop show live usage, kill sends signals, and nice/renice adjust CPU priority.",
		Question: "A process is consuming 100% CPU and making your server unresponsive. What command shows you which process is the culprit, and how do you stop it?",
		Explanation: "`top` or `htop` shows a live-updating list of processes sorted by CPU usage — the offender will be at the top. Note its PID (process ID). `kill <PID>` sends SIGTERM (please shut down gracefully); if it ignores that, `kill -9 <PID>` sends SIGKILL (forced termination, cannot be ignored). `ps aux` gives a static snapshot: `ps aux | grep nginx` finds all nginx processes. `nice -n 10 command` starts a command at lower priority (less CPU greed); `renice +10 -p <PID>` lowers priority of a running process without killing it.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Users & Processes",
		Name:     "Linux signals in practice (kill, pkill, trap)",
		Overview: "Signals are async notifications to processes; common ones are SIGTERM (15, graceful stop), SIGKILL (9, forced stop), SIGHUP (1, reload config), SIGINT (2, Ctrl+C); bash trap lets scripts clean up on exit.",
		Question: "Your bash script creates temp files during its run. You want those files cleaned up automatically when the script is stopped with Ctrl+C or killed. What bash feature registers a cleanup function that runs on any signal?",
		Explanation: "`trap 'rm -f /tmp/mytemp; exit' INT TERM EXIT` registers a cleanup command that runs when the script receives SIGINT (Ctrl+C), SIGTERM (kill), or exits normally. Without this, killed scripts leave temp files behind. `pkill -f pattern` sends a signal to all processes whose command line matches a pattern — useful when you don't know the PID. `kill -HUP <PID>` sends SIGHUP, which many daemons (nginx, sshd) interpret as 'reload your config file without restarting' — a zero-downtime config reload.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "systemd and service management",
		Overview: "systemd is the init system and service manager on modern Linux distros; systemctl start/stop/restart/enable/disable manages services, and unit files define how services run, restart on failure, and depend on each other.",
		Question: "Your web server process crashes at 3am. You want it to restart automatically within seconds — without manual intervention. On a modern Linux system, what tool manages this, and how do you configure automatic restart?",
		Explanation: "`systemctl enable --now nginx` starts nginx immediately and registers it to start automatically on boot. A unit file at `/etc/systemd/system/myapp.service` with `Restart=always` and `RestartSec=3` makes systemd restart your app within 3 seconds of any crash. `systemctl status nginx` shows whether it's running, its recent log output, and its PID. `systemctl reload nginx` sends SIGHUP for a config reload; `systemctl restart nginx` does a full stop/start. `systemctl list-units --failed` shows everything that has failed.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "journald and log management (journalctl)",
		Overview: "systemd-journald collects structured logs from all services and the kernel into a binary journal; journalctl queries them with filters by service, time, priority, and boot, replacing scattered text log files.",
		Question: "On a modern Linux system with systemd, you want to see the last 100 log lines from the nginx service and follow new entries as they arrive in real time. What is the command?",
		Explanation: "`journalctl -u nginx -n 100 -f` shows the last 100 lines from the nginx service and follows new entries (`-f` is like `tail -f`). `journalctl -u nginx --since '1 hour ago'` narrows to the last hour. `journalctl -p err` shows only error-level and above messages across all services. `journalctl -b` shows logs since the last boot; `journalctl -b -1` shows the previous boot (useful after a crash). The journal stores structured metadata alongside the message — host, PID, timestamp, priority — enabling much richer queries than grepping plain text log files.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "cron and systemd timers",
		Overview: "cron runs commands on a schedule defined in crontab files (minute/hour/day/month/weekday); systemd timers are the modern alternative with better logging and dependency handling.",
		Question: "You need a database backup script to run automatically every night at 2am on a Linux server. What is the traditional Linux tool for scheduling recurring tasks, and how do you define the schedule?",
		Explanation: "`crontab -e` opens your personal cron schedule. A line like `0 2 * * * /usr/local/bin/backup.sh` runs the script at 2:00am every day — the five fields are minute (0), hour (2), day-of-month (*=every), month (*), day-of-week (*). `0 */6 * * *` runs every 6 hours; `*/15 * * * *` runs every 15 minutes. System-wide cron jobs live in `/etc/cron.d/`. Systemd timers are more powerful: they log to journald (so you can see if a backup failed), support dependencies, and can run missed jobs after a reboot with `Persistent=true`.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "SSH and key-based authentication",
		Overview: "SSH (Secure Shell) provides encrypted remote terminal access; key-based authentication replaces passwords with a private/public key pair — the server holds the public key, the client proves possession of the private key without sending it.",
		Question: "You need to connect to a remote Linux server securely from your laptop, and you want to automate deployments without typing a password. How do you set up passwordless SSH access?",
		Explanation: "`ssh-keygen -t ed25519` generates a key pair: `~/.ssh/id_ed25519` (private, never share this) and `~/.ssh/id_ed25519.pub` (public, safe to distribute). `ssh-copy-id user@server` appends your public key to `~/.ssh/authorized_keys` on the server. From then on, `ssh user@server` authenticates via a cryptographic challenge-response — the server sends a random challenge, your SSH client signs it with the private key, the server verifies with your public key. No password travels the network. Disable password auth entirely in `/etc/ssh/sshd_config` (`PasswordAuthentication no`) to eliminate brute-force attacks.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "Linux disk management and LVM",
		Overview: "LVM (Logical Volume Manager) adds a layer between physical disks and filesystems: physical volumes (PVs) join a volume group (VG), from which logical volumes (LVs) are carved out and can be resized, snapshotted, and extended online.",
		Question: "Your server's /var partition is full and causing log write failures. You have a spare disk. How do you add that disk's space to the existing partition without taking the server offline?",
		Explanation: "With LVM: `pvcreate /dev/sdb` initialises the new disk; `vgextend myvg /dev/sdb` adds it to the volume group; `lvextend -L +50G /dev/myvg/var` grows the logical volume; `resize2fs /dev/myvg/var` (for ext4) expands the filesystem to fill the new space — all while the server is running. Without LVM you'd need to unmount, repartition, and resize — risky and requires downtime. LVM snapshots (`lvcreate --snapshot`) capture a point-in-time copy of a volume for backups or rollbacks. `df -h` shows disk usage; `lsblk` shows the full disk/partition/LVM hierarchy.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — System Administration",
		Name:     "Linux swap space",
		Overview: "Swap is disk space the kernel uses as overflow when RAM is full — processes are paged out to swap to free RAM; it prevents OOM kills at the cost of dramatically slower access, since disk is ~10,000× slower than RAM.",
		Question: "Your Linux server has 8 GB RAM but a memory-hungry process needs 10 GB. Instead of crashing, the system slows down severely but keeps running. What mechanism provides this overflow capacity?",
		Explanation: "When RAM fills up, the Linux kernel moves the least-recently-used memory pages to a swap partition or swap file on disk (paging out). When that memory is needed again, it's read back from disk (paging in) — a process called swapping. The severe slowdown happens because disk is thousands of times slower than RAM. `free -h` shows RAM and swap usage; `swapon --show` lists active swap devices. `swappiness` (0–100) controls how aggressively Linux uses swap before running out of RAM — servers often set it to 10 to prefer keeping hot data in RAM and only swap under real pressure.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Internals",
		Name:     "/proc and /sys virtual filesystems",
		Overview: "/proc exposes live kernel and process data as readable files (e.g., /proc/meminfo, /proc/<pid>/status); /sys exposes hardware and kernel subsystem configuration; both are virtual — no bytes are written to disk.",
		Question: "You want to check how much memory a specific process is using, see the kernel's current TCP settings, and inspect a running process's open file handles — without installing any monitoring tools. Where does Linux expose all this live data?",
		Explanation: "`cat /proc/meminfo` shows current RAM usage. `cat /proc/1234/status` shows memory, CPU time, and state of process 1234. `ls /proc/1234/fd/` lists all open file descriptors — useful for finding 'ghost' files (deleted but still held open). `/sys/kernel/` and `/proc/sys/` expose kernel parameters: `cat /proc/sys/net/ipv4/ip_forward` shows if IP forwarding is enabled; `echo 1 > /proc/sys/net/ipv4/ip_forward` enables it instantly. `sysctl -w net.ipv4.ip_forward=1` does the same thing — both write to `/proc/sys/`. Changes to `/proc/sys/` are lost on reboot unless written to `/etc/sysctl.conf`.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Internals",
		Name:     "Linux kernel modules",
		Overview: "Kernel modules are chunks of kernel code that can be loaded and unloaded at runtime without rebooting; lsmod lists loaded modules, modprobe loads a module with its dependencies, rmmod unloads one.",
		Question: "Your Linux server needs to support a new network card, but recompiling the entire kernel just to add one driver is impractical. What mechanism allows adding new kernel functionality at runtime?",
		Explanation: "Kernel modules (`.ko` files) are pieces of kernel code compiled separately that can be inserted into and removed from a running kernel. `lsmod` lists currently loaded modules; `modprobe e1000e` loads the Intel network driver (and any modules it depends on); `rmmod e1000e` removes it. Most device drivers, filesystem drivers, and VPN implementations (WireGuard) ship as modules. `modinfo <module>` shows a module's description, parameters, and which kernel version it was built for. `dmesg | tail` shows kernel log messages — when you plug in a USB drive, you'll see the kernel loading the relevant module there.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Internals",
		Name:     "Linux boot process (BIOS → GRUB → kernel → initrd → systemd)",
		Overview: "Linux boots in five stages: firmware (BIOS/UEFI) → bootloader (GRUB) → kernel loads and initialises hardware → initrd mounts a temporary root filesystem with needed drivers → init/systemd starts all services.",
		Question: "A Linux server fails to boot. You see it posts (BIOS screen appears), GRUB menu shows, but then it hangs with 'VFS: Unable to mount root fs'. Which stage of the boot process failed, and why?",
		Explanation: "The five stages: (1) BIOS/UEFI runs POST and finds the boot disk. (2) GRUB loads the kernel from `/boot` into RAM. (3) The kernel initialises CPU, memory, and basic hardware. (4) The kernel unpacks the initrd (initial ramdisk) — a tiny temporary filesystem containing drivers needed to mount the real root filesystem. If the driver for your disk controller is missing from the initrd, the kernel can't find the disk, and you get 'unable to mount root fs'. (5) Once the real root is mounted, systemd starts as PID 1 and launches all services in dependency order.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Internals",
		Name:     "Linux filesystem types (ext4, xfs, btrfs, tmpfs)",
		Overview: "ext4 is the stable, widely-supported default; xfs excels at large files and parallel I/O; btrfs adds copy-on-write snapshots and checksums; tmpfs lives entirely in RAM and is gone on reboot.",
		Question: "You're setting up a new Linux server. The installer asks which filesystem type to use for the root partition. ext4, xfs, and btrfs are the main options. What are the practical differences for a typical server?",
		Explanation: "ext4 is the safe default — stable, well-understood, and supported by every recovery tool. xfs is excellent for large files and high I/O parallelism (databases, video storage). btrfs offers powerful features: copy-on-write snapshots (take a point-in-time copy of a volume instantly), transparent compression, and data checksums that detect silent bit rot — but it's more complex and historically less stable under high write load. tmpfs is special: it lives in RAM (`/tmp`, `/run`, `/dev/shm`), so reads/writes are RAM-speed, but all data disappears on reboot. Check filesystem usage with `df -Th` (the `-T` flag shows filesystem type).",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Internals",
		Name:     "Linux performance analysis (strace, lsof, perf)",
		Overview: "strace traces every system call a process makes in real time; lsof lists all open files and network connections; perf records CPU events and call stacks for profiling.",
		Question: "Your application is mysteriously slow and you suspect it's spending time in unexpected system calls. What Linux tool gives you a live trace of every kernel call your app makes, with arguments and timing?",
		Explanation: "`strace -p <PID>` attaches to a running process and prints every syscall: `read(3, ..., 4096) = 4096` (0.0003s). You'll immediately spot if it's stuck on `futex` (lock contention), `epoll_wait` (blocked waiting for I/O), or making thousands of tiny `read()` calls instead of one large one. `strace -c ./myapp` prints a sorted summary of syscall counts and time spent — great for a quick audit. `lsof -p <PID>` lists every file, socket, and pipe a process has open. `perf stat ./myapp` shows CPU counters (cache misses, branch mispredictions); `perf top` shows a live flame-graph-style view of where CPU time is spent.",
	},
	{
		Category: "Linux",
		Layer:    "Linux — Networking",
		Name:     "Linux networking commands (ip, ss, tcpdump)",
		Overview: "The modern Linux networking toolkit: `ip` manages interfaces, routes, and neighbours (replacing ifconfig); `ss` shows socket statistics (replacing netstat); `tcpdump` captures live network packets for inspection.",
		Question: "You want to see all open TCP connections and listening ports on your Linux server, find which process owns each socket, and capture packets on a specific port for debugging. Which three commands cover these tasks?",
		Explanation: "`ss -tlnp` shows all listening TCP sockets with the process name: `LISTEN 0 128 0.0.0.0:80 ... users:((\"nginx\",pid=1234,fd=6))`. `ip addr show` lists all network interfaces and their IPs (replaces `ifconfig`); `ip route show` shows the routing table. `tcpdump -i eth0 port 80 -A` captures all HTTP traffic on eth0 and prints it as ASCII — invaluable for debugging what's actually being sent. `tcpdump -w capture.pcap` saves to a file you can open in Wireshark. `netstat -tlnp` still works on most systems but is deprecated in favour of `ss`.",
	},

	// ── Virtualization ────────────────────────────────────────────────────────

	{
		Category: "Virtualization",
		Layer:    "Virtualization — Concepts",
		Name:     "Type 1 vs Type 2 hypervisors",
		Overview: "Type 1 (bare-metal) hypervisors run directly on hardware with no host OS (VMware ESXi, Hyper-V, KVM); Type 2 hypervisors run as an application inside a host OS (VirtualBox, VMware Workstation) — Type 1 is faster and used in production.",
		Question: "AWS EC2 runs thousands of customer VMs on bare metal with near-native performance. VirtualBox on your laptop runs VMs but noticeably slower. What fundamental architectural difference explains this performance gap?",
		Explanation: "Type 1 (bare-metal): the hypervisor IS the operating system — it boots directly on the hardware and has exclusive control of the CPU, RAM, and devices. Guest VMs share hardware with maximum efficiency; VMware ESXi, Microsoft Hyper-V, and KVM/Linux are Type 1. Type 2: the hypervisor is a regular application running inside a host OS (Windows, macOS). Every guest VM operation must first go through the host OS kernel — an extra layer of overhead. VirtualBox and VMware Workstation are Type 2. The line blurs with KVM: Linux with KVM loaded is technically Type 1, because the kernel itself becomes the hypervisor.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Concepts",
		Name:     "Full virtualisation vs para-virtualisation",
		Overview: "Full virtualisation presents a completely unmodified hardware interface to the guest OS; para-virtualisation modifies the guest OS to use a hypervisor API directly, eliminating the overhead of trapping privileged instructions.",
		Question: "A standard Linux VM is unaware it's running inside a hypervisor and performs at near-native speed. But some cloud VMs run even faster by having the guest OS 'cooperate' with the hypervisor using special drivers. What is this cooperation called?",
		Explanation: "In full virtualisation, every time the guest OS tries a privileged operation (like writing to a hardware register), the hypervisor must intercept it, emulate the hardware response, and resume the guest — adding latency. Para-virtualisation gives the guest OS special 'virtio' drivers that communicate directly with the hypervisor instead of pretending to talk to real hardware. A `virtio-net` network driver can push a packet to the hypervisor in one hypercall instead of emulating a real Ethernet card. The result: network and disk I/O can be 2–5× faster. Cloud VMs always install virtio drivers during setup.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Concepts",
		Name:     "VMs vs containers (isolation trade-off)",
		Overview: "VMs provide full OS isolation (separate kernel per VM, hardware boundary via hypervisor) at the cost of minutes to start and gigabytes of overhead; containers share the host kernel, start in milliseconds, and use megabytes — but offer weaker isolation.",
		Question: "A container starts in milliseconds using megabytes of RAM. A VM takes minutes and gigabytes. Yet cloud providers still charge more for VMs and enterprises still use them for critical workloads. What does a VM provide that a container fundamentally cannot?",
		Explanation: "A VM runs a completely separate kernel — a compromised container can potentially escape to the host by exploiting a kernel vulnerability (container escape), because both share the same kernel. A VM escape requires breaking out of the hypervisor, which has a much smaller attack surface. VMs also provide: different guest OSes on the same host (Windows VM on Linux host), legacy OS support, stronger compliance/regulatory guarantees (PCI-DSS often requires VM-level isolation), and hardware-level resource guarantees. Kata Containers and gVisor are hybrid approaches — they run containers inside lightweight VMs to get container speed with VM-level isolation.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Tools",
		Name:     "KVM (Kernel-based Virtual Machine)",
		Overview: "KVM is a Linux kernel module that turns Linux itself into a Type 1 hypervisor by exposing /dev/kvm; it uses CPU virtualisation extensions (Intel VT-x, AMD-V) so guest code runs directly on the CPU at near-native speed.",
		Question: "AWS, GCP, and most Linux-based cloud providers run VMs without a separate hypervisor product installed. Linux itself hosts the VMs. What built-in Linux kernel feature makes this possible?",
		Explanation: "KVM (Kernel-based Virtual Machine) is a module built into the Linux kernel since 2007. When loaded, it exposes `/dev/kvm` — a device file that QEMU and libvirt use to create and manage VMs. The CPU's hardware virtualisation extensions (Intel VT-x or AMD-V) allow guest code to run directly on the physical CPU in a sandboxed mode, only trapping to KVM for privileged operations. `lsmod | grep kvm` checks if it's loaded; `kvm-ok` (from cpu-checker) verifies your CPU supports it. AWS EC2 runs on KVM-based Nitro hypervisor; GCE uses KVM; most OpenStack deployments use KVM.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Tools",
		Name:     "QEMU and hardware emulation",
		Overview: "QEMU is an open-source machine emulator that provides virtual CPU, disk, network, and other devices for VMs; paired with KVM, QEMU handles device emulation while KVM handles CPU virtualisation — together they are the foundation of Linux-based virtualisation.",
		Question: "KVM handles CPU virtualisation efficiently, but a VM also needs a virtual disk, virtual network card, and virtual USB — none of which KVM provides. What open-source tool emulates all this hardware for KVM virtual machines?",
		Explanation: "QEMU (Quick EMUlator) can emulate an entire computer from scratch — CPU, motherboard, storage controllers, network cards, display. Without KVM, QEMU emulates the CPU in software (slow but supports any architecture: run ARM code on an x86 host). With KVM, QEMU hands off CPU execution to KVM and only emulates the I/O devices. `virsh`, `virt-manager`, and libvirt are management layers on top of QEMU+KVM. QEMU is also used in embedded development to run code for ARM or RISC-V microcontrollers on your x86 laptop without actual hardware.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Tools",
		Name:     "VMware ESXi and vSphere",
		Overview: "VMware ESXi is a Type 1 bare-metal hypervisor for enterprise data centers; vSphere is the management platform on top, providing live VM migration (vMotion), high availability, and resource scheduling across clusters of ESXi hosts.",
		Question: "An enterprise data centre needs to migrate a running VM from one physical server to another for hardware maintenance — with zero downtime for the application running inside. What VMware product provides this capability?",
		Explanation: "VMware vMotion (part of vSphere) migrates a running VM between ESXi hosts by copying its memory pages to the destination while the VM keeps running, then doing a final tiny sync and flipping traffic over — the VM never stops. This enables live hardware maintenance, load balancing, and disaster recovery. vSphere also provides High Availability (HA): if an ESXi host crashes, its VMs automatically restart on surviving hosts within minutes. DRS (Distributed Resource Scheduler) moves VMs between hosts automatically to balance CPU and memory load. This enterprise feature set is why VMware dominated data centers for decades despite the free alternatives.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Tools",
		Name:     "Vagrant for VM management",
		Overview: "Vagrant is a developer tool that provisions and manages VMs (via VirtualBox, VMware, or cloud providers) from a declarative Vagrantfile, enabling reproducible, disposable development environments with one command.",
		Question: "A team needs every developer to have an identical Linux development environment on their Mac or Windows laptop — same OS version, same packages, same config. Defining it in code so `one command creates everything`. What tool provides this for local VMs?",
		Explanation: "A `Vagrantfile` describes the VM: `config.vm.box = 'ubuntu/jammy64'`, `config.vm.network 'forwarded_port', guest: 3000, host: 3000`, and a shell provisioner that runs apt-install commands on first boot. `vagrant up` downloads the base box, creates the VM, and runs provisioning. `vagrant ssh` drops you into the VM. `vagrant destroy` removes it completely. Unlike Docker, Vagrant uses full VMs — you get a real Linux kernel, which matters for kernel-level development, network experimentation, or testing system configuration management tools like Ansible and Chef in isolation.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — WSL",
		Name:     "WSL 1 architecture (syscall translation)",
		Overview: "WSL 1 runs native Linux ELF binaries on Windows by translating Linux system calls to Windows NT kernel calls in real time — no VM is involved; it is fast to start but has filesystem and syscall compatibility limitations.",
		Question: "Windows Subsystem for Linux version 1 could run Linux programs (bash, grep, git) natively on Windows without a VM. How was this technically possible — Linux binaries normally require a Linux kernel?",
		Explanation: "WSL 1 contains a Linux syscall translation layer built into Windows. When a Linux ELF binary calls `read()` or `mmap()`, Windows intercepts the Linux syscall number and translates it to the equivalent Windows NT kernel call. There is no Linux kernel involved — Windows itself does the work. This made WSL 1 fast to start (no VM boot) and low on memory, but it had gaps: syscalls that map poorly to Windows NT (like `inotify`, `fork()`, some socket options) either didn't work or behaved differently. Filesystem performance for Windows files was great, but Linux files on the Windows NTFS drive were slow.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — WSL",
		Name:     "WSL 2 architecture (lightweight VM with real Linux kernel)",
		Overview: "WSL 2 runs a full Linux kernel inside a lightweight Hyper-V virtual machine; it has complete syscall compatibility, fast filesystem performance for Linux files, and starts in about 1 second thanks to VM state snapshots.",
		Question: "WSL 2 fixed the compatibility issues of WSL 1 by taking a completely different approach. Unlike WSL 1, WSL 2 runs an actual Linux kernel. How is a real Linux kernel running on Windows without the 2-minute boot time of a traditional VM?",
		Explanation: "WSL 2 uses Hyper-V (Microsoft's Type 1 hypervisor, built into Windows 10/11) to run a lightweight VM containing a Microsoft-maintained Linux kernel. The trick for fast startup: the VM's memory state is preserved between sessions (like a suspended VM), so starting WSL 2 resumes the snapshot in ~1 second rather than booting from scratch. The Linux kernel is a real, unmodified kernel — so all syscalls work correctly, Docker Desktop can run real containers inside it, and you can run `dmesg`, load kernel modules, and do anything you'd do on a real Linux system. File access between Windows and Linux goes through a 9P protocol virtio connection.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — WSL",
		Name:     "WSL 2 integration with Windows (networking, filesystem, GUI)",
		Overview: "WSL 2 integrates with Windows via a shared /mnt/c drive for Windows files, automatic port forwarding so localhost works from both sides, and WSLg for running Linux GUI apps natively on the Windows desktop.",
		Question: "You're using WSL 2 and start a web server on port 3000 inside Linux. You open a browser on Windows and type localhost:3000 — and it works. You also run a Linux GUI app and it opens as a regular Windows window. How does WSL 2 bridge these two worlds?",
		Explanation: "WSL 2 automatically sets up port forwarding: anything listening on a port in the Linux VM is accessible via localhost on Windows, and vice versa. Windows files are mounted at `/mnt/c/`, `/mnt/d/` etc. — Linux can read and write Windows files, but NTFS doesn't support Linux permissions, so files there always show as 777. Linux's native ext4 filesystem (stored in a VHD file) is much faster for Linux tools. WSLg (WSL GUI) runs an invisible Wayland/X11 compositor inside the VM and connects to Windows' RDP display subsystem — Linux GUI apps like gedit, VS Code Server, or even full desktop environments appear as native Windows windows.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Advanced",
		Name:     "VM snapshots and live migration",
		Overview: "A snapshot captures a VM's complete state (memory, disk, CPU registers) at a point in time for instant rollback; live migration moves a running VM between physical hosts with zero downtime by iteratively copying memory.",
		Question: "Before applying a risky kernel update to a production VM, you want to save its complete state so you can roll back to exactly this moment if anything breaks. What VM feature enables this instant point-in-time save?",
		Explanation: "A snapshot freezes the VM's entire state: a copy of RAM contents, the CPU register state, and a point-in-time copy of the disk (using copy-on-write so only changes are stored). Reverting takes seconds — the VM jumps back to that exact moment, undoing all changes. Live migration (vMotion, KVM live migrate) does the opposite in time: it copies the VM's memory to another host while the VM keeps running, page by page, then does one final tiny sync and switches the VM over. The VM experiences less than 100ms of freeze time. This is how cloud providers do hardware maintenance without rebooting customer VMs.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Advanced",
		Name:     "Nested virtualisation",
		Overview: "Nested virtualisation allows a VM to itself act as a hypervisor and run guest VMs inside — requiring the outer hypervisor to expose virtualisation extensions (Intel VT-x, AMD-V) to the guest; used for CI/CD, cloud development, and testing.",
		Question: "You want to run a Kubernetes cluster (which uses VMs or containers) inside a cloud VM that is itself a virtual machine. The inner VM needs access to hardware virtualisation features. What must the outer hypervisor support?",
		Explanation: "Normally, a hypervisor keeps CPU virtualisation extensions (VT-x, AMD-V) hidden from guest VMs — a guest can't tell it's virtualised and can't create further VMs. Nested virtualisation makes the hypervisor expose these extensions to the guest, so the guest can run KVM or Hyper-V and create its own VMs. Enable it in KVM with `modprobe kvm_intel nested=1`. AWS supports it on `.metal` and most modern instance types. It's essential for: running Docker-in-Docker without workarounds, testing Kubernetes on a single cloud VM, CI/CD pipelines that need to build and test VM images, and testing infrastructure-as-code tools like Terraform.",
	},
	{
		Category: "Virtualization",
		Layer:    "Virtualization — Advanced",
		Name:     "IOMMU and PCI passthrough",
		Overview: "IOMMU (Input-Output Memory Management Unit) maps DMA addresses for devices; PCI passthrough uses IOMMU to give a VM exclusive, direct access to a physical PCIe device (GPU, NIC, NVMe) at native hardware performance.",
		Question: "You want to give a VM direct access to a physical GPU for machine learning training — not emulated, not shared, but the actual hardware running at full speed. What hardware and hypervisor feature enables this?",
		Explanation: "Normally, VMs use emulated or para-virtual devices. PCI passthrough (or SR-IOV for network cards) lets the hypervisor assign a real physical PCIe device exclusively to one VM. The VM's driver talks directly to the hardware — no hypervisor in the data path. IOMMU (Intel VT-d, AMD-Vi) is the CPU/chipset feature that makes this safe: it prevents the device's DMA (direct memory access) from accessing host memory, so a misbehaving GPU can't corrupt the hypervisor. This is how GPU-accelerated cloud instances (AWS p3, GCP A100) deliver near-bare-metal ML performance inside VMs.",
	},

	// ── Algorithms & Data Structures ─────────────────────────────────────────

	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Fundamentals",
		Name:     "Big O notation",
		Overview: "Big O describes how an algorithm's time or space grows relative to input size: O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n), O(n²) quadratic, O(2ⁿ) exponential — ignoring constants and lower-order terms.",
		Question: "Algorithm A takes 10ms for 100 items and 40ms for 200 items. Algorithm B takes 10ms for 100 items and 1,000ms for 200 items. How do you express this difference in growth behaviour with a single notation?",
		Explanation: "Algorithm A doubles when input doubles — that's O(n) linear growth. Algorithm B squares when input doubles — that's O(n²) quadratic. Big O describes the growth story, ignoring constants. For n=1,000,000: O(n) does 1M operations; O(n²) does 1 trillion — that's the difference between a fast web request and a program that never finishes. O(log n) is extremely efficient: doubling the input adds just one more step (binary search). O(1) is ideal: same time regardless of size (hash map lookup). Comparing algorithms, O(n log n) sort beats O(n²) sort for any input over ~100 items, even if O(n²) is faster on tiny inputs.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Fundamentals",
		Name:     "Arrays vs linked lists",
		Overview: "Arrays store elements contiguously in memory with O(1) random access by index but O(n) mid-insertion; linked lists store elements anywhere with O(1) insertion if you have a pointer but O(n) access by index.",
		Question: "You frequently insert items in the middle of a list. A colleague suggests an array. You suggest a linked list. Who is right — and what is the trade-off you're accepting?",
		Explanation: "An array is like a row of numbered seats — jumping to seat 500 is instant (O(1)). Inserting a new seat in the middle means physically shifting all seats after it (O(n)). A linked list is like a treasure hunt: each element holds a pointer to the next. Inserting is just changing two pointers (O(1)) once you're at the position — but finding that position still requires following 500 pointers (O(n)). Arrays win for random access and cache performance (contiguous memory means the CPU pre-fetches neighbours). Linked lists win for frequent insertions and deletions at arbitrary positions. Go slices are backed by arrays; linked list insertion is rarely worth the cache miss penalty in practice.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Fundamentals",
		Name:     "Hash tables (hash maps)",
		Overview: "A hash table maps keys to array indices via a hash function, providing O(1) average-case lookup, insert, and delete; collisions (two keys hashing to the same index) are handled by chaining or open addressing.",
		Question: "A dictionary with 1 million words still responds to lookups in the same time as a dictionary with 10 words. What data structure makes lookup time independent of the collection size?",
		Explanation: "A hash function converts a key (like 'username') to an integer that indexes into an array of buckets — compute the bucket, jump straight to it. No searching needed. Collisions happen when two keys land in the same bucket; chaining stores a linked list of collisions per bucket. A well-tuned hash map keeps the load factor (entries/buckets) below ~0.75 and rehashes (doubles the bucket array) when exceeded. Hash maps are behind Python dicts, Go maps, and Java HashMaps. The worst case (all keys collide) is O(n), but a good hash function makes this astronomically unlikely. Hash flooding attacks deliberately craft colliding inputs — defended against by randomising the hash seed per run.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Fundamentals",
		Name:     "Stacks and queues",
		Overview: "A stack is LIFO (Last-In-First-Out) like a stack of plates; a queue is FIFO (First-In-First-Out) like a waiting line — both support O(1) push/enqueue and pop/dequeue at their respective ends.",
		Question: "Your browser's Back button returns pages in the reverse order you visited them. A print spooler processes jobs in the order they were submitted. These two orderings are opposites. What data structures model each?",
		Explanation: "Stack (LIFO): the last item pushed is the first popped — like stacking plates. The browser's history, function call stack (the last function called returns first), undo/redo, and depth-first search all use stacks. Queue (FIFO): the first item enqueued is the first dequeued — like a checkout line. Used for: message queues (RabbitMQ, Kafka), breadth-first search, task scheduling, buffering between producers and consumers. A deque (double-ended queue) supports efficient add/remove at both ends. In Go, slices serve as both: `append(s, x)` with `s = s[1:]` gives a queue; with `s = s[:len(s)-1]` gives a stack.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Fundamentals",
		Name:     "Binary search",
		Overview: "Binary search finds a target in a sorted collection in O(log n) by repeatedly halving the search space — compare the midpoint, discard the impossible half, repeat until found or the space is empty.",
		Question: "You need to find a name in a sorted list of 1 million entries. Checking each one in order takes up to 1 million comparisons. What algorithm finds it in at most 20 comparisons?",
		Explanation: "Like guessing a number 1–1,000: start at 500. 'Too high' → go to 250. 'Too low' → go to 375. Each guess eliminates half the remaining options. 1 million items → log₂(1,000,000) ≈ 20 comparisons. The hard requirement: data must be sorted first. Binary search underpins database B-tree index lookups, `git bisect` (finds which commit introduced a bug by binary-searching history), and sorted array APIs in every standard library. Classic off-by-one bug: compute midpoint as `left + (right-left)/2`, not `(left+right)/2` — the latter overflows for large indices in languages with fixed-width integers.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "Trees and binary search trees (BST)",
		Overview: "A BST stores nodes where the left child holds smaller values and the right holds larger, enabling O(log n) average search, insert, and delete; self-balancing variants (AVL, Red-Black) guarantee O(log n) in the worst case.",
		Question: "You need a structure that supports fast insert, delete, and lookup AND keeps elements in sorted order so you can also efficiently find the minimum, maximum, or a range. Hash maps are fast but unordered. What fits?",
		Explanation: "In a BST, searching is like binary search on a tree: go left if smaller, right if larger, until found or nil. The danger: inserting already-sorted data creates a straight chain, degrading to O(n) like a linked list. Self-balancing trees (AVL, Red-Black) automatically rebalance after modifications to guarantee O(log n). Go's standard library doesn't include a BST, but Java's `TreeMap`, C++'s `std::map`, and Python's `sortedcontainers.SortedList` are all backed by balanced BSTs. Unlike hash maps, BSTs enable range queries like 'find all keys between 100 and 200' efficiently by traversing a subtree.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "Heaps and priority queues",
		Overview: "A heap is a tree satisfying the heap property (parent ≥ all children for max-heap); it supports O(1) peek at the max/min and O(log n) insert and extract — the underlying structure for priority queues and heapsort.",
		Question: "A hospital emergency room treats the most critical patient first, regardless of arrival order. What data structure always gives O(1) access to the highest-priority item and O(log n) insertion?",
		Explanation: "A max-heap guarantees the largest element is always at the root — you can look at it in O(1). Inserting 'bubbles up' the new element to its correct position in O(log n). Extracting the max removes the root, moves the last leaf up, and 'sifts down' in O(log n). Go's `container/heap`, Java's `PriorityQueue`, and Python's `heapq` implement this. Real uses: task schedulers (run highest-priority job next), Dijkstra's shortest-path algorithm (always expand the nearest unvisited node first), streaming top-K problems ('what are the 10 most popular items as data streams in?'). Heapsort uses a heap to sort in O(n log n) with O(1) extra space.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "Graphs and graph traversal (BFS, DFS)",
		Overview: "A graph is nodes (vertices) connected by edges; BFS (Breadth-First Search) explores all neighbours at distance d before d+1, guaranteeing shortest unweighted paths; DFS (Depth-First Search) explores as deep as possible before backtracking.",
		Question: "You want the shortest path between two people in a social network — fewest 'friend of a friend' hops. Which graph traversal algorithm guarantees the shortest path, and why does the other one fail at this?",
		Explanation: "BFS uses a queue: start at node A, add all direct friends, then their friends, etc. The first time you reach node B, you've found the shortest path in hops. DFS uses a stack (or recursion): dive deep down one path before backtracking — it might find a 10-hop path before discovering the 2-hop one. BFS = shortest unweighted path, web crawlers, level-order traversal. DFS = detecting cycles, topological sort (build dependencies in order), finding all connected components, solving mazes. Both run in O(V+E) — vertices plus edges. For weighted graphs (roads with distances), use Dijkstra's algorithm: BFS with a priority queue, always expanding the nearest unvisited node.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Techniques",
		Name:     "Dynamic programming",
		Overview: "Dynamic programming solves problems by breaking them into overlapping subproblems, storing each result once (memoisation or tabulation) to avoid redundant recomputation — turning exponential algorithms into polynomial ones.",
		Question: "Computing Fibonacci naively is exponential — fib(30) recalculates fib(28) dozens of times. Storing each result after first computing it reduces it to O(n). What technique is this, and what problems does it generalise to?",
		Explanation: "Two flavours: top-down (memoisation) — write a recursive solution and cache results; bottom-up (tabulation) — fill a results table from the simplest subproblem up to the full answer. The key insight: if a recursive solution keeps recomputing the same inputs, cache them. Recognise DP problems by two properties: 'overlapping subproblems' (same sub-calculations recur) and 'optimal substructure' (the optimal solution is built from optimal solutions to subproblems). Classic problems: coin change (minimum coins for an amount), longest common subsequence (DNA alignment, diff tools), knapsack, edit distance (spell-check). Real use: Git's diff algorithm, database query optimisers, game AI.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Techniques",
		Name:     "Sorting algorithms (quicksort, mergesort, heapsort)",
		Overview: "Efficient sorts achieve O(n log n); quicksort is fastest in practice but O(n²) worst-case; mergesort is O(n log n) worst-case and stable; heapsort is O(n log n) worst-case with O(1) space; counting/radix sort achieves O(n) for bounded integers.",
		Question: "Go's `sort.Slice` sorts 1 million integers in milliseconds. A naive O(n²) sort would take minutes. What class of algorithms achieves this O(n log n) speed, and what does that actually mean in operations?",
		Explanation: "For 1 million elements: O(n²) = 1 trillion operations (minutes); O(n log n) = 20 million operations (milliseconds). Quicksort picks a pivot, partitions smaller items left, larger right, recurses — average O(n log n) with excellent cache performance. Its weakness: sorted input with a naive pivot degrades to O(n²) — solved by random pivot. Mergesort: split in half recursively, merge sorted halves — always O(n log n) and stable (equal elements keep original order). Python's `sorted()` uses Timsort (hybrid merge/insertion sort); Go's sort uses pdqsort (pattern-defeating quicksort). For integers in a known range, counting sort and radix sort achieve O(n) by using counting instead of comparison.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Techniques",
		Name:     "Two pointers and sliding window",
		Overview: "Two pointers solve sorted-array problems by moving left and right pointers toward each other in O(n); sliding window maintains a moving subarray, updating the result by adding one element and removing another — turning O(n·k) into O(n).",
		Question: "Find the maximum sum of any contiguous subarray of length k in an array of n numbers. A naive approach checks all windows in O(n·k). What technique solves this in O(n) with a single pass?",
		Explanation: "Sliding window: compute the sum of the first k elements. To move the window right by one position, add the new right element and subtract the element that fell off the left — one addition and one subtraction instead of re-summing k elements each step. This makes it O(n) total. Use it for: maximum/minimum subarray of fixed size, longest substring without repeating characters, minimum window containing all required characters. Two pointers: start one pointer at each end of a sorted array. If their sum is too large, move the right pointer left; too small, move the left pointer right. Finds pairs summing to a target in O(n) instead of O(n²).",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Techniques",
		Name:     "Recursion and stack overflow",
		Overview: "Recursion uses the call stack to save each function's local state; very deep recursion exhausts the stack (typically 1–8 MB), causing stack overflow — solved by tail-call optimisation, iterative rewriting, or using an explicit stack data structure.",
		Question: "A recursive tree traversal crashes with a stack overflow on a tree with 100,000 nodes. The logic is correct. What caused the crash, and what are two ways to fix it?",
		Explanation: "Every function call pushes a frame onto the stack: local variables, parameters, and the return address. A typical stack is 1–8 MB. 100,000 nested calls × a few hundred bytes each = stack overflow. Fix 1: convert to iteration using an explicit stack data structure (a slice in Go) — instead of the call stack growing, you manage your own stack that lives in heap memory. Fix 2: tail-call optimisation (TCO) — if the recursive call is the very last operation in the function, some compilers reuse the current frame instead of creating a new one, making recursion use O(1) stack space. Python raises RecursionError at 1,000 calls. Go goroutines start with tiny stacks that grow automatically, making deep recursion less likely to crash but still unbounded.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "Tries (prefix trees)",
		Overview: "A trie stores strings character by character in a tree where each root-to-marked-node path spells a valid word; prefix search is O(length of prefix) regardless of vocabulary size — ideal for autocomplete and spell-checking.",
		Question: "An autocomplete feature must instantly suggest all words starting with 'pre' from a dictionary of 1 million words. A hash map lookup doesn't support prefix search. What specialised data structure makes this O(p) where p is the prefix length?",
		Explanation: "In a trie, each node represents one character with up to 26 children. The path root→'p'→'r'→'e' leads to every word starting with 'pre'. Finding completions requires only traversing the subtree below that path — O(p + results), not O(vocabulary size). Inserting and searching any word is O(length of word). Tries use more memory than hash maps (one node per character per word) but enable prefix search, autocomplete, spell-checking, and IP prefix matching in routers. Compressed tries (radix trees, Patricia tries) merge single-child chains into one node, saving memory. Real uses: browser address bars, IDE autocomplete, search engine query suggestions, and Linux's route lookup (`ip route`).",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "Bloom filters",
		Overview: "A bloom filter uses multiple hash functions to set bits in a bit array; it guarantees no false negatives but allows configurable false positives — using dramatically less memory than storing the full set.",
		Question: "A database wants to quickly check if a record does NOT exist before doing an expensive disk lookup — across 100 million records. Storing all IDs in a hash map would use gigabytes. What probabilistic structure answers 'definitely not present' in kilobytes?",
		Explanation: "To add an item: hash it with k functions, set those k bit positions in the array to 1. To check membership: hash it the same k ways — if any bit is 0, the item is definitely not in the set. If all k bits are 1, it's probably in the set (some might be set by other items — a false positive). The guarantee: zero false negatives. With a 1% false positive rate, 100 million items fits in ~114 MB instead of gigabytes. Real uses: Cassandra and HBase use bloom filters to skip disk reads for missing keys, Chrome's Safe Browsing checks URLs against a bloom filter before a server call, CDNs check if a URL is cached before hitting origin.",
	},
	{
		Category: "Algorithms & DS",
		Layer:    "Algorithms — Data Structures",
		Name:     "B-trees and database indexes",
		Overview: "A B-tree is a self-balancing tree where nodes hold many keys and have many children, optimised for block-based disk I/O; B+ trees (where all values live in linked leaf nodes) are used by most relational database indexes for O(log n) exact lookups and efficient range scans.",
		Question: "A database table has 100 million rows. Without an index, every query scans all rows. Adding an index makes exact lookups instant and range queries (`WHERE age BETWEEN 20 AND 30`) efficient too. What data structure provides both?",
		Explanation: "A B-tree node holds hundreds of keys — the tree stays very shallow (3–4 levels) even for 100 million rows, needing only 3–4 disk reads to find any row. B+ trees store all actual values in leaf nodes that are linked together in sorted order. This makes range queries fast: find the first matching leaf, then scan forward through linked leaves without touching upper tree levels. Hash indexes are O(1) for exact lookups but useless for range queries. `EXPLAIN ANALYZE` in PostgreSQL shows whether a query uses an index scan or a sequential scan. Creating the right indexes is often the most impactful optimisation for slow queries.",
	},

	// ── LLMs & AI ─────────────────────────────────────────────────────────────

	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Architecture",
		Name:     "Transformer architecture",
		Overview: "The Transformer processes entire sequences in parallel using attention mechanisms rather than sequentially like RNNs — enabling massive GPU parallelisation, better long-range dependencies, and scaling to hundreds of billions of parameters.",
		Question: "GPT-4, Claude, and Gemini are all built on the same core architecture invented in 2017. What is it called, and what made it replace RNNs — the previous state-of-the-art for language tasks?",
		Explanation: "Before Transformers, RNNs processed text word-by-word sequentially — word 100 couldn't start processing until word 99 was done. Transformers process all words simultaneously: every position attends to every other position at once. This parallelism makes GPU training massively more efficient — a 175B parameter model would take years with RNNs but months with Transformers. The 2017 paper 'Attention Is All You Need' (Google) introduced this. Core components: self-attention layers (each token looks at all others), feed-forward layers per position, layer normalisation, and positional encodings (since positions are processed in parallel, the model must be told word order).",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Architecture",
		Name:     "Self-attention mechanism",
		Overview: "Self-attention computes attention scores (relevance weights) between every pair of tokens using Query, Key, and Value vectors, then outputs a weighted sum of Values — letting each token directly attend to any other regardless of distance.",
		Question: "In 'The animal didn't cross the street because it was too tired', the word 'it' refers to 'animal'. How does a Transformer figure out this reference across multiple words without reading left-to-right?",
		Explanation: "For each token, self-attention computes three vectors: Query ('what am I looking for?'), Key ('what do I contain?'), Value ('what do I contribute?'). Attention score = dot product of one token's Query with every other token's Key — normalized to probabilities. The output is a weighted sum of all Values, weighted by those scores. For 'it', its Query strongly matches 'animal's Key, so 'animal' dominates the output — the reference is resolved. Multi-head attention runs this process several times in parallel with different learned Q/K/V weights, capturing different relationship types simultaneously (grammatical, semantic, coreference).",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Architecture",
		Name:     "Tokenization and tokens",
		Overview: "Tokenization splits text into subword tokens using algorithms like BPE (Byte-Pair Encoding); tokens are the unit of computation and billing for LLM APIs — roughly 1 token per ¾ English word or 4 characters.",
		Question: "When you send text to a GPT model, it doesn't process individual letters or whole words. It splits text into pieces called 'tokens'. What are these — and why does this matter for cost and context limits?",
		Explanation: "BPE builds a vocabulary by merging frequent character pairs iteratively. Common words like 'the' are single tokens; rare words like 'supercalifragilistic' become many tokens. About 1 token ≈ 4 characters in English. Code, non-English languages, and special characters are often less efficient (more tokens per word). This matters because: API pricing is per token (1M input tokens ≈ $5–$30 depending on model), context limits are in tokens not words (a 128k context window ≈ 96,000 English words), and long prompts with lots of examples cost more. OpenAI's `tiktoken` library lets you count tokens before sending a request.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Architecture",
		Name:     "Embeddings and vector representations",
		Overview: "Embeddings map text to dense vectors in high-dimensional space where semantically similar items are geometrically close; cosine similarity between vectors measures semantic relatedness without any keyword matching.",
		Question: "A semantic search engine finds documents about 'vehicle crashes' when you search for 'car accidents' — with zero shared words. What mathematical representation makes meaning-based matching possible?",
		Explanation: "An embedding model converts text to a vector of 768–3072 floating-point numbers. 'Car' and 'automobile' get nearly identical vectors; 'car' and 'democracy' get very different ones. Cosine similarity measures the angle between vectors: 1.0 = same direction (identical meaning), 0 = unrelated. Word2Vec (2013) showed these vectors capture relationships: `king - man + woman ≈ queen`. Modern embedding models (OpenAI text-embedding-3, sentence-transformers) encode entire sentences and paragraphs. Vector databases (Pinecone, pgvector, Weaviate, Qdrant) store millions of embeddings and find nearest neighbours in milliseconds — enabling semantic search, recommendation systems, and RAG.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Training",
		Name:     "Pre-training vs fine-tuning",
		Overview: "Pre-training learns general language patterns from massive unlabelled text by predicting the next token; fine-tuning adapts a pre-trained model to specific tasks or behaviours using smaller labelled datasets — far cheaper than training from scratch.",
		Question: "GPT-4 cost ~$100M to pre-train on internet text. Yet it also knows to behave as a helpful assistant and refuses harmful requests. Pre-training on web text alone didn't teach it that. What additional stage shaped this behaviour?",
		Explanation: "Pre-training: feed the model trillions of tokens and train it to predict the next word — it learns grammar, facts, reasoning, and world knowledge, but it just completes text in whatever style matches the prompt. Fine-tuning: continue training on curated examples of desired behaviour. Instruction fine-tuning shows (prompt, good response) pairs. RLHF further aligns with human preferences. The economics: pre-training GPT-4 costs $100M+; fine-tuning an open model like Llama on your own task data might cost $100–$1,000. This is why open-source base models (Llama 3, Mistral, Qwen) are enormously valuable — the expensive base is done once, and anyone can fine-tune for their use case.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Training",
		Name:     "RLHF (Reinforcement Learning from Human Feedback)",
		Overview: "RLHF trains a reward model on human preference rankings between model outputs, then uses RL (PPO) to fine-tune the LLM to maximise reward — aligning model responses with human values of helpfulness, harmlessness, and honesty.",
		Question: "After pre-training, a language model can complete text but might produce toxic, biased, or unhelpful outputs unpredictably. What training technique uses human judges to steer the model toward safer, more helpful responses?",
		Explanation: "Three steps: (1) generate several responses to the same prompt, (2) have human raters rank them best to worst, (3) train a separate 'reward model' to predict which response humans prefer. Then use RL (PPO algorithm) to fine-tune the LLM to generate responses that score highly on the reward model — reinforcing good behaviour, penalising bad. This is how ChatGPT, Claude, and Gemini became assistants rather than raw text completers. DPO (Direct Preference Optimisation, 2023) achieves similar alignment without the complex RL step — directly training on pairs of preferred vs. rejected responses — and has become the standard for open-source fine-tuning.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Inference",
		Name:     "Temperature and sampling strategies",
		Overview: "Temperature scales the model's output scores before converting to probabilities: low temperature (near 0) makes output near-deterministic, high temperature makes it more random and creative; top-p and top-k further constrain which tokens are sampled.",
		Question: "You call a GPT API twice with the same prompt. With temperature 0 you get identical responses; with temperature 1.0 you get different responses each time. What does temperature actually control?",
		Explanation: "The model outputs raw scores (logits) for every possible next token. Dividing by temperature T before converting to probabilities: T=0 makes the highest-scoring token overwhelmingly dominant (always the same word chosen); T=1 samples proportionally to the raw scores; T=2 flattens the distribution (any token becomes plausible). Top-p (nucleus) sampling: only sample from tokens whose cumulative probability reaches p — e.g., top-p=0.9 uses the smallest set of top tokens covering 90% probability mass. Top-k: only consider the k most likely tokens. For code generation and factual answers, use low temperature (0.0–0.3). For brainstorming and creative writing, use higher temperature (0.7–1.0).",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Inference",
		Name:     "Context window and KV cache",
		Overview: "The context window is the maximum tokens the model can attend to at once; self-attention is O(n²) in context length — doubling context length quadruples attention computation; the KV cache stores past tokens' key-value pairs to avoid recomputing them during generation.",
		Question: "A 128k-context LLM costs 10× more to call with a 100k-token prompt than a 10k-token prompt — much more than the 10× raw token count suggests. What property of the attention mechanism causes this superlinear cost?",
		Explanation: "Self-attention computes scores between every pair of tokens: 1,000 tokens = 1M pair comparisons; 10,000 = 100M; 100,000 = 10 billion. This O(n²) scaling makes long contexts expensive in both computation and GPU memory. The KV cache solves generation speed: during text generation, the key-value vectors of all previous tokens are cached so each new token only needs to attend to stored KV pairs rather than recompute everything. Without KV cache, generating 1,000 tokens would require 1,000 full forward passes. With it, each new token is one small incremental computation. Modern techniques like Flash Attention, Ring Attention, and sliding window attention reduce the O(n²) cost for training very long contexts.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Applications",
		Name:     "RAG (Retrieval-Augmented Generation)",
		Overview: "RAG retrieves relevant documents from a vector database at query time and injects them into the prompt context, allowing the model to answer questions about knowledge it was never trained on — without fine-tuning.",
		Question: "A customer support chatbot needs to answer questions about your specific product docs — information the base LLM has never seen. Re-training is too expensive. What architecture gives the model access to your documents at query time?",
		Explanation: "RAG pipeline: (1) Offline — chunk your documents, embed each chunk, store vectors in a database. (2) At query time — embed the user's question, find the k most semantically similar document chunks, inject them into the prompt: 'Use the following context: [retrieved docs]. Question: [question]'. (3) The LLM answers using the provided context. Benefits: no model retraining needed, knowledge updates by re-embedding changed docs, answers can cite sources. Limitations: context window limits how many docs fit in the prompt, and retrieval quality determines answer quality (if the right chunk isn't retrieved, the answer is wrong). LangChain, LlamaIndex, and Haystack are popular frameworks for building RAG systems.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Applications",
		Name:     "LLM hallucination",
		Overview: "Hallucination is when an LLM generates factually incorrect or entirely fabricated information with apparent confidence — a consequence of the model learning to produce plausible-sounding text rather than verifying factual accuracy.",
		Question: "You ask an LLM to cite sources for a claim. It returns a perfectly formatted academic paper title, journal, author names, and DOI — but the paper doesn't exist. The model showed no uncertainty. What is this failure called and why does it happen?",
		Explanation: "LLMs are trained to predict the most statistically likely next token given context — they generate what a citation looks like (title format, journal name, DOI format) even when there's no actual paper behind it. The model doesn't 'look things up'; it pattern-matches. Asking for a citation produces something that fits the citation pattern, real or not. Mitigations: RAG (force the model to use provided sources), tool use (let the model call a search API and quote actual results), asking the model to express uncertainty explicitly, and fact-checking critical outputs against authoritative sources. Modern models are better at expressing uncertainty but hallucination is not fully solved in any current model.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Applications",
		Name:     "Prompt engineering and few-shot learning",
		Overview: "Prompt engineering designs inputs to guide LLM behaviour; few-shot learning includes labelled examples directly in the prompt — the model recognises the pattern and continues it without any weight updates.",
		Question: "You need an LLM to classify customer emails as 'urgent' or 'normal'. With just an instruction, accuracy is mediocre. Adding three labelled examples in the prompt dramatically improves it. What technique is this and why does it work?",
		Explanation: "Few-shot prompting: add 2–5 examples before your actual request: 'Email: Server is down NOW. Label: urgent. Email: Invoice question. Label: normal. Email: [new email]. Label:'. The model recognises the pattern and completes it correctly. This works because the model saw millions of 'complete the pattern' examples during pre-training. Chain-of-thought prompting adds reasoning steps to examples or just says 'think step by step' — this dramatically improves performance on maths and multi-step logic by making the model reason out loud before answering. Zero-shot (just instructions), few-shot (examples in prompt), and fine-tuning (examples in training data) form a spectrum of increasing cost and increasing consistency.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Efficiency",
		Name:     "Model quantisation (INT4, INT8, GGUF)",
		Overview: "Quantisation reduces model weight precision from 32-bit or 16-bit floats to 8-bit or 4-bit integers, shrinking memory requirements by 4–8× with modest quality loss — enabling large models to run on consumer hardware.",
		Question: "A 70B parameter LLM needs ~140 GB of GPU VRAM at float16 — far beyond any consumer GPU. How do developers run these models on a single 24 GB GPU at home?",
		Explanation: "Each parameter stored as float16 (2 bytes): 70B × 2 = 140 GB. As INT8 (1 byte): 70 GB. As INT4 (0.5 bytes): 35 GB — fits on two consumer GPUs or a Mac with 48 GB unified memory. Quantisation maps weights to the nearest value in a smaller discrete range, like rounding a precise measurement to the nearest integer — some precision lost, but quality remains surprisingly good. GPTQ, AWQ, and GGUF are popular formats; llama.cpp runs GGUF models efficiently on CPU; `ollama` wraps this for a simple local LLM server. Quality: INT8 is barely perceptible, INT4 shows noticeable degradation on complex reasoning, Q5/Q6 (5- or 6-bit) is a popular sweet spot.",
	},
	{
		Category: "LLMs & AI",
		Layer:    "LLMs — Efficiency",
		Name:     "LoRA and parameter-efficient fine-tuning",
		Overview: "LoRA (Low-Rank Adaptation) freezes the original model weights and adds tiny trainable matrices alongside each attention layer — achieving ~90% of full fine-tuning quality while training <1% of parameters and using a fraction of the GPU memory.",
		Question: "You want to fine-tune a 70B LLM on your custom data. Full fine-tuning needs 140+ GB of GPU RAM and weeks of compute. What technique fine-tunes the model by adding and training only a tiny fraction of new parameters?",
		Explanation: "A weight matrix of size 4096×4096 (16M parameters) is frozen. LoRA adds two small matrices alongside it: a 4096×r and r×4096 matrix, where rank r is typically 4–64. For r=8, that's only 65,536 new parameters instead of 16M — ~250× smaller. Only these tiny matrices are updated during training; the original frozen weights never change. After training, the LoRA matrices can be merged back into the original weights for zero added inference cost. QLoRA combines 4-bit quantisation of the frozen base model with 16-bit LoRA training — fine-tuning a 70B model requires only ~48 GB of GPU RAM instead of 140+ GB, making it possible on two consumer A6000 GPUs or a single H100.",
	},

	// ── ML Fundamentals ───────────────────────────────────────────────────────

	{
		Category: "ML Fundamentals",
		Layer:    "ML — Core Concepts",
		Name:     "Supervised vs unsupervised learning",
		Overview: "Supervised learning trains on labelled (input, correct output) pairs to learn a mapping; unsupervised learning finds patterns or structure in unlabelled data — clusters, compressed representations, or generative models.",
		Question: "A spam filter is trained on emails labelled 'spam' or 'not spam'. A recommendation engine discovers user groups from purchase history with no labels at all. What is the fundamental difference between these two approaches?",
		Explanation: "Supervised: you provide inputs AND correct answers during training. The model learns 'given this input, produce this output'. Examples: classification (spam/not-spam), regression (predict house price), object detection (draw a box around the cat). Unsupervised: only inputs, no labels. The model finds hidden structure on its own. K-means clustering groups similar customers; PCA reduces 1,000 features to 2 for visualisation; autoencoders learn compressed representations. Reinforcement learning is a third paradigm: an agent takes actions in an environment and learns from reward signals (neither labelled pairs nor raw data). Most LLMs use all three: unsupervised pre-training on text, supervised fine-tuning, and RL from human feedback.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Core Concepts",
		Name:     "Training, validation, and test sets",
		Overview: "Training data trains model weights; the validation set guides hyperparameter tuning and model selection without contaminating the final estimate; the test set is held out entirely and used only once — to estimate real-world performance.",
		Question: "Your model scores 99% on training data and 65% on the validation set. You tune hyperparameters until validation accuracy reaches 95%. Then you test on the test set and get 70%. Why isn't the validation score a reliable estimate of real-world performance?",
		Explanation: "When you use the validation set to guide decisions (pick the best model, tune hyperparameters), you're indirectly fitting to it — the model that 'won' on validation is partly lucky on that specific data. The test set simulates completely unseen data: never look at it until you've made all decisions and are ready to report your final result. Using the test set multiple times to guide decisions turns it into a second validation set — your reported accuracy becomes optimistic. Data leakage (when test/production data patterns accidentally influence training) is the most common reason ML models fail in production despite high test scores.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Core Concepts",
		Name:     "Overfitting and underfitting",
		Overview: "Overfitting: the model memorises training noise instead of generalising — high training accuracy, low test accuracy; underfitting: the model is too simple to capture the pattern — low accuracy on both; regularisation, more data, and model complexity tuning fix each.",
		Question: "A neural network achieves 99% accuracy on training data but only 55% on the test set. A linear model achieves 70% on both. What are these two failure modes and how do you fix each?",
		Explanation: "Overfitting = memorising the exam practice questions without understanding the material — you fail new questions. Signs: training accuracy >> validation accuracy, loss curves diverge after early training. Fixes: more training data, dropout, L1/L2 regularisation, early stopping (stop training when validation loss stops improving), simpler model. Underfitting = barely studying — you fail everything. Signs: high loss on both train and validation from the start. Fixes: more complex model (more layers/parameters), longer training, more features, lower learning rate. The bias-variance tradeoff: simple models underfit (high bias); complex models overfit (high variance). Cross-validation finds the model complexity that minimises the sum of both errors.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Training",
		Name:     "Gradient descent and learning rate",
		Overview: "Gradient descent minimises the loss function by iteratively moving parameters in the opposite direction of the gradient (steepest uphill), scaled by the learning rate; SGD, mini-batch SGD, and Adam are the main variants.",
		Question: "A neural network starts with random weights producing terrible predictions. During training, it gradually improves. What algorithm repeatedly nudges all parameters in the direction that reduces the prediction error?",
		Explanation: "Imagine blindfolded on a hilly landscape trying to reach the lowest valley — feel the slope, take a step downhill, repeat. Gradient descent does this in millions of dimensions (one per parameter). The gradient tells us: 'increase this weight → loss increases by X' — so we move the opposite direction. Learning rate controls step size: too large = you overshoot the valley and oscillate; too small = takes forever to converge. Stochastic gradient descent (SGD): compute gradient on one example (noisy but fast). Mini-batch SGD: average gradient over 32–512 examples — the standard. Adam optimizer adds momentum (velocity accumulates in the downhill direction) and per-parameter adaptive learning rates — the default choice for most neural networks.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Training",
		Name:     "Backpropagation",
		Overview: "Backpropagation applies the chain rule of calculus to compute the gradient of the loss with respect to every parameter by propagating the error signal backward layer by layer from the output to the input.",
		Question: "A network with 50 layers and 100M parameters produces a wrong prediction. How does it calculate how much each of those 100M parameters contributed to the error — efficiently, in one backward pass?",
		Explanation: "Forward pass: feed input through all layers, compute prediction, compute loss (how wrong). Backward pass: starting at the loss, apply the chain rule layer by layer going backward. Chain rule: if A affects B which affects C, then how much does A affect C? = how much A affects B × how much B affects C. Each layer receives the gradient from the layer above it, computes gradients for its own weights, and passes the gradient below. Result: one gradient value for every parameter — same cost as one forward pass. Automatic differentiation (PyTorch autograd, TensorFlow) does this symbolically: you write the forward pass, call `.backward()`, and all gradients are computed automatically without you touching the math.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Training",
		Name:     "Activation functions (ReLU, sigmoid, softmax)",
		Overview: "Activation functions apply non-linear transformations after each layer's linear computation; without them, any deep network collapses to a single linear layer; ReLU is standard for hidden layers, sigmoid for binary output, softmax for multi-class probability output.",
		Question: "Without activation functions, stacking 100 linear layers is mathematically identical to one linear layer — the network can't learn complex patterns. What type of operation breaks this limitation?",
		Explanation: "Any series of matrix multiplications is itself just one matrix multiplication — linear. Non-linearity breaks this: ReLU (max(0, x)) outputs 0 for negative inputs, x for positive — simple, fast, avoids the vanishing gradient problem that plagued sigmoid/tanh in deep networks. Hidden layer default: ReLU or GELU (smoother variant used in Transformers). Binary classification output: sigmoid (maps any number to 0–1, interpretable as probability). Multi-class output: softmax (converts a vector of scores to probabilities that sum to 1, e.g., [cat: 0.7, dog: 0.2, bird: 0.1]). Tanh maps to -1 to 1 — used in LSTM gates. The choice of activation affects gradient flow, training speed, and what functions the network can learn.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Training",
		Name:     "Loss functions (cross-entropy, MSE)",
		Overview: "Loss functions quantify prediction error; cross-entropy is standard for classification (heavily penalises confident wrong predictions), MSE (mean squared error) is standard for regression — the choice of loss defines what the model optimises toward.",
		Question: "You're training an image classifier and use Mean Squared Error as the loss. Training is slow and the model never learns confidently. What loss function is designed specifically for classification tasks?",
		Explanation: "MSE = average of (predicted - actual)² — great for predicting continuous values (house prices, temperatures). For classification, MSE's gradient nearly vanishes when the model is confidently wrong (sigmoid output near 0 or 1 has tiny gradient), making training slow. Cross-entropy loss = -log(probability assigned to the correct class). If the model predicts 1% chance for the correct class, loss = -log(0.01) = 4.6 — a huge gradient pushing hard to correct. If it predicts 99% for the correct class, loss ≈ 0. This creates the strongest training signal exactly when the model is most wrong. Binary cross-entropy for two classes; categorical cross-entropy for multiple classes — always pair softmax output with categorical cross-entropy.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Training",
		Name:     "Dropout and regularisation (L1, L2)",
		Overview: "Dropout randomly zeroes neurons during training to prevent co-adaptation and reduce overfitting; L2 regularisation (weight decay) adds a penalty for large weights to the loss, preventing the model from memorising noise.",
		Question: "Your deep network memorises training data perfectly but fails on test data. What two techniques combat this without making the model smaller or collecting more data?",
		Explanation: "Dropout (2014): during each training step, randomly zero 20–50% of activations. Different neurons drop out each batch, so the network can't rely on any single neuron — it must learn redundant, distributed representations. At test time, all neurons are active but outputs are scaled by the keep probability. It's like training thousands of slightly different networks and averaging them. L2 regularisation (weight decay): add λ × sum(w²) to the loss function. The optimizer now minimises both prediction error and weight magnitude — preventing any single weight from becoming very large and dominating. L1 regularisation (add λ × sum(|w|)) drives some weights to exactly zero — implicit feature selection. Batch normalisation also acts as a regulariser by adding noise through batch statistics.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Architectures",
		Name:     "Convolutional Neural Networks (CNNs)",
		Overview: "CNNs apply learned filters that slide across spatial input to detect local patterns; weight sharing (same filter used everywhere) drastically reduces parameters and provides translation invariance — foundational for image, audio, and some NLP tasks.",
		Question: "A fully connected network on a 1920×1080 image needs billions of parameters for just the first layer — completely impractical. What architecture works efficiently on images by exploiting their spatial structure?",
		Explanation: "A convolutional filter is a small matrix (e.g., 3×3) of learned weights that slides across the image, computing a dot product at each position. One filter detects one type of pattern (an edge in a specific direction, a texture) wherever it appears. Key insight: an edge detector should work the same anywhere in the image — so one set of 9 weights serves the entire 1920×1080 image instead of one weight per pixel position (weight sharing). Stacking conv+pooling layers builds a hierarchy: layer 1 detects edges, layer 3 detects shapes, layer 7 detects eyes and wheels. Pooling (take max in each 2×2 region) shrinks spatial dimensions progressively. Famous architectures: ResNet (skip connections to prevent vanishing gradients), EfficientNet (optimal depth/width/resolution scaling), Vision Transformer (Transformer applied to image patches).",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Evaluation",
		Name:     "Bias-variance tradeoff",
		Overview: "Bias measures systematic error from wrong model assumptions (underfitting); variance measures sensitivity to training data noise (overfitting); total error = bias² + variance + irreducible noise — there is a fundamental tension between the two.",
		Question: "A simple linear model consistently predicts the wrong values in the same direction — it's wrong but predictably so. A complex neural network gives wildly different predictions every time you retrain it. What framework captures this fundamental tension in model complexity?",
		Explanation: "High bias: the model makes the same systematic mistake regardless of which training data it sees — 'always predicts house price increases with rooms, always ignoring the crime rate'. High variance: the model is so sensitive to specific training examples that retraining on a slightly different sample gives a completely different model. Adding model complexity reduces bias but increases variance. Adding more training data primarily reduces variance without fixing bias. Ensemble methods (random forests: average many high-variance trees) reduce variance. Regularisation reduces variance. The goal: find the model complexity where bias² + variance is minimised on your validation data — the sweet spot between too-simple and too-complex.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Evaluation",
		Name:     "Precision, recall, F1, and confusion matrix",
		Overview: "Precision = true positives / predicted positives (accuracy of positive predictions); recall = true positives / actual positives (coverage of real positives); F1 is the harmonic mean — use these when class imbalance makes accuracy meaningless.",
		Question: "Your cancer detection model has 99% accuracy — but it predicts 'no cancer' for everyone, because 99% of patients are cancer-free. Why is accuracy useless here, and what metrics actually matter?",
		Explanation: "Accuracy treats all errors equally. For cancer detection: missing a real cancer (false negative) is catastrophic; a false alarm (false positive) causes unnecessary worry but isn't fatal. Recall = TP/(TP+FN): what fraction of actual cancer cases did the model catch? This is what matters in medical screening. Precision = TP/(TP+FP): of all the 'cancer' predictions, how many were real? Matters for spam filters (don't accidentally mark real email as spam). F1 = 2×(precision×recall)/(precision+recall) — their harmonic mean; a model that's 100% precision but 0% recall (predict nothing) gets F1=0. Always plot the confusion matrix to see the full picture. AUC-ROC measures the tradeoff between true positive and false positive rates across all decision thresholds — useful for model comparison on imbalanced datasets.",
	},
	{
		Category: "ML Fundamentals",
		Layer:    "ML — Techniques",
		Name:     "Transfer learning",
		Overview: "Transfer learning uses a model pre-trained on a large dataset as a starting point, then fine-tunes it on a smaller target dataset — pre-trained features transfer across related tasks, requiring far less data and compute than training from scratch.",
		Question: "You need to classify industrial defects but only have 500 labelled photos. Training a vision model from scratch on 500 images would overfit badly. How can you build a good classifier with so little data?",
		Explanation: "A ResNet50 pre-trained on ImageNet (1.2M images, 1,000 classes) has already learned to detect edges, textures, shapes, and complex visual patterns in its first layers — these features are useful for almost any visual task. Fine-tuning: freeze the early layers (they already extract good features), replace the final classification head with one matching your classes, and train only the last few layers on your 500 photos. The pre-trained layers provide strong starting features; your small dataset only needs to teach the final decision boundary. The same principle in NLP: BERT pre-trained on Wikipedia; fine-tune for sentiment classification with 1,000 labelled reviews and outperform a model trained from scratch on 100,000. This is why small teams build state-of-the-art models: the expensive base training is shared.",
	},

	// ── System Design ─────────────────────────────────────────────────────────

	{
		Category: "System Design",
		Layer:    "System Design — Scaling",
		Name:     "Horizontal vs vertical scaling",
		Overview: "Vertical scaling (scale up) adds more CPU/RAM to one machine; horizontal scaling (scale out) adds more machines and distributes load — horizontal scaling removes the single-machine ceiling but requires stateless services and a load balancer.",
		Question: "Your web server is overwhelmed at peak traffic. You can either upgrade it to a more powerful machine, or add five more identical servers behind a load balancer. What are these two approaches called and which is more widely used at internet scale?",
		Explanation: "Vertical scaling is simple — just buy a bigger server. No code changes needed. But it has a hard ceiling (the most powerful single machine available), costs grow exponentially, and a single machine is a single point of failure. Horizontal scaling adds identical servers and distributes traffic between them. It can grow theoretically without limit and is fault-tolerant (one server dying doesn't take down the service). The catch: your application must be stateless — no local session data, no in-memory state that only lives on one server. Sessions go in Redis, files go in object storage (S3), and every server becomes interchangeable. Almost every large web service (Google, Netflix, Amazon) scales horizontally. The shift from vertical to horizontal is often the defining architectural moment for a growing startup.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Scaling",
		Name:     "Load balancing",
		Overview: "A load balancer distributes incoming requests across a pool of servers using strategies like round-robin, least connections, or consistent hashing — it also performs health checks and removes failed servers from rotation.",
		Question: "You have five web servers and 10,000 requests per second. A single entry point must distribute traffic evenly, detect failed servers, and stop sending them traffic. What component handles all of this?",
		Explanation: "A load balancer sits in front of your servers and acts as the single entry point for all traffic. Round-robin: each server gets requests in turn (1, 2, 3, 4, 5, 1, 2, ...). Least connections: new requests go to the server with the fewest active connections — better when requests have wildly different processing times. IP hash: the same client IP always goes to the same server — useful for sticky sessions without server-side state sharing. Health checks: the load balancer periodically pings each server; if one fails to respond, it's removed from rotation automatically. Layer 4 load balancers (TCP level) are fast but can't inspect HTTP; Layer 7 (HTTP level) can route based on URL path, headers, or content — e.g., route `/api/*` to API servers and `/static/*` to CDN. Examples: AWS ALB/NLB, nginx, HAProxy, Cloudflare.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Scaling",
		Name:     "Caching strategies (cache-aside, write-through, eviction)",
		Overview: "Caching stores frequently accessed data in fast memory (Redis, Memcached) to avoid repeated expensive computations or database queries; cache-aside, write-through, and write-behind are the main population strategies; LRU, LFU, and TTL are the main eviction policies.",
		Question: "Your database is slow because the same 1,000 popular product pages are queried millions of times a day. Caching these in memory would eliminate 95% of database load. What are the two main patterns for keeping the cache and database in sync?",
		Explanation: "Cache-aside (lazy loading): the application checks the cache first; on a miss, it fetches from the database, stores the result in the cache, then returns it. Future requests hit the cache. Simple and resilient — a cache failure just falls back to the database. Data can be stale until TTL expires. Write-through: every write goes to both cache and database simultaneously. Cache is always up to date but every write is slower (two writes instead of one). Write-behind (write-back): writes go to cache immediately and asynchronously to the database — fast writes but risk of data loss if cache crashes before flushing. Eviction: when the cache is full, LRU (Least Recently Used) removes the item not accessed longest; LFU (Least Frequently Used) removes the least popular item; TTL (time-to-live) expires entries automatically. Redis is the industry standard: it supports strings, hashes, sorted sets, pub/sub, and persistence.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Scaling",
		Name:     "CDN (Content Delivery Network)",
		Overview: "A CDN caches static assets (images, JS, CSS, videos) at edge nodes geographically close to users — reducing latency by serving content from nearby servers instead of a distant origin, and offloading traffic from the origin.",
		Question: "Your website loads slowly for users in Asia because all assets are served from a US server. Adding servers in Asia seems expensive to manage. What infrastructure layer serves content from locations close to each user automatically?",
		Explanation: "A CDN like Cloudflare, AWS CloudFront, or Fastly operates hundreds of 'edge' data centres worldwide. When a user in Tokyo requests your logo, it comes from a Tokyo edge node (milliseconds) instead of your US origin server (200ms+ round trip). First request: edge node is a cache miss, fetches from origin, caches locally. Subsequent requests: served from edge cache. CDNs handle: static files (images, JS, CSS, videos), API caching for read-heavy endpoints, DDoS mitigation (absorb attack traffic at the edge before it reaches your origin), TLS termination, and geo-routing. Cache-Control headers control how long CDN caches content. Invalidating a CDN cache globally after a deployment takes seconds on most providers. For a startup, Cloudflare's free tier alone can handle massive traffic spikes without touching your origin.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Databases",
		Name:     "Database replication (primary-replica)",
		Overview: "Database replication copies data from a primary (write) server to one or more replicas (read servers) — reads scale horizontally while writes remain on the primary; replicas also provide failover if the primary dies.",
		Question: "Your database handles 90% read traffic and 10% writes. The single database server is a bottleneck and a single point of failure. What architecture separates reads from writes and provides redundancy?",
		Explanation: "Primary-replica replication: all writes go to the primary, which streams changes to replicas. Reads are distributed across replicas. A database handling 10,000 reads/second becomes 3 replicas each handling ~3,333 reads/second. If the primary fails, a replica is promoted to primary (automatic failover). Replication lag is the key challenge: replicas are asynchronously updated, so a read immediately after a write might return stale data — 'read-your-writes' consistency is not guaranteed by default. Solutions: route read-after-write requests to the primary, wait for replication to confirm, or use synchronous replication (slower writes, guaranteed consistency). Multi-primary (active-active) lets multiple nodes accept writes but requires conflict resolution. PostgreSQL streaming replication, MySQL binlog replication, and MongoDB replica sets all implement this pattern.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Databases",
		Name:     "Database sharding (partitioning)",
		Overview: "Sharding splits a database horizontally — different rows go to different database servers (shards) based on a shard key — enabling data volumes and write throughput to scale beyond what a single machine can handle.",
		Question: "Your single PostgreSQL database holds 10 TB of user data and a single machine can no longer handle the write throughput or store the data. Replicas only help with reads. What technique splits the data itself across multiple database servers?",
		Explanation: "Sharding partitions rows across servers by a shard key. User ID-based sharding: user IDs 0–999,999 go to shard 1, 1M–1.99M to shard 2, etc. Hash sharding: hash(user_id) % num_shards determines the shard — distributes load evenly. Queries involving only one user's data go to a single shard. Cross-shard queries (aggregate across all shards) are expensive — require querying all shards and merging results. Resharding (adding more shards as you grow) is painful: consistent hashing minimises data movement when adding shards. The golden rule: avoid sharding as long as possible — it adds enormous complexity. Before sharding, exhaust: indexing, query optimisation, read replicas, caching, vertical scaling, partitioning within a single database. Cassandra, MongoDB, and CockroachDB have built-in sharding; PostgreSQL requires manual setup or tools like Citus.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Databases",
		Name:     "SQL vs NoSQL trade-offs",
		Overview: "SQL databases provide ACID transactions, rigid schemas, and powerful joins — ideal for structured relational data; NoSQL databases sacrifice some consistency and query flexibility for horizontal scalability, flexible schemas, and specialised data models.",
		Question: "You're designing a system to store user profiles that may have wildly different fields — some users have 2 fields, others have 50. Rigid SQL schemas require dozens of migrations for every field change. What type of database handles this flexible structure naturally?",
		Explanation: "SQL (PostgreSQL, MySQL): strict schemas, ACID transactions (atomic writes across multiple tables), powerful JOINs, and decades of battle-tested reliability. Best for: financial data, inventory, anything requiring multi-row consistency. NoSQL comes in flavours: Document stores (MongoDB, Firestore) store JSON-like documents — flexible schema, no JOINs needed. Key-value stores (Redis, DynamoDB) — O(1) lookup by key, no complex queries. Wide-column (Cassandra, BigTable) — optimised for time-series and event logs, massive write throughput. Graph databases (Neo4j) — optimised for relationship traversal (social networks, recommendations). The myth: NoSQL = 'web scale'. Reality: most applications need SQL's consistency guarantees. Choose NoSQL when you have a specific problem it solves (flexible schema, extreme write throughput, specialised access patterns) — not because it sounds scalable.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Distributed Systems",
		Name:     "CAP theorem",
		Overview: "CAP theorem states that a distributed system can guarantee at most two of three properties: Consistency (all nodes see the same data), Availability (every request gets a response), and Partition tolerance (the system works despite network splits) — in practice you must choose CP or AP.",
		Question: "Your distributed database is split into two halves by a network failure. You must choose: keep serving requests with potentially stale data (available but inconsistent), or refuse requests until the network heals (consistent but unavailable). What theorem describes this unavoidable trade-off?",
		Explanation: "Partition tolerance is not optional in any real distributed system — networks fail. So the real choice is: CP or AP. CP systems (Consistent + Partition tolerant): when a partition occurs, they stop accepting writes to avoid inconsistency. HBase, Zookeeper, etcd, and bank databases choose CP — you'd rather refuse a transaction than risk double-spending money. AP systems (Available + Partition tolerant): they keep serving requests during a partition, accepting that different nodes may temporarily disagree. Cassandra, DynamoDB, and DNS choose AP — serving stale data is better than being unavailable. PACELC extends CAP: even without partitions, there's a latency vs consistency tradeoff. In practice, 'consistency' exists on a spectrum — eventual consistency (AP systems converge to the same state eventually) is sufficient for most use cases (product likes, view counts, shopping carts).",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Distributed Systems",
		Name:     "Eventual consistency",
		Overview: "Eventual consistency guarantees that if no new updates are made to a data item, all replicas will eventually converge to the same value — the system is available and partition-tolerant but reads may return stale data during the convergence window.",
		Question: "You post a tweet and a friend in another data centre sees it 2 seconds later. During those 2 seconds, some users see the tweet and others don't. The system is 'eventually consistent'. What does this mean in practice?",
		Explanation: "Eventual consistency means: the system guarantees that all replicas will converge to the same value — eventually. During the convergence window (milliseconds to seconds), different replicas may return different values. This is acceptable for: social media feeds, product view counts, shopping cart totals, DNS propagation (can take hours). It is NOT acceptable for: bank balances, inventory counts ('only 1 item left'), authentication sessions. Systems that use eventual consistency (DynamoDB, Cassandra, S3) achieve higher availability and lower latency by not requiring all replicas to agree before responding. Conflict resolution: 'last write wins' (timestamp-based), vector clocks (track causality), or CRDTs (data structures that always merge deterministically). The key question when designing: 'what is the worst case if a user reads stale data?'",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Distributed Systems",
		Name:     "Message queues and async processing",
		Overview: "Message queues (Kafka, RabbitMQ, SQS) decouple producers from consumers — producers publish messages without waiting for processing, enabling async workflows, load levelling, and resilience to downstream failures.",
		Question: "When a user signs up, you need to send a welcome email, create a billing account, and index their profile for search. Doing all three synchronously makes the signup request slow and fragile. How do you decouple these tasks?",
		Explanation: "A message queue sits between the producer (signup API) and consumers (email service, billing service, search indexer). The signup handler publishes one 'user_created' event to the queue and immediately returns success to the user. Each downstream service consumes events from the queue independently and at its own pace. Benefits: speed (signup returns in 10ms instead of waiting for 3 slow services), resilience (if the email service is down, the queue holds the message until it recovers — no lost events), load levelling (a traffic spike produces 10,000 messages; consumers process them steadily at 1,000/minute without being overwhelmed). Kafka: high-throughput log-based streaming, messages are retained and replayable — used for event sourcing, analytics pipelines. RabbitMQ: traditional message broker, flexible routing, acknowledgement-based delivery. AWS SQS: fully managed, simple queue semantics. The pattern: prefer async for operations the user doesn't need to wait for.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Distributed Systems",
		Name:     "Consistent hashing",
		Overview: "Consistent hashing places both servers and keys on a virtual ring; adding or removing a server only remaps a fraction of keys (1/n) instead of all keys — critical for distributed caches and databases where remapping everything on every change is catastrophic.",
		Question: "You have a cache cluster of 5 servers. When you add a 6th server using simple `hash(key) % 6`, nearly all cached keys map to different servers — a mass cache miss that hammers your database. What hashing scheme minimises remapping when servers are added or removed?",
		Explanation: "Simple modular hashing: `hash(key) % n_servers`. Change n from 5 to 6 — almost every key remaps to a different server. All cache misses simultaneously = database overwhelmed. Consistent hashing: place both servers and keys on a hash ring (0 to 2³²). Each key belongs to the nearest server clockwise on the ring. Add a server: only keys between the new server and its predecessor remap — on average 1/n of all keys, not all keys. Remove a server: only its keys move to the next server on the ring. Virtual nodes (vnodes): each physical server is represented by 100–200 points on the ring, ensuring even distribution even with few servers. Amazon DynamoDB, Apache Cassandra, and Memcached all use consistent hashing to distribute data across nodes without catastrophic remapping during scaling.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Reliability",
		Name:     "Rate limiting algorithms",
		Overview: "Rate limiting controls the number of requests a client can make in a time window to prevent abuse and protect system capacity; token bucket, leaky bucket, sliding window counter, and fixed window counter are the main algorithms — each with different burst behaviour.",
		Question: "Your API allows 100 requests per minute per user, but a misbehaving client sends 100 requests in the first second then 0 for the rest of the minute. A fixed window counter allows all 100 requests through instantly. What algorithm smooths out request bursts?",
		Explanation: "Fixed window: count requests in a fixed 60-second window — simple but 'boundary burst' allows 200 requests in 2 seconds straddling a window boundary. Sliding window log: store timestamps of each request; count how many fall within the last 60 seconds — accurate but memory-intensive. Sliding window counter: interpolate between two fixed windows — good accuracy with low memory. Token bucket: a bucket holds up to N tokens; tokens refill at rate R per second; each request consumes one token. Allows bursts (use multiple tokens at once) up to bucket size, then enforces the average rate — the most common algorithm. Leaky bucket: requests enter a queue and are processed at a fixed rate — pure rate smoothing, no bursts allowed. Redis is the most common implementation substrate: `INCR` + `EXPIRE` for fixed window; sorted sets for sliding window log. HTTP 429 Too Many Requests is the standard response code for rate-limited requests.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Reliability",
		Name:     "Circuit breaker pattern",
		Overview: "The circuit breaker monitors calls to a downstream service; after a failure threshold is exceeded it 'opens' — failing fast without attempting the call — then periodically probes to see if the service has recovered before 'closing' again.",
		Question: "Your service calls a slow, failing payment API. Each call times out after 30 seconds, exhausting your thread pool. Your service goes down because of someone else's outage. What pattern fails fast and protects your service from cascading failures?",
		Explanation: "Without a circuit breaker, your service waits 30 seconds per request — threads pile up waiting for the payment API, your thread pool exhausts, your service itself crashes. The circuit breaker sits in front of the external call and has three states: Closed (normal — calls pass through, failures are counted). Open (tripped — after e.g. 50% failure rate in 30 seconds, all calls immediately return an error without attempting the actual call — fail fast in milliseconds instead of 30 seconds). Half-open (recovery probe — after a cool-down period, one test request is allowed through; if it succeeds, close the circuit; if not, reopen). This pattern turns 'my dependency is down so I'm down too' into 'my dependency is down, I return a fallback response and recover when it does'. Netflix's Hystrix popularised this; Resilience4j, Polly (.NET), and Go's `gobreaker` library implement it.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Reliability",
		Name:     "Idempotency in distributed systems",
		Overview: "An idempotent operation produces the same result whether executed once or many times — critical in distributed systems where network failures cause retries, potentially duplicating payments, emails, or database inserts.",
		Question: "A user clicks 'Pay' and the network drops after your server processes the payment but before it sends a response. The client retries. Your server processes the payment again — charging the user twice. How do you prevent duplicate processing on retries?",
		Explanation: "Idempotency key: the client generates a unique ID (UUID) for each logical operation and includes it in the request header. The server stores processed idempotency keys in Redis with the result. On receiving a request: if the key was already processed, return the cached result immediately without reprocessing. If new, process it, store the result keyed by the idempotency key, then return the response. The client can safely retry any number of times — the second and subsequent calls are no-ops. Stripe's API requires an `Idempotency-Key` header for all payment requests. HTTP GET, PUT, and DELETE are naturally idempotent by specification; POST is not. Database-level idempotency: use `INSERT … ON CONFLICT DO NOTHING` with a unique constraint on the idempotency key column, making the deduplication atomic.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Architecture",
		Name:     "Microservices vs monolith",
		Overview: "A monolith deploys all functionality as one unit — simple to develop and operate early on; microservices split functionality into independently deployable services — enabling team autonomy and independent scaling but at the cost of distributed system complexity.",
		Question: "Your startup has 5 engineers and a single Rails app that's becoming hard to deploy. A colleague suggests splitting it into 20 microservices. Is this a good idea, and what does the tradeoff actually look like?",
		Explanation: "Monolith advantages: simple local development, one deployment, no network calls between components, easy debugging (one log stream), strong consistency (one database transaction). Start here. Microservices advantages: teams own and deploy services independently (Spotify's 'squad' model), services can be written in different languages, individual services scale independently (scale only the image-processing service, not the auth service). Microservices costs: network latency between services, distributed tracing to debug a request spanning 10 services, eventual consistency between service databases, complex deployment (Kubernetes, service mesh), and the overhead of API contracts between teams. Rule of thumb: start with a well-structured monolith. Migrate to microservices when you have a specific problem to solve (a team blocked on another team, one component needing vastly different scaling). Amazon was a monolith for years before the famous 'everyone must expose services via APIs' mandate.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Architecture",
		Name:     "API gateway",
		Overview: "An API gateway is a single entry point for all client requests to a microservices backend — handling authentication, rate limiting, request routing, SSL termination, logging, and response transformation centrally rather than duplicating this in every service.",
		Question: "You have 15 microservices. Every service implements authentication, rate limiting, and logging independently — 15 copies of the same code, each potentially different. What architectural component centralises all of this?",
		Explanation: "An API gateway sits between clients and your services, acting as the 'front door'. It handles cross-cutting concerns: Authentication — verify JWT tokens or API keys once, before requests reach any service. Rate limiting — enforce per-client limits without each service implementing it. Routing — route `/users/*` to the user service, `/payments/*` to the payment service. SSL termination — decrypt HTTPS once at the gateway; services communicate internally over plain HTTP. Request/response transformation — rename fields, aggregate multiple service calls into one response. Logging — centralised access logs for all API traffic. Examples: AWS API Gateway, Kong, nginx with Lua, Traefik. The tradeoff: the gateway becomes a critical bottleneck and single point of failure — it must be highly available and fast. A gateway that adds 50ms to every request is 50ms of user-visible latency.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Architecture",
		Name:     "Event-driven architecture and event sourcing",
		Overview: "Event-driven architecture communicates state changes as immutable events on a bus (Kafka, SNS); event sourcing stores the full history of events as the source of truth rather than only the current state — enabling audit logs, temporal queries, and replaying history.",
		Question: "A traditional database stores the current account balance. If you want to know what the balance was 3 months ago, or replay all transactions to find a bug, it's impossible. What architectural pattern stores the history of every change instead of just the latest state?",
		Explanation: "Event sourcing: instead of `UPDATE accounts SET balance=500`, you append an event: `{type: 'deposit', amount: 200, timestamp: ...}`. The current balance is computed by replaying all events from the beginning. Benefits: complete audit trail (required for financial systems), rewind to any point in time, rebuild projections (create a new 'read model' by replaying all events from scratch), replay events to fix bugs in downstream systems. Event-driven architecture uses events to communicate between services: when an order is placed, publish an `order_placed` event to Kafka; the inventory service, email service, and analytics service all subscribe independently. This decouples services — the order service doesn't know or care about who consumes the event. Kafka retains events for days or weeks, enabling new consumers to read past events. The main challenge: querying event-sourced data requires building and maintaining read-side projections (CQRS — Command Query Responsibility Segregation).",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Storage",
		Name:     "Object storage vs block storage vs file storage",
		Overview: "Block storage provides raw disk volumes (like an SSD attached to a server); file storage provides a shared filesystem with a directory tree; object storage (S3) stores arbitrary blobs addressable by key — infinitely scalable and cheap but no in-place edit.",
		Question: "You need to store 50 million user-uploaded images that are served publicly. Storing them on the application server's disk doesn't scale, and a traditional filesystem can't be shared across 10 servers. What type of storage is designed for this?",
		Explanation: "Block storage (AWS EBS, Azure Disk): a virtual disk attached to one server. The OS treats it like a local drive — very fast (low latency), supports any filesystem, but not shared across servers and tied to the machine lifecycle. File storage (AWS EFS, NFS): a shared filesystem mounted by multiple servers simultaneously. Good for shared configs, legacy apps expecting a filesystem API. But expensive at scale and not globally distributed. Object storage (AWS S3, GCS, Cloudflare R2): store any file (image, video, log) as an object identified by a key string. No directories — just buckets and keys. Access via HTTP API. Infinitely scalable, pay per GB stored. Objects are immutable — you can't append to a file; you overwrite it entirely. Global availability, built-in versioning, lifecycle rules (auto-delete after 90 days). Why S3 for user uploads: cheap ($0.023/GB/month), HTTP URLs directly servable to users, no server management, automatic replication across availability zones.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Storage",
		Name:     "Write-ahead logging (WAL) and durability",
		Overview: "Write-ahead logging ensures durability by writing changes to an append-only log before applying them to data pages — on crash, the database replays the log to recover committed transactions, guaranteeing no data loss for committed writes.",
		Question: "Your database server crashes mid-write. On restart, how does the database know which transactions to recover and which to roll back — without scanning every data file?",
		Explanation: "Before modifying any data page on disk, the database first writes a log record describing the change to the WAL (a sequential append-only file). Sequential writes are much faster than random I/O — the WAL is the reason databases can commit transactions quickly. On crash recovery: the database replays WAL records for committed transactions whose data pages weren't yet flushed ('redo') and rolls back WAL records for transactions that were mid-flight ('undo'). This guarantees ACID durability: if `COMMIT` returned success, the WAL was already flushed — the transaction is durable even if the server crashes a millisecond later. PostgreSQL's WAL also enables streaming replication: replicas receive and replay the same WAL stream the primary writes. `fsync()` is the syscall that ensures WAL writes reach physical disk (not just OS buffer) — disabling it makes writes faster but loses the durability guarantee.",
	},
	{
		Category: "System Design",
		Layer:    "System Design — Distributed Systems",
		Name:     "Distributed transactions and the Saga pattern",
		Overview: "Distributed transactions spanning multiple services or databases cannot use a single ACID transaction; the Saga pattern decomposes the operation into a sequence of local transactions, each publishing an event, with compensating transactions to undo previous steps on failure.",
		Question: "An e-commerce checkout must deduct inventory, charge the customer, and create a shipment — across three separate microservices with three separate databases. A traditional database transaction can't span all three. How do you maintain consistency?",
		Explanation: "Two-phase commit (2PC): a coordinator asks all participants to 'prepare' (lock resources), then tells all to 'commit' or all to 'rollback'. Distributed locking during the prepare phase is slow and the coordinator is a single point of failure — rarely used in modern microservices. Saga pattern: choreography-based — each service publishes an event after its local transaction; downstream services react to events. If inventory deduction publishes `inventory_reserved`, payment service charges the card and publishes `payment_completed`, then shipment is created. On failure: each step has a compensating transaction (refund payment, release inventory). Orchestration-based Saga: a central saga orchestrator sends commands and handles the sequence — easier to reason about. The Saga pattern accepts eventual consistency — there's a brief window where inventory is deducted but payment hasn't been charged. Good design includes timeouts and automatic compensation to resolve partial states.",
	},

	// ── Java + Spring ─────────────────────────────────────────────────────────

	{
		Category: "Java + Spring",
		Layer:    "Java — JVM Internals",
		Name:     "JVM and bytecode execution",
		Overview: "Java source compiles to platform-independent bytecode (.class files) that the JVM interprets or JIT-compiles to native machine code at runtime — 'write once, run anywhere' is achieved because every platform ships its own JVM implementation.",
		Question: "Java code runs on Windows, Linux, and macOS from the same .jar file without recompilation. C code must be compiled separately for each OS. What layer makes Java portable, and what does it actually execute?",
		Explanation: "The Java compiler (`javac`) produces bytecode — a compact instruction set for a virtual machine, not for any real CPU. The JVM on each platform interprets or compiles this bytecode to native CPU instructions. The JIT (Just-In-Time) compiler watches which bytecode runs frequently (hot paths) and compiles those to optimised native code on the fly — so long-running Java programs approach C-level performance. The JVM also manages memory, provides garbage collection, enforces security, and loads classes dynamically. GraalVM's Native Image breaks this model: it AOT (ahead-of-time) compiles Java to a native binary for one target platform — no JVM needed at runtime, instant startup, less memory — used by Quarkus and Micronaut for cloud functions.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — JVM Internals",
		Name:     "Java Garbage Collection (GC strategies)",
		Overview: "Java's GC automatically reclaims heap memory for objects with no live references; modern GCs (G1, ZGC, Shenandoah) use generational collection and concurrent marking to minimise stop-the-world pauses — critical for latency-sensitive services.",
		Question: "Your Java service handles real-time requests but occasionally freezes for 500ms while the GC runs. Users see timeouts. What is happening and how do you reduce these pauses?",
		Explanation: "Stop-the-world (STW) pauses: the GC must freeze all application threads to safely scan the heap for live objects. Old GCs (Serial, Parallel) had pauses proportional to heap size — a 32 GB heap could pause for seconds. G1GC (default since Java 9): divides the heap into regions, collects the most garbage-dense regions first ('Garbage First'), targeting configurable pause goals. ZGC (Java 15+) and Shenandoah: concurrent GCs that do almost all work while the application runs, targeting sub-millisecond pauses even on terabyte heaps. Generational hypothesis: most objects die young. Young generation (Eden + Survivor) is collected frequently with tiny pauses (minor GC); old generation is collected infrequently (major GC). Tune GC with JVM flags: `-XX:+UseZGC`, `-Xmx4g`, `-Xms4g`. Monitor with JVM metrics, GC logs (`-Xlog:gc`), and visualise with GCEasy.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — JVM Internals",
		Name:     "Java memory model (heap, stack, metaspace)",
		Overview: "JVM memory is divided into heap (objects and arrays, GC-managed), stack (per-thread frames with local variables and return addresses), metaspace (class metadata, method bytecode), and code cache (JIT-compiled native code).",
		Question: "Your Java service throws `OutOfMemoryError: Java heap space` under load. A colleague says 'just add more RAM', but another says 'check for memory leaks'. What parts of JVM memory exist and which one is leaking?",
		Explanation: "Heap: where all objects live. Too small → OOM under load. Growing without bound → memory leak (objects referenced but never used — common in static collections, thread-locals, listeners not removed). Stack: each thread has its own stack. Deep recursion → `StackOverflowError`. Thread pools with thousands of threads → stack memory adds up. Metaspace (replaced PermGen in Java 8): stores class definitions, method bytecodes, and constant pools. Frameworks that generate classes at runtime (Spring's proxy generation, Hibernate, ByteBuddy) can fill metaspace — `-XX:MaxMetaspaceSize` caps it. Code cache: JIT-compiled native code — rarely an issue. Diagnose heap leaks: heap dump (`jmap -dump:live,format=b,file=heap.hprof <pid>`), then analyse with Eclipse MAT or VisualVM to find which objects are holding references unexpectedly.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Core Language",
		Name:     "equals() and hashCode() contract",
		Overview: "If two objects are equal by equals(), they must have the same hashCode(); failing this contract causes HashMap and HashSet to silently malfunction — objects are stored but cannot be found.",
		Question: "You store a custom `User` object in a `HashMap`. You override `equals()` but forget `hashCode()`. You can `put()` the user, but `get()` always returns null even with an equal key. Why?",
		Explanation: "HashMap uses the hash code to find the right bucket first, then equals() to find the right entry within that bucket. If two equal objects have different hash codes (the default Object.hashCode() returns the memory address), they land in different buckets — `get()` checks the wrong bucket and finds nothing. The contract: if `a.equals(b)` is true, then `a.hashCode() == b.hashCode()` must be true. The inverse is not required: different objects can share a hash code (hash collision). Best practice: always override both together, using the same fields in both. Java 7+: `Objects.hash(field1, field2)` generates a correct hashCode. IDEs (IntelliJ, Eclipse) generate correct pairs automatically. Records (Java 16+) auto-generate correct equals/hashCode based on all components.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Core Language",
		Name:     "Checked vs unchecked exceptions",
		Overview: "Checked exceptions (extend Exception) must be declared or caught by the caller — enforced by the compiler; unchecked exceptions (extend RuntimeException) do not — used for programming errors and conditions callers cannot reasonably handle.",
		Question: "Your method throws `IOException` when a file is missing. Every caller must either catch it or declare `throws IOException`. A colleague says 'just use a RuntimeException instead'. When should you use each?",
		Explanation: "Checked exceptions signal conditions that a well-written program should anticipate and recover from: file not found (maybe prompt the user), network timeout (maybe retry), SQL exception (maybe log and show an error page). The compiler forcing callers to handle them ensures recovery is considered. Unchecked (RuntimeException) signals programming errors: NullPointerException (caller should have null-checked), ArrayIndexOutOfBoundsException (bug in the caller), IllegalArgumentException (caller passed invalid input). Modern Java style leans toward unchecked for most application code — checked exceptions make lambdas and streams awkward (checked exceptions can't be thrown inside `forEach`). Spring wraps JDBC's checked `SQLException` into unchecked `DataAccessException`. Kotlin has no checked exceptions. When in doubt: use unchecked and document the conditions in Javadoc.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Core Language",
		Name:     "Java Generics and type erasure",
		Overview: "Java generics add compile-time type safety; type erasure removes generic type information at runtime — `List<String>` and `List<Integer>` are both just `List` at runtime, which is why instanceof checks on generic types and generic array creation are prohibited.",
		Question: "At runtime, `instanceof List<String>` is a compile error. A method accepting `List<String>` and `List<Integer>` cannot be overloaded. Why does Java behave this way?",
		Explanation: "Generics were added to Java 5 while maintaining backward compatibility with Java 1.4 bytecode and libraries. The solution: type erasure — the compiler uses generic types for compile-time checks, then erases them from the bytecode. `List<String>` compiles to plain `List` in the bytecode; `(String) list.get(0)` casts are inserted automatically. At runtime, the JVM only sees `List` — there's no way to check if it's a `List<String>`. Consequences: you can't do `new T[]` (no generic arrays), you can't do `new T()` (no generic instantiation without a Class<T>), and you can't overload on generic types. Workarounds: pass a `Class<T>` token at runtime, use `TypeToken` (Guava) for complex generic types, or use Kotlin's `reified` type parameters which preserve type info at the call site.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Core Language",
		Name:     "Java Streams API and functional style",
		Overview: "Java Streams (Java 8+) provide a declarative, lazy pipeline for processing collections: source → intermediate operations (filter, map, sorted) → terminal operation (collect, reduce, forEach) — enabling concise, readable data transformations.",
		Question: "You have a `List<Order>` and need to filter orders over $100, extract customer names, deduplicate, sort alphabetically, and collect to a list. How does the Streams API compare to a traditional for-loop approach?",
		Explanation: "Stream pipeline: `orders.stream().filter(o -> o.getAmount() > 100).map(Order::getCustomerName).distinct().sorted().collect(Collectors.toList())` — reads like a description of the problem. Intermediate operations (filter, map, distinct, sorted) are lazy: they don't execute until a terminal operation is called. This enables short-circuiting — `findFirst()` stops after the first match without processing the whole list. Parallel streams: `.parallelStream()` splits work across CPU cores automatically — effective for CPU-bound operations on large collections, but adds overhead for small collections and operations with side effects. `Collectors.groupingBy()` creates maps: `groupingBy(Order::getStatus)` → `Map<Status, List<Order>>`. Common mistake: modifying external state inside a stream — streams must be stateless and non-interfering to be parallelisable.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Core Language",
		Name:     "Interface vs abstract class in Java",
		Overview: "Interfaces define contracts (what a class can do) with no state; abstract classes define partial implementations (what a class is) with state — Java allows implementing multiple interfaces but extending only one abstract class.",
		Question: "You're designing a `Shape` hierarchy. Should `Shape` be an interface or an abstract class — and what changes if you add a `calculateArea()` method that shares implementation logic across all shapes?",
		Explanation: "Interface: a pure contract. All methods are implicitly abstract and public. No instance fields (only static final constants). A class can implement many interfaces — Java's answer to multiple inheritance of behaviour. Java 8 added default methods (concrete implementations in interfaces) and static methods. Use interfaces when: defining a capability (Comparable, Serializable, Runnable) that any class can adopt regardless of its hierarchy. Abstract class: can have instance fields, constructors, and both abstract and concrete methods. Enforces an IS-A relationship. Use when: you want to share implementation code among related classes (a `Shape` with a `color` field and a `toString()` implementation). Golden rule: program to interfaces, not implementations. If `calculateArea()` has no shared logic, make it an interface method. If it shares code (e.g., unit conversion), put the shared logic in a default method or helper.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Concurrency",
		Name:     "Java concurrency (synchronized, volatile, happens-before)",
		Overview: "Java's memory model guarantees visibility of shared state only at synchronisation boundaries; `synchronized` provides mutual exclusion and visibility; `volatile` guarantees visibility without mutual exclusion; happens-before defines when one thread's writes are visible to another.",
		Question: "Two threads share a `boolean running = true` field. Thread A sets `running = false`. Thread B reads `running` in a loop but never sees the change and loops forever. What guarantees Thread B will see the update?",
		Explanation: "Without synchronisation, the JVM and CPU are free to cache field values in registers or CPU caches. Thread B may read a stale cached value indefinitely. `volatile boolean running`: marks the field as always read from and written to main memory. Thread B will see Thread A's update — but volatile does NOT provide atomicity (read-modify-write like `count++` is not atomic even on volatile). `synchronized`: entering a synchronized block flushes the thread's cache and establishes a happens-before relationship — all writes before the lock release are visible to the next thread that acquires the same lock. For compound operations (check-then-act, read-modify-write), use `synchronized` or `java.util.concurrent.atomic` classes (`AtomicInteger`, `AtomicBoolean`) which use CPU-level atomic instructions without locking. `java.util.concurrent` (Java 5+): prefer `ReentrantLock`, `ConcurrentHashMap`, `BlockingQueue` over raw `synchronized`.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Concurrency",
		Name:     "Thread pools and ExecutorService",
		Overview: "Thread pools pre-create and reuse threads to avoid the expensive overhead of creating a new OS thread per task; ExecutorService manages pool size, task queuing, and lifecycle — critical for controlling resource usage in servers.",
		Question: "For every incoming HTTP request, your service creates a new Java thread to handle it. Under heavy load, 10,000 threads are created, the JVM runs out of memory, and the server crashes. What abstraction prevents this?",
		Explanation: "Creating a thread allocates ~512 KB of stack memory and involves an OS system call — thousands of threads are expensive. Thread pools solve this: create N threads once; submit tasks to a queue; threads pick up tasks as they become free. `Executors.newFixedThreadPool(20)` — exactly 20 threads, tasks queue when all are busy. `newCachedThreadPool()` — creates threads on demand but reuses idle ones; good for short tasks. `newScheduledThreadPool()` — runs tasks on a schedule. `ThreadPoolExecutor` gives full control: core size, max size, keep-alive time, queue type (bounded `ArrayBlockingQueue` vs unbounded `LinkedBlockingQueue` — prefer bounded to avoid memory issues under load). Java 21 virtual threads: `Executors.newVirtualThreadPerTaskExecutor()` — creates millions of lightweight virtual threads without the OS overhead, eliminating most thread pool tuning for I/O-bound services.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Concurrency",
		Name:     "CompletableFuture and async Java",
		Overview: "CompletableFuture (Java 8+) represents an asynchronous computation that may complete in the future — it enables chaining, combining, and composing async operations without blocking threads, using callbacks on completion.",
		Question: "Your service calls three external APIs and currently awaits each one sequentially — 3 × 500ms = 1,500ms. All three are independent. How do you call them in parallel and combine the results without blocking three threads?",
		Explanation: "Sequential: `a.get(); b.get(); c.get()` — 1,500ms. Parallel with CompletableFuture: `CompletableFuture.allOf(futureA, futureB, futureC).thenRun(() -> combine(futureA.join(), futureB.join(), futureC.join()))` — ~500ms (the slowest one). `supplyAsync(() -> callApi())` executes the call on the ForkJoinPool. Chaining: `.thenApply(result -> transform(result))` — non-blocking transformation after completion. `.thenCompose(result -> anotherFuture(result))` — flat-maps futures (like flatMap for streams). `.exceptionally(e -> fallback)` — handles failures. `allOf()` waits for all; `anyOf()` completes when any one does. CompletableFuture is the Java equivalent of JavaScript's Promise. Spring WebFlux and Project Reactor go further — fully reactive, non-blocking I/O throughout the stack.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Modern Features",
		Name:     "Java Optional and null safety",
		Overview: "Optional<T> is a container that either holds a value or is empty — making the possibility of absence explicit in the type system and enabling fluent null-safe chaining instead of defensive null checks.",
		Question: "A method returning `User` returns `null` when the user isn't found. Every caller must null-check or risk NullPointerException. How does Optional<User> improve this, and what should you avoid with Optional?",
		Explanation: "Without Optional: `User u = findUser(id); if (u != null) { String name = u.getName(); }` — easy to forget the null check. With Optional: `findUser(id).map(User::getName).orElse(\"Unknown\")` — the null case is handled in the pipeline. `orElse(default)`: return a value if present, else the default. `orElseGet(() -> compute())`: lazy default (only computed if empty). `orElseThrow(() -> new NotFoundException())`: throw if empty. `ifPresent(u -> log(u))`: run a side effect only if present. `flatMap()`: chain operations that also return Optional. What to avoid: `optional.get()` without `isPresent()` — this throws if empty, no better than null. Don't use Optional as a method parameter — it complicates the API. Don't use Optional in fields or collections — use empty collections instead. Optional is for return types of methods that genuinely may produce no result.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Modern Features",
		Name:     "Java virtual threads (Project Loom)",
		Overview: "Virtual threads (Java 21) are lightweight threads managed by the JVM rather than the OS — you can create millions of them with minimal overhead, eliminating thread pool sizing and enabling a simple synchronous coding style for high-concurrency I/O services.",
		Question: "Your service uses a fixed thread pool of 200 threads because OS threads are expensive. Under load, requests queue up waiting for a free thread. Java 21 virtual threads claim to solve this. What are they and how?",
		Explanation: "An OS thread costs ~1 MB of stack and is scheduled by the kernel. Virtual threads cost a few KB and are scheduled by the JVM. When a virtual thread blocks on I/O (database query, HTTP call), the JVM unmounts it from its carrier OS thread, which then picks up another runnable virtual thread — similar to Go's goroutines. Result: 1 virtual thread per request, blocking I/O style, but the OS only sees a handful of carrier threads. A server handling 10,000 concurrent requests needs only 10,000 virtual threads (tiny memory) instead of 10,000 OS threads (10 GB of stack). Migration: `Executors.newVirtualThreadPerTaskExecutor()` or configure Spring Boot's embedded Tomcat to use virtual threads. Caveat: virtual threads don't help CPU-bound code (still need a thread pool bounded to CPU core count) and pinning (a virtual thread using `synchronized` blocks its carrier OS thread — use `ReentrantLock` instead).",
	},
	{
		Category: "Java + Spring",
		Layer:    "Java — Modern Features",
		Name:     "Java Records",
		Overview: "Records (Java 16+) are concise, immutable data carrier classes — the compiler auto-generates constructor, getters, equals(), hashCode(), and toString() based on the declared components.",
		Question: "You have a `Point` class with `x` and `y` fields, a constructor, getters, and boilerplate equals/hashCode/toString — 50 lines of code. How do records reduce this to 1 line?",
		Explanation: "`record Point(int x, int y) {}` — one line. The compiler generates: a constructor `Point(int x, int y)`, accessor methods `x()` and `y()`, `equals()` comparing all components, `hashCode()` based on all components, and `toString()` like `Point[x=3, y=4]`. Components are implicitly `private final` — records are immutable by default. You can add compact constructors for validation: `record Point(int x, int y) { Point { if (x < 0) throw new IllegalArgumentException(); } }`. Records can implement interfaces, have static fields, and define additional methods — but can't extend classes (they implicitly extend `java.lang.Record`). Perfect for: DTOs, value objects, response/request bodies, config data, query results. Use with `sealed` interfaces for algebraic data types: `sealed interface Shape permits Circle, Rectangle {}` `record Circle(double radius) implements Shape {}`.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Core",
		Name:     "Spring IoC container and dependency injection",
		Overview: "Spring's IoC (Inversion of Control) container creates and wires application objects (beans) — instead of classes constructing their dependencies themselves, Spring injects them — making code testable, modular, and decoupled from specific implementations.",
		Question: "Your `OrderService` creates a `new PaymentService()` internally. Testing `OrderService` requires a real `PaymentService`. How does Spring's dependency injection fix this and what does 'inversion of control' mean?",
		Explanation: "Without DI: `OrderService` controls its own dependencies — it decides which `PaymentService` implementation to use. Inverting control: `OrderService` declares what it needs (via a constructor parameter or `@Autowired` field), and the Spring container provides it. The container reads your configuration (`@Configuration` classes or `@Component` annotations), instantiates all beans, and wires them together. For testing: the container injects a mock `PaymentService` — `OrderService` is none the wiser. Constructor injection (preferred): `@Component class OrderService { private final PaymentService paymentService; OrderService(PaymentService ps) { this.paymentService = ps; } }` — dependencies are explicit and the class is testable without Spring. Field injection (`@Autowired` on fields) is convenient but hides dependencies and makes testing without Spring harder. Spring Boot's `@SpringBootApplication` triggers component scanning of the whole package tree.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Core",
		Name:     "Spring bean scopes and lifecycle",
		Overview: "Spring beans have a scope (singleton by default — one shared instance; prototype — new instance per request; request/session — one per HTTP request/session in web apps); the container calls lifecycle callbacks on creation and destruction.",
		Question: "A `@Component` service holding request-specific state (current user, locale) is shared across all requests because it's a singleton. Concurrent requests corrupt each other's state. What scope fixes this?",
		Explanation: "Singleton (default): one instance per Spring container — fine for stateless services. All concurrent calls share the same object — any mutable state is a race condition. Prototype: `@Scope(\"prototype\")` — a new instance is created every time it's injected or retrieved. The container creates it but does NOT destroy it — you're responsible for cleanup. Request scope: `@Scope(\"request\")` — one instance per HTTP request, destroyed when the request ends. Session scope: one instance per HTTP session. Lifecycle callbacks: `@PostConstruct` is called after all dependencies are injected — ideal for initialisation logic. `@PreDestroy` is called when the container shuts down — ideal for releasing resources (closing connections, saving state). Alternatively implement `InitializingBean`/`DisposableBean` interfaces. `@Lazy` delays bean creation until first use — reduces startup time and breaks circular dependency cycles.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Core",
		Name:     "Spring AOP (Aspect-Oriented Programming)",
		Overview: "Spring AOP adds cross-cutting concerns (logging, security, transactions, caching) to beans by wrapping them in proxies at runtime — you write the concern once in an Aspect and apply it declaratively with annotations, without touching the business code.",
		Question: "You need to log the execution time of every method in your service layer. Adding `System.currentTimeMillis()` calls to every method is repetitive and mixes logging concerns with business logic. How does Spring AOP centralise this?",
		Explanation: "AOP terminology: Aspect = the cross-cutting concern (your timing logger). Advice = what runs (the timing code). Pointcut = where it applies (expression matching 'all methods in com.example.service.*'). Join point = the actual method call being intercepted. Spring wraps your bean in a JDK proxy (for interface-based beans) or a CGLIB subclass proxy. When a method is called, the proxy runs the advice around the real call. `@Around` advice: `Object proceed = joinPoint.proceed(); long elapsed = System.currentTimeMillis() - start;` — runs before AND after the real method, can modify the return value or suppress the call. `@Before` runs before only; `@After` always runs; `@AfterReturning` runs on success; `@AfterThrowing` runs on exception. `@Transactional`, `@Cacheable`, `@Secured`, and `@Async` in Spring are all implemented as AOP aspects internally.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Web",
		Name:     "Spring MVC and the DispatcherServlet",
		Overview: "Spring MVC handles HTTP requests via a central DispatcherServlet that routes requests to @Controller methods, invokes HandlerAdapters, resolves return values to responses, and applies HandlerInterceptors — the foundation for all Spring web applications.",
		Question: "An HTTP request arrives at your Spring Boot app. Before reaching your `@GetMapping` method, it passes through several layers. What does the DispatcherServlet do and in what order?",
		Explanation: "Request flow: (1) DispatcherServlet receives all requests (registered as the root Servlet). (2) HandlerMapping resolves which `@Controller` method matches the path and method. (3) HandlerInterceptors run `preHandle()` — authentication checks, logging, CORS. (4) HandlerAdapter invokes the controller method, resolving `@RequestParam`, `@PathVariable`, `@RequestBody` (via Jackson), and `@RequestHeader` from the raw request. (5) Controller returns a view name, `ResponseEntity<T>`, or an `@ResponseBody` object. (6) ViewResolver resolves a view name to a template (Thymeleaf, FreeMarker). For REST APIs: Jackson serialises the return object to JSON. `@RestController = @Controller + @ResponseBody` on every method. HandlerExceptionResolver / `@ControllerAdvice` with `@ExceptionHandler` catches exceptions globally. `@ResponseStatus` maps exceptions to HTTP status codes.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Web",
		Name:     "Spring Boot auto-configuration",
		Overview: "Spring Boot auto-configuration automatically configures Spring beans based on classpath contents and application properties — if `spring-boot-starter-data-jpa` is on the classpath, a DataSource, EntityManager, and TransactionManager are configured without any XML or Java config.",
		Question: "A plain Spring app requires dozens of `@Bean` definitions just to connect to a database. A Spring Boot app connects with just a connection URL in `application.properties`. What mechanism provides all the missing configuration?",
		Explanation: "Spring Boot scans `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (or `spring.factories` in older versions) — a list of all auto-configuration classes. Each is annotated with `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty` — conditions that must be true for the configuration to apply. `DataSourceAutoConfiguration` activates only if HikariCP and a JDBC driver are on the classpath AND no `DataSource` bean is already defined (so yours takes precedence). `application.properties` / `application.yml` externalises all defaults: `spring.datasource.url`, `spring.jpa.hibernate.ddl-auto`, etc. To see what was configured and why: `--debug` flag prints a 'conditions evaluation report'. To override: define your own `@Bean` of the same type. This convention-over-configuration model means a production-ready Spring Boot app needs almost zero boilerplate configuration.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Data",
		Name:     "Spring Data JPA and repositories",
		Overview: "Spring Data JPA generates repository implementations at runtime from interfaces — method names like `findByEmailAndActive(String email, boolean active)` are parsed and converted to JPQL queries automatically, eliminating boilerplate DAO code.",
		Question: "Writing SQL for every CRUD operation is repetitive. Spring Data JPA claims you can query a database by just declaring a method signature on an interface. How does this work?",
		Explanation: "You declare: `interface UserRepository extends JpaRepository<User, Long> { List<User> findByLastNameOrderByCreatedAtDesc(String lastName); }`. Spring Data parses the method name: `find…By` = SELECT WHERE, `LastName` = WHERE last_name = ?, `OrderByCreatedAtDesc` = ORDER BY created_at DESC. No implementation needed — Spring generates it at startup. For complex queries: `@Query(\"SELECT u FROM User u WHERE u.email LIKE %:domain\")` with `@Param(\"domain\")`. `JpaRepository` gives you `save()`, `findById()`, `findAll()`, `deleteById()` for free. Pagination: `findAll(PageRequest.of(0, 20, Sort.by(\"createdAt\").descending()))` returns a `Page<User>` with total count. Projections: return an interface with only the fields you need — Spring generates a query selecting only those columns. `@EntityGraph` solves N+1 problems by eagerly loading associations when needed.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Data",
		Name:     "JPA N+1 query problem",
		Overview: "The N+1 problem occurs when fetching N parent entities triggers N additional queries to load their lazy-loaded children — 1 query for orders + 1 query per order for its items = N+1 queries total, devastating performance.",
		Question: "You fetch 100 orders to display on a page. Each `Order` has a list of `OrderItem` (lazy-loaded). Rendering the page triggers 101 database queries — 1 for orders and 1 for each order's items. What causes this and how do you fix it?",
		Explanation: "JPA's default fetch type for collections is `LAZY`: the collection is only loaded when first accessed. Iterating over 100 orders and accessing `order.getItems()` on each triggers a query for each order's items — 100 extra queries. Fix 1: `@Query(\"SELECT o FROM Order o JOIN FETCH o.items WHERE o.status = :status\")` — JOIN FETCH loads everything in one SQL query. Fix 2: `@EntityGraph(attributePaths = {\"items\"})` on the repository method — same effect, declarative. Fix 3: `@BatchSize(size = 50)` on the collection — Hibernate loads items in batches of 50 (2 queries instead of 100). Fix 4: use projections/DTOs via `@Query` selecting only the fields you need — no lazy loading at all. Detection: Hibernate's `spring.jpa.show-sql=true` + `logging.level.org.hibernate.SQL=DEBUG` reveals N+1 in logs. Datasource-proxy or p6spy logs queries with stack traces to find the source.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Data",
		Name:     "Spring @Transactional",
		Overview: "@Transactional wraps a method in a database transaction via AOP proxy — if the method throws a RuntimeException, the transaction rolls back; it supports propagation (REQUIRED, REQUIRES_NEW, NESTED) and isolation levels.",
		Question: "Your service method calls two repository methods. The first saves a user, the second sends a notification. If the notification fails, the user save should roll back. How does `@Transactional` achieve this and what traps must you avoid?",
		Explanation: "@Transactional works via a Spring AOP proxy — the proxy starts a transaction before the method runs and commits (or rolls back) after. The real bean's method sees one transaction for both operations; if it throws a RuntimeException, the proxy rolls back both. Key traps: (1) Self-invocation — calling a `@Transactional` method from another method in the same class bypasses the proxy — no transaction. Call through the Spring bean (inject the bean into itself or use `AopContext.currentProxy()`). (2) Checked exceptions don't roll back by default — only RuntimeException and Error. Use `@Transactional(rollbackFor = Exception.class)` to include checked exceptions. (3) Propagation: `REQUIRED` (default) joins an existing transaction or creates one. `REQUIRES_NEW` always creates a new transaction (suspending the outer one) — use for audit logs that must commit even if the outer transaction rolls back. (4) `@Transactional` only on public methods — Spring's proxy can't intercept private or package-private methods.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Security",
		Name:     "Spring Security authentication and authorisation",
		Overview: "Spring Security intercepts requests via a filter chain; authentication verifies identity (username+password, JWT, OAuth2 token); authorisation checks whether the authenticated principal has the required role or permission for the requested resource.",
		Question: "Your REST API must allow unauthenticated access to `/public/**`, require a valid JWT for `/api/**`, and restrict `/admin/**` to users with the ADMIN role. How does Spring Security handle all three rules?",
		Explanation: "Spring Security wraps every HTTP request in a `SecurityFilterChain` — a chain of filters that process authentication and authorisation before the request reaches a controller. Configuration with `SecurityFilterChain` bean: `http.authorizeHttpRequests(auth -> auth.requestMatchers(\"/public/**\").permitAll().requestMatchers(\"/admin/**\").hasRole(\"ADMIN\").anyRequest().authenticated())`. For JWT: add a custom `OncePerRequestFilter` that extracts and validates the JWT from the `Authorization: Bearer <token>` header, then sets a `UsernamePasswordAuthenticationToken` in the `SecurityContextHolder`. Spring Security's `@PreAuthorize(\"hasRole('ADMIN')\")` enables method-level security checked via AOP. `UserDetailsService` loads the user by username from your database. `PasswordEncoder` (BCrypt) hashes passwords — never store plain text. OAuth2 Login (`spring-boot-starter-oauth2-client`) handles Google/GitHub SSO login with minimal configuration.",
	},
	{
		Category: "Java + Spring",
		Layer:    "Spring — Operations",
		Name:     "Spring Boot Actuator and observability",
		Overview: "Spring Boot Actuator exposes production-ready endpoints (/health, /metrics, /info, /env, /loggers) for monitoring and management; integrated with Micrometer, it exports metrics to Prometheus, Datadog, or Grafana.",
		Question: "Your Spring Boot service is deployed in production and you need to check if it's healthy, see JVM memory usage, view active database connections, and change a log level without restarting. How does Spring Boot expose all of this?",
		Explanation: "Add `spring-boot-starter-actuator` to get endpoints automatically. `/actuator/health` — liveness/readiness checks (database connectivity, disk space, custom indicators). Kubernetes uses this for health probes. `/actuator/metrics` — all Micrometer metrics: JVM memory, GC pauses, thread counts, HTTP request counts and latencies, datasource pool sizes. `/actuator/env` — all resolved config properties. `/actuator/loggers` — list loggers and change levels at runtime: `POST /actuator/loggers/com.example HTTP/1.1 {\"configuredLevel\": \"DEBUG\"}`. `/actuator/threaddump` — current thread stack traces for diagnosing deadlocks. `/actuator/heapdump` — download a heap dump. Security: expose only health by default; enable others selectively, and secure admin endpoints with Spring Security. Micrometer: add `micrometer-registry-prometheus` and Prometheus scrapes `/actuator/prometheus` for all metrics.",
	},

	// ── JavaScript / TypeScript / React / Node.js ─────────────────────────────

	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Runtime",
		Name:     "JavaScript event loop",
		Overview: "JavaScript is single-threaded; the event loop processes one task at a time from the call stack, then drains the microtask queue (Promises), then takes the next macro-task (setTimeout, I/O callbacks) — enabling non-blocking async code without threads.",
		Question: "You have `setTimeout(() => console.log('timeout'), 0)` followed by `Promise.resolve().then(() => console.log('promise'))`. Both are 'instant'. Which prints first and why?",
		Explanation: "The event loop has two queues: microtask queue (Promises, queueMicrotask, MutationObserver) and macrotask queue (setTimeout, setInterval, I/O callbacks, requestAnimationFrame). After each task from the call stack completes, the event loop drains the entire microtask queue before picking the next macrotask. `Promise.resolve().then(...)` adds to the microtask queue; `setTimeout(..., 0)` adds to the macrotask queue. So even with 0ms delay, the Promise callback runs first. Call stack: synchronous code runs to completion — a long-running loop blocks the event loop and freezes the browser/server. `async/await` is syntactic sugar over Promises — `await` suspends the async function, returns control to the event loop, and resumes when the Promise resolves (as a microtask). Node.js adds `setImmediate` (after I/O phase) and `process.nextTick` (highest priority microtask, before Promise callbacks).",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Async",
		Name:     "Promises and async/await",
		Overview: "Promises represent eventual completion or failure of an async operation; async/await provides synchronous-looking syntax for Promise chains — both enable non-blocking async code without callback hell.",
		Question: "Callback hell: `fetchUser(id, (user) => fetchOrders(user.id, (orders) => fetchItems(orders[0].id, (items) => ...)`. How do Promises and async/await flatten this?",
		Explanation: "Promise chain: `fetchUser(id).then(user => fetchOrders(user.id)).then(orders => fetchItems(orders[0].id)).then(items => ...)` — linear and readable. `async/await`: `async function load(id) { const user = await fetchUser(id); const orders = await fetchOrders(user.id); const items = await fetchItems(orders[0].id); }` — reads like synchronous code. Error handling: `try/catch` with async/await vs `.catch()` on the chain. Parallel execution: `await` on each sequentially waits for each before starting the next. Use `Promise.all([fetchA(), fetchB(), fetchC()])` to run in parallel and wait for all — returns an array of results or rejects if any rejects. `Promise.allSettled()` waits for all and reports success or failure for each without early-exit on failure. `Promise.race()` resolves/rejects with whichever Promise settles first — useful for timeouts.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Core",
		Name:     "JavaScript closures",
		Overview: "A closure is a function that retains access to its outer scope's variables even after the outer function has returned — the function 'closes over' the variables from its definition environment.",
		Question: "A `makeCounter()` function returns an inner function. Calling the returned function increments and returns a count. How does the inner function remember `count` after `makeCounter` has finished executing?",
		Explanation: "`function makeCounter() { let count = 0; return () => ++count; }`. The returned arrow function forms a closure over `count`. Even after `makeCounter` returns, the `count` variable is kept alive in memory because the returned function holds a reference to it. Each call to `makeCounter()` creates an independent `count` — closures capture variables by reference, not by value. Classic gotcha: `for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 0); }` prints 3, 3, 3 — all closures share the same `var i` and read it after the loop ends. Fix: use `let` (block-scoped, one binding per iteration) or wrap in an IIFE. Closures enable: module patterns (private state), memoisation (cache previous results), event handlers that remember their context, and partial application (pre-fill some arguments).",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Core",
		Name:     "Prototype chain and prototypal inheritance",
		Overview: "Every JavaScript object has an internal [[Prototype]] link to another object; property lookups walk the chain until found or null is reached — this is JS inheritance, and class syntax is syntactic sugar over this prototype mechanism.",
		Question: "A JavaScript `Dog` class extends `Animal`. When you call `dog.speak()`, JS checks `dog` for the method, then `Dog.prototype`, then `Animal.prototype`. What is this chain called and how does `class` relate to it?",
		Explanation: "JavaScript has no classical inheritance — only objects and prototype links. `class` syntax (ES6) is syntactic sugar over constructor functions and prototype assignment: `class Dog extends Animal {}` desugars to `Dog.prototype = Object.create(Animal.prototype); Dog.prototype.constructor = Dog`. Property lookup: check the object itself → its prototype → that prototype's prototype → ... → Object.prototype → null. `Object.create(proto)` creates an object with `proto` as its prototype — explicit prototypal inheritance without classes. `hasOwnProperty(key)` checks only the object itself, not the chain. `instanceof` checks if a constructor's prototype appears anywhere in the prototype chain. `Object.getPrototypeOf(obj)` reveals the prototype. Modern code should use `class` syntax — it's cleaner and less error-prone — but understanding prototypes is essential for debugging framework internals.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Core",
		Name:     "`this` keyword and binding rules",
		Overview: "In JavaScript, `this` is determined by how a function is called, not where it is defined — four binding rules apply: default (global/undefined in strict mode), implicit (object method call), explicit (call/apply/bind), and new (constructor call); arrow functions have no own `this`.",
		Question: "You pass an object method as a callback to `addEventListener`. Inside the callback, `this` is the DOM element, not your object. Why, and how do you fix it?",
		Explanation: "When a function is called as `element.addEventListener('click', obj.handleClick)`, the function reference is extracted from `obj` — it loses its object context. Inside `handleClick`, `this` is the event target (the element), not `obj`. Four `this` rules: (1) Default: called as `func()` → `this` is `window` (or `undefined` in strict mode). (2) Implicit: `obj.method()` → `this` is `obj`. (3) Explicit: `func.call(obj, arg)`, `func.apply(obj, [args])`, `func.bind(obj)` → `this` is `obj`. (4) `new Func()` → `this` is the newly created object. Arrow functions: no own `this` — they inherit `this` from the enclosing lexical scope where they were defined. Fix for the callback problem: `addEventListener('click', this.handleClick.bind(this))` or `addEventListener('click', (e) => this.handleClick(e))` — the arrow function captures `this` from the class constructor.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "TypeScript — Type System",
		Name:     "TypeScript type vs interface",
		Overview: "Both `type` and `interface` define shapes for objects; interfaces are open (can be merged via declaration merging) and support `extends`; type aliases are more flexible — they can alias primitives, unions, intersections, and tuples — and cannot be merged.",
		Question: "You're defining a shape for an API response object. Should you use `interface ApiResponse { ... }` or `type ApiResponse = { ... }`? When does the choice actually matter?",
		Explanation: "In practice: both work for object shapes and the choice is mostly style. Use `interface` when: defining the shape of a class or an extensible public API contract — third-party libraries can augment it via declaration merging (`interface Window { myCustomProp: string }` adds to the built-in `Window`). Use `type` when: creating a union (`type Status = 'pending' | 'done' | 'failed'`), an intersection (`type AdminUser = User & AdminPermissions`), a tuple (`type Pair = [string, number]`), or aliasing a primitive. Key differences: interfaces support `extends` and can be implemented by classes; type aliases cannot be re-opened. `extends` on interface checks compatibility structurally; interface `extends` requires the extended type to be compatible. TypeScript team recommendation (as of 4.x): prefer `interface` for object shapes, `type` for everything else.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "TypeScript — Type System",
		Name:     "TypeScript Generics",
		Overview: "Generics parameterise types and functions so they work with multiple types while remaining type-safe — like a template: `function identity<T>(x: T): T` works with any type T, and the compiler tracks the concrete type at each call site.",
		Question: "You write a `getFirst(arr)` function that returns the first element. Without generics it returns `any`, losing type safety. How do generics let you express 'the return type is the same as the array element type'?",
		Explanation: "`function getFirst<T>(arr: T[]): T { return arr[0]; }` — T is inferred at the call site: `getFirst([1, 2, 3])` → TypeScript infers `T = number`, return type is `number`. `getFirst(['a', 'b'])` → `T = string`. Generic constraints: `function getLength<T extends { length: number }>(x: T): number { return x.length; }` — T must have a `length` property. Multiple type parameters: `function map<T, U>(arr: T[], fn: (x: T) => U): U[]`. Generic interfaces: `interface Repository<T> { findById(id: number): T; save(entity: T): void; }`. Generic classes: `class Stack<T> { push(item: T): void {...} pop(): T {...} }`. Default types: `type Container<T = string> = { value: T }`. Conditional types: `type IsArray<T> = T extends any[] ? true : false`.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "TypeScript — Type System",
		Name:     "TypeScript utility types (Partial, Pick, Omit, Record)",
		Overview: "TypeScript's built-in utility types transform existing types — Partial<T> makes all properties optional, Required<T> makes all required, Pick<T, K> keeps only listed keys, Omit<T, K> removes listed keys, Record<K, V> creates a map type.",
		Question: "You have a full `User` type but a PATCH endpoint only needs optional fields. A `CreateUser` type needs all fields except `id`. How do utility types derive these from the original type without duplication?",
		Explanation: "`Partial<User>` — every property becomes optional. `Required<User>` — every property becomes required. `Pick<User, 'name' | 'email'>` — only name and email. `Omit<User, 'id'>` — everything except id (perfect for create DTOs). `Record<string, number>` — an object with string keys and number values. `Readonly<User>` — all properties become readonly. `ReturnType<typeof someFunction>` — the return type of a function without calling it. `Parameters<typeof fn>` — tuple of the parameter types. `NonNullable<string | null | undefined>` → `string`. These are all implemented with conditional types and mapped types internally. Composition: `type PatchUser = Partial<Omit<User, 'id'>>` — a patch body that's all optional and lacks the id. Avoids copy-paste type definitions that drift out of sync.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "TypeScript — Type System",
		Name:     "TypeScript discriminated unions",
		Overview: "A discriminated union is a union of types each having a common literal property ('discriminant') — TypeScript narrows the type within a switch/if block on the discriminant, giving type-safe access to variant-specific fields.",
		Question: "A `Result` type can be `{status: 'ok', data: User}` or `{status: 'error', message: string}`. After a fetch, how does TypeScript know which fields are available without casting?",
		Explanation: "`type Result = { status: 'ok'; data: User } | { status: 'error'; message: string }`. TypeScript uses the `status` literal as the discriminant. In a `switch (result.status)` block: `case 'ok': result.data // ✓ TypeScript knows data exists` `case 'error': result.message // ✓ TypeScript knows message exists`. Trying to access `result.data` outside a narrowing block is a compile error. This pattern models: API responses, loading states (`'idle' | 'loading' | 'success' | 'error'`), action types in Redux reducers, command/event types. With `switch` and exhaustiveness checking: adding a `default: const _: never = result` causes a compile error if a new variant is added to the union but not handled — the type system enforces completeness. This is the TypeScript equivalent of algebraic data types / tagged unions from functional languages.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "React — Core",
		Name:     "React hooks: useState and useEffect",
		Overview: "useState provides component-local state that triggers re-renders on change; useEffect runs side effects (data fetching, subscriptions, DOM mutations) after renders, with a dependency array controlling when it re-runs.",
		Question: "Your component needs to fetch user data when it mounts and re-fetch when the userId prop changes. It also sets up an event listener that must be removed on unmount. How do useState and useEffect handle all of this?",
		Explanation: "`const [user, setUser] = useState(null)` — `user` is the current state, `setUser` triggers a re-render with new state. `useEffect(() => { fetchUser(userId).then(setUser); return () => cleanup(); }, [userId])`. The dependency array `[userId]`: run the effect after mount AND whenever `userId` changes. Return a cleanup function to remove subscriptions/timers on unmount or before the next effect run. Empty array `[]`: run once on mount only. No array: run after every render (rarely what you want). Common mistake: including a function in the dependency array that is recreated every render — causes infinite loops. Fix: wrap the function in `useCallback` or move it inside the effect. `useEffect` is always asynchronous (runs after the browser paints); `useLayoutEffect` runs synchronously before paint — use for DOM measurements.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "React — Core",
		Name:     "useMemo and useCallback — performance hooks",
		Overview: "useMemo caches an expensive computed value and recomputes only when dependencies change; useCallback caches a function reference and recreates it only when dependencies change — both prevent unnecessary child re-renders when passed as props.",
		Question: "A parent component re-renders on every keystroke. It passes an `onSubmit` function and a filtered list to a child wrapped in `React.memo`. Despite `memo`, the child re-renders every time. Why, and what fixes it?",
		Explanation: "Every parent re-render creates a new `onSubmit` function reference and a new filtered array — even if their content is identical. `React.memo` does a shallow comparison of props: new reference = different prop = re-render. `useCallback(fn, [deps])`: returns the same function reference across re-renders unless dependencies change. `useMemo(() => compute(), [deps])`: returns the same value across re-renders unless dependencies change. Fix: `const onSubmit = useCallback(() => handleSubmit(data), [data])` and `const filtered = useMemo(() => items.filter(predicate), [items, predicate])`. When to use: useMemo for expensive computations (sorting/filtering large arrays), useCallback for functions passed to heavily-rendered children or used as useEffect dependencies. Do NOT add them everywhere — they have overhead. Profile first with React DevTools Profiler, then optimise hot spots.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "React — State Management",
		Name:     "React Context API vs global state (Redux, Zustand)",
		Overview: "Context provides dependency injection for a subtree — any component can read a value without prop-drilling; it is not optimised for frequent state updates because every consumer re-renders when context value changes — global state libraries (Redux, Zustand, Jotai) solve this with selective subscriptions.",
		Question: "You use Context to share a large app state object. Updating one field causes every component consuming the context to re-render, even those that only read other fields. How do you fix this?",
		Explanation: "Context re-renders all consumers when the context value changes — because React can't know which parts of the value each consumer reads. Workarounds: split into multiple contexts (AuthContext, ThemeContext, CartContext), each updating independently. Memoize children with `React.memo`. Use a state management library: Zustand uses selectors — `const count = useStore(state => state.count)` — the component only re-renders if `count` changes, even if other state fields change. Redux with `useSelector` works the same way. Jotai and Recoil use atoms — granular pieces of state each with their own subscribers. Rule of thumb: Context is ideal for slow-changing global data (theme, locale, auth user). Use Zustand/Redux for frequently-updated shared state (shopping cart, UI state, server data). React Query / TanStack Query manages server-side state (caching, refetching, loading states) specifically — use it instead of Redux for API data.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "React — Rendering",
		Name:     "React virtual DOM and reconciliation",
		Overview: "React keeps a virtual DOM (a lightweight JS tree mirroring the real DOM); on state change it re-renders the component tree into a new virtual DOM, diffs it against the previous one (reconciliation), and applies only the minimal real DOM changes.",
		Question: "React says it's 'fast' because of the virtual DOM. But rendering a virtual DOM and diffing it adds work. Why is this faster than directly manipulating the real DOM on every change?",
		Explanation: "Real DOM mutations are expensive — changing a property can trigger layout recalculation, style recalculation, and repaint (browser rendering pipeline). Reading DOM properties like `offsetWidth` forces synchronous layout ('layout thrashing'). The virtual DOM is a plain JavaScript object — creating and diffing JS objects is cheap compared to touching the real DOM. React batches all virtual DOM changes and applies them to the real DOM in one pass, avoiding interleaved reads and writes that cause layout thrashing. The reconciler's diffing algorithm: compare element types — if type changed, unmount old subtree and mount new one. If same type, update only changed attributes. The `key` prop: tells React which list item is which across re-renders — without keys, React can't tell if an item moved or was replaced, leading to wrong updates. `key` must be stable (not array index) for list items that can reorder.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "React — Modern",
		Name:     "React Server Components (RSC)",
		Overview: "React Server Components run only on the server — they can directly access databases, file systems, and secrets without API calls, and send only HTML/serialised output to the client — reducing JavaScript bundle size and eliminating client-server waterfalls.",
		Question: "A page needs to fetch data from a database and render it. Traditional React fetches via an API call from the client. Server Components claim to fetch data directly in the component — on the server. How does this change the architecture?",
		Explanation: "Traditional flow: browser downloads JS → React renders skeleton → component calls fetch('/api/data') → server queries DB → client receives JSON → React renders data. Two round trips, API layer needed. RSC flow: browser requests page → server runs the component → component calls DB directly → server sends finished HTML + serialised component tree → browser renders immediately. The component never ships to the browser — its code (imports, logic) is excluded from the JS bundle. Server Components can be async: `async function UserList() { const users = await db.query('...'); return <ul>{users.map(...)}</ul>; }`. Client Components are still needed for interactivity (`useState`, event handlers) — mark with `'use client'` directive. Next.js App Router makes all components Server Components by default; opt into Client Components only where needed. Result: faster initial load, smaller JS bundles, no API layer for non-public data.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "Node.js",
		Name:     "Node.js event loop and non-blocking I/O",
		Overview: "Node.js runs JavaScript single-threaded but uses libuv's thread pool and OS async APIs for I/O — disk reads, network calls, and DNS lookups are offloaded so the event loop is never blocked waiting for them.",
		Question: "Node.js handles thousands of concurrent HTTP connections despite being single-threaded. How does it avoid one slow database query blocking all other requests?",
		Explanation: "When Node calls `fs.readFile()` or `http.get()`, it hands the I/O operation to libuv, which delegates to OS-level async APIs (epoll on Linux, kqueue on macOS, IOCP on Windows) or its own thread pool (4 threads by default for file I/O and DNS). The event loop is immediately free to handle other requests. When the I/O completes, the callback is added to the event loop queue and called in turn. The event loop phases (in order): timers (setTimeout/setInterval callbacks) → pending callbacks → idle/prepare → poll (wait for I/O, run I/O callbacks) → check (setImmediate) → close callbacks. CPU-bound work blocks the event loop: `JSON.parse()` on a 100MB file freezes all connections for the duration. Solutions: Worker Threads for CPU-bound tasks (Node 10.5+), `cluster` module to fork multiple processes (one per CPU core), or offload to a dedicated worker service.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "Node.js",
		Name:     "Express middleware pattern",
		Overview: "Express middleware is a function `(req, res, next) => {}` that processes an HTTP request — middleware runs in the order it's registered, each calling `next()` to pass control or ending the cycle with `res.send()`; this enables composable authentication, logging, parsing, and error handling.",
		Question: "Your Express app needs to: parse JSON bodies, log every request, verify a JWT, and then route to the controller. How does middleware compose these four concerns?",
		Explanation: "Express is a stack of middleware: `app.use(express.json())` → parses the JSON body and adds `req.body`. `app.use(morgan('combined'))` → logs the request. `app.use(authMiddleware)` → verifies the JWT from `Authorization` header, adds `req.user`, or calls `next(err)` with an error if invalid. `app.get('/users', usersController)` → the actual route handler. Each middleware calls `next()` to pass to the next middleware, or `next(err)` to jump to the error handler. Error handling middleware has signature `(err, req, res, next)` — Express recognises it by the 4-argument signature. `Router`: group related routes and middleware: `const api = express.Router(); api.use(authMiddleware); api.get('/users', ...)`. Middleware order matters: `express.json()` must run before any handler that reads `req.body`. Async middleware: wrap with try/catch and call `next(err)` on rejection, or use `express-async-errors` package.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "Node.js",
		Name:     "ES modules vs CommonJS",
		Overview: "CommonJS (require/module.exports) is Node.js's original module system — synchronous, dynamic; ES modules (import/export) are the JavaScript standard — static, asynchronous, enabling tree-shaking and top-level await; Node.js supports both with different file extensions and interop limitations.",
		Question: "You mix `require()` and `import` in a Node.js project and get errors. You try top-level `await` and it fails in a .js file. What are the two module systems and how does Node decide which to use?",
		Explanation: "CommonJS: `require('./module')` is synchronous and dynamic — you can call it inside an if statement or loop. Exports: `module.exports = { fn }` or `exports.fn = fn`. Every Node.js file before ES modules. ES Modules: `import { fn } from './module.js'` is static and asynchronous. The static nature enables tree-shaking (bundlers remove unused exports). `import()` is dynamic and returns a Promise. Top-level `await` is supported in ES modules but NOT in CommonJS. Node.js decides: `.cjs` = always CommonJS, `.mjs` = always ESM, `.js` = depends on `\"type\": \"module\"` in `package.json` (ESM if present, else CJS). Interop: ESM can import CJS (it gets the `module.exports` as the default export), but CJS cannot `require()` ESM — only `import()` dynamically. TypeScript: `tsconfig.json` `\"module\": \"CommonJS\"` or `\"ESNext\"` controls output.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "Node.js",
		Name:     "Node.js streams",
		Overview: "Streams process data incrementally as it arrives — a readable stream produces chunks, a writable stream consumes chunks, a transform stream processes each chunk — enabling processing of files or network data larger than available memory.",
		Question: "Reading a 10 GB log file with `fs.readFile()` crashes with OOM because it loads the entire file into memory. How do streams solve this?",
		Explanation: "Streams process data chunk by chunk — default chunk size is 64 KB. `fs.createReadStream('huge.log')` reads and emits chunks as they come off disk without buffering the whole file. Pipe: `readStream.pipe(gzip).pipe(writeStream)` — reads a chunk, compresses it, writes it, moves to the next chunk. The memory footprint stays at ~few MB regardless of file size. This is how `curl | tar` works in bash — gigabyte archives extracted with constant memory. Types: `Readable` (source: file, HTTP request, stdin), `Writable` (destination: file, HTTP response, stdout), `Transform` (in-out: gzip, encryption, JSON parsing), `Duplex` (TCP socket — both readable and writable). Async iteration (Node 10+): `for await (const chunk of readStream) { process(chunk) }` — the cleanest API. Backpressure: `writable.write()` returns false when the internal buffer is full — pause reading until the `drain` event fires to avoid buffering unlimited data.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Core",
		Name:     "JavaScript generators and iterators",
		Overview: "Generators are functions that can pause (`yield`) and resume, producing values lazily on demand — they implement the iterator protocol and enable infinite sequences, custom iterables, and cooperative coroutines.",
		Question: "You need an infinite sequence of IDs (1, 2, 3, ...) without creating a huge array upfront. A regular function can't pause halfway through. What language feature produces values one at a time on demand?",
		Explanation: "`function* idGenerator() { let id = 1; while (true) { yield id++; } }`. Call `gen.next()` → `{ value: 1, done: false }`. Call again → `{ value: 2, done: false }`. The function pauses at `yield`, resumes from there on `.next()`. Generators implement the iterator protocol, so they work with `for...of`, spread `[...gen]` (careful with infinite!), and destructuring. Practical uses: paginated API fetching (`yield` each page of results), lazy data pipelines (transform a stream of data without materialising intermediate arrays), cooperative multitasking. `async function*` (async generators): `yield await fetch(url)` — works with `for await (const page of asyncGen)`. Redux-Saga uses generators extensively for orchestrating async side effects in a testable, sequential style.",
	},
	{
		Category: "JS / TS / React",
		Layer:    "JavaScript — Core",
		Name:     "JavaScript event delegation",
		Overview: "Event delegation attaches one event listener to a parent element instead of many listeners to individual children — it works because events bubble up from the target through all ancestors, and `event.target` identifies the actual element clicked.",
		Question: "A list with 10,000 items needs click handling on each item. Adding 10,000 event listeners is slow and breaks for dynamically added items. How does event delegation solve both problems with one listener?",
		Explanation: "DOM events bubble: clicking a `<li>` fires the click event on `<li>`, then `<ul>`, then `<body>`, then `window`. One listener on `<ul>` catches all clicks from any child. `ul.addEventListener('click', (e) => { if (e.target.matches('li.item')) { handleItem(e.target); } })`. `e.target` is the element actually clicked; `e.currentTarget` is the element with the listener. Benefits: one listener instead of N — less memory. Works for items added dynamically after the listener is set up. Used in jQuery's `.on(selector, handler)`, React's synthetic event system (React attaches one listener per event type to the root container and delegates from there — not to individual elements). Caution: `stopPropagation()` inside a child listener prevents the parent from receiving the event — breaks delegation.",
	},

	// ── Golang ────────────────────────────────────────────────────────────────

	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "Goroutines vs OS threads",
		Overview: "Goroutines are lightweight user-space threads managed by the Go runtime — they start with a 2–8 KB stack that grows dynamically, can be multiplexed onto a small pool of OS threads (M:N scheduling), and cost microseconds to create vs milliseconds for OS threads.",
		Question: "Starting 100,000 OS threads would crash most systems. Go programs routinely run 100,000+ goroutines without issue. What makes goroutines so much cheaper than OS threads?",
		Explanation: "OS thread: ~1–8 MB stack (fixed at creation), scheduled by the kernel, context switch requires ring-0 transition (~microseconds of overhead). Creating one involves multiple system calls. Goroutine: starts at 2–8 KB stack, grows and shrinks automatically as needed, scheduled by the Go runtime in user space. Creating one is just a function call and stack allocation — takes ~1 microsecond. Go's M:N scheduler multiplexes G goroutines onto M OS threads (where M = GOMAXPROCS, default = number of CPU cores). When a goroutine blocks on I/O or a channel, the scheduler detaches it from its OS thread and runs another goroutine in its place — the OS thread is never idle waiting. This is why Go HTTP servers can handle massive concurrency with `go handleRequest(conn)` per connection — each goroutine is a few KB, not a few MB.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "Channels (buffered vs unbuffered)",
		Overview: "Channels are typed conduits for communicating between goroutines; an unbuffered channel synchronises sender and receiver (both must be ready simultaneously); a buffered channel allows the sender to proceed without a waiting receiver up to the buffer capacity.",
		Question: "A goroutine sends data on a channel but the receiver isn't ready yet. With an unbuffered channel the sender blocks. With `make(chan int, 10)` the sender doesn't block (yet). What is the practical difference and when do you use each?",
		Explanation: "Unbuffered `make(chan T)`: send blocks until a receiver is ready; receive blocks until a sender sends. This is a rendezvous — both parties synchronise at the channel. Use for: signalling (done channels), guaranteed handoff, making concurrency easier to reason about. Buffered `make(chan T, N)`: sender proceeds without blocking until the buffer is full; receiver blocks only if the buffer is empty. Use for: smoothing bursts (producer faster than consumer temporarily), worker pools (send N jobs upfront). Common mistake: using a large buffer to 'fix' a blocking issue without understanding why — the root cause may be a goroutine leak or wrong design. Closing a channel: `close(ch)` signals no more values; receivers drain remaining values then get the zero value forever. `for v := range ch` reads until the channel is closed. Never send on a closed channel — panic. Only the sender should close.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "select statement",
		Overview: "The `select` statement waits on multiple channel operations simultaneously — it blocks until one is ready, then executes that case; if multiple are ready, one is chosen at random; a `default` case makes it non-blocking.",
		Question: "A goroutine reads from two channels — whichever has data first. If neither has data within 5 seconds, it should time out. How does `select` handle all three cases without polling?",
		Explanation: "`select { case v := <-ch1: handle(v); case v := <-ch2: handle(v); case <-time.After(5*time.Second): handleTimeout(); }` — blocks until one case is ready. `time.After` returns a channel that receives one value after the duration. All cases are evaluated simultaneously — no priority unless you implement it with a nested select. Non-blocking receive with default: `select { case v := <-ch: process(v); default: doOtherWork() }` — checks the channel without blocking. Use select for: fan-in (merge multiple channels into one), timeout patterns, cancellation (`case <-ctx.Done(): return ctx.Err()`), worker coordination. A `select {}` with no cases blocks forever — used to keep the main goroutine alive. `select` on nil channels blocks forever — useful for dynamically disabling a case: set a channel to nil to make that case unreachable.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "sync.Mutex and sync.RWMutex",
		Overview: "sync.Mutex provides mutual exclusion — only one goroutine holds the lock at a time; sync.RWMutex allows multiple concurrent readers OR one exclusive writer — essential for protecting shared state accessed by multiple goroutines.",
		Question: "A shared `map[string]int` is read by 100 goroutines and written by 1. Using a regular `Mutex` means all reads block each other. What type of lock allows concurrent reads while still protecting writes?",
		Explanation: "`sync.RWMutex`: `mu.RLock()` / `mu.RUnlock()` for reads — multiple goroutines can hold a read lock simultaneously. `mu.Lock()` / `mu.Unlock()` for writes — exclusive, blocks until all readers release. This is perfect for read-heavy workloads. Regular `sync.Mutex`: only one goroutine at a time regardless of read/write. For maps specifically: Go's built-in map is not safe for concurrent use at all — even concurrent reads can panic if a write is occurring. Use `sync.RWMutex` to guard all map access, or use `sync.Map` (built-in concurrent map, good when keys are written once and read many times). Defer pattern: `mu.Lock(); defer mu.Unlock()` — the defer ensures the unlock always happens even if the function panics. Common bug: copying a `sync.Mutex` by value — the copy has an independent lock state. Always use a pointer receiver or keep the mutex in a struct and pass pointers to the struct.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "context.Context",
		Overview: "context.Context carries deadlines, cancellation signals, and request-scoped values across API boundaries and goroutine boundaries — all functions that do I/O or spawn goroutines should accept a `ctx context.Context` as their first parameter.",
		Question: "A request spawns three goroutines to call external services. The user cancels the request. How do the three goroutines know to stop and clean up immediately instead of wasting resources?",
		Explanation: "`ctx, cancel := context.WithCancel(parentCtx)`. Call `cancel()` when the request ends — this closes the `ctx.Done()` channel. Each goroutine checks `ctx.Done()` in a select: `select { case <-ctx.Done(): return ctx.Err(); case result := <-workCh: process(result) }`. `context.WithTimeout(parent, 5*time.Second)` creates a context that auto-cancels after 5 seconds — used for external API timeouts. `context.WithDeadline(parent, time)` cancels at an absolute time. `context.WithValue(parent, key, val)` stores request-scoped values (request ID, authenticated user) — retrieve with `ctx.Value(key)`. Rules: pass ctx as the first argument (by convention), never store ctx in a struct, and always call the cancel function (even if the deadline fires first) to free resources. HTTP clients in Go accept a context: `http.NewRequestWithContext(ctx, ...)` — the request is cancelled if the context expires.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Concurrency",
		Name:     "sync.WaitGroup",
		Overview: "sync.WaitGroup waits for a collection of goroutines to finish — `Add(n)` increments the counter, each goroutine calls `Done()` when finished, and `Wait()` blocks until the counter reaches zero.",
		Question: "You launch 10 goroutines to process items in parallel. How do you wait for all 10 to finish before returning from the function — without channels or sleep?",
		Explanation: "`var wg sync.WaitGroup; wg.Add(10); for i := 0; i < 10; i++ { go func(i int) { defer wg.Done(); process(i) }(i) }; wg.Wait()`. `Add` must be called before the goroutine starts (not inside the goroutine) — the goroutine might finish before `Add` is called if launched immediately. `defer wg.Done()` ensures Done is called even if the goroutine panics. Common mistake: ranging over a slice and capturing the loop variable: `go func() { process(item) }()` — `item` is the last value by the time the goroutine runs. Fix: pass as parameter `go func(item Item) { process(item) }(item)`. WaitGroup + errgroup: `golang.org/x/sync/errgroup` is a WaitGroup that also collects errors from goroutines — `g.Go(func() error { return process() })` and `err := g.Wait()` returns the first non-nil error.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "Go interfaces (implicit implementation)",
		Overview: "In Go, a type implements an interface by having all the required methods — no `implements` keyword needed; this structural typing enables interfaces to be defined in the package that uses them, not the package that provides them, maximising decoupling.",
		Question: "In Java you write `class Dog implements Animal`. In Go there is no such declaration. How does Go know that a `Dog` type implements an `Animal` interface?",
		Explanation: "Go uses structural (duck) typing: if a type has all the methods an interface declares, it implements it — implicitly. `type Animal interface { Speak() string }`. `type Dog struct {}; func (d Dog) Speak() string { return \"Woof\" }`. `Dog` implements `Animal` without ever mentioning `Animal`. This means: the interface is defined by the consumer, not the provider. A third-party library's `Database` struct can satisfy your locally defined `Storer` interface without changing the library at all. Empty interface `interface{}` (or `any` in Go 1.18+): every type implements it — used for accepting any value. Type assertions: `v, ok := i.(Dog)` — checks if interface value holds a `Dog` and extracts it. Type switch: `switch v := i.(type) { case Dog: ...; case Cat: ...; }`. Interface values are nil if both the type and the value are nil — the nil interface pitfall: an interface holding a nil `*Dog` pointer is NOT nil.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "Go error handling (errors as values)",
		Overview: "Go treats errors as ordinary values returned from functions — `func Do() (Result, error)` — callers check the error explicitly; `errors.Is()` checks for specific errors, `errors.As()` extracts a typed error, and `fmt.Errorf(\"...: %w\", err)` wraps errors with context.",
		Question: "A Go function returns an error from a database call. By the time it reaches the top-level handler, you've lost all context about where the error occurred. How do you add context without losing the original error?",
		Explanation: "`fmt.Errorf(\"getting user %d: %w\", id, err)` — wraps the original error and adds context. The `%w` verb (Go 1.13+) makes the error 'unwrappable'. At the top level: `errors.Is(err, sql.ErrNoRows)` walks the error chain (unwrapping with %w) and returns true if `sql.ErrNoRows` appears anywhere in the chain. `errors.As(err, &myErr)` finds the first error in the chain that can be assigned to `myErr` (a concrete error type). Convention: the last return value is error; if nil, no error occurred; if non-nil, the other values may be invalid. Sentinel errors: `var ErrNotFound = errors.New(\"not found\")` — compare with `errors.Is`. Custom error types: `type ValidationError struct { Field string; Message string }; func (e *ValidationError) Error() string { ... }` — extract with `errors.As`. Never discard errors: `if err != nil { return fmt.Errorf(\"context: %w\", err) }`.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "defer, panic, and recover",
		Overview: "`defer` schedules a function call to run when the surrounding function returns (in LIFO order); `panic` stops normal execution and unwinds the stack calling deferred functions; `recover` inside a deferred function catches the panic and returns its value.",
		Question: "A goroutine panics due to a nil pointer dereference. The entire program crashes. How do you catch the panic and log it gracefully — similar to try/catch in other languages?",
		Explanation: "`defer` guarantees cleanup regardless of how a function exits (normal return, early return, panic): `defer f.Close()`, `defer mu.Unlock()`, `defer wg.Done()`. Multiple defers run in LIFO order — last deferred, first called. `panic(\"something went wrong\")` — immediately stops the function, runs all deferred functions up the call stack, then crashes the program with a stack trace. `recover()`: when called inside a deferred function, it stops the unwind and returns the panic value: `defer func() { if r := recover(); r != nil { log.Printf(\"recovered: %v\", r) } }()`. Goroutines: panics in a goroutine can only be recovered inside that goroutine — a panic in a goroutine without a recover kills the whole process. HTTP servers: net/http recovers panics in handlers automatically — but your goroutines spawned inside handlers must guard themselves. Best practice: use panic only for truly unrecoverable programming errors; use error returns for expected failure conditions.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "Go slices (internals: pointer, length, capacity)",
		Overview: "A Go slice is a descriptor with three fields: a pointer to an underlying array, a length, and a capacity — multiple slices can share the same backing array; `append` may or may not allocate a new array depending on remaining capacity.",
		Question: "You pass a slice to a function and `append` to it inside. The caller's slice is unchanged. But modifying an element inside the function IS visible to the caller. Why does this happen?",
		Explanation: "A slice descriptor (pointer, len, cap) is passed by value. The pointer still points to the same backing array, so modifying elements via the pointer affects the caller's data. But `append` inside the function: if len < cap, append extends the slice in place — but the caller's slice descriptor still has the original len, so the caller doesn't see the new element. If append exceeds capacity, Go allocates a new array — the function's pointer changes, completely decoupled from the caller. To share appends: pass a pointer to the slice (`*[]T`) or return the new slice. `append` growth: Go doubles capacity up to 1024 elements, then grows by ~25% — amortised O(1). Pre-allocate with `make([]T, 0, expectedSize)` to avoid repeated reallocations. Slice operations share the backing array: `s[1:3]` shares memory with `s` — modifying `s[2]` modifies `s[1:3][1]`. Use `copy()` to get independent slices.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "Go struct embedding (composition over inheritance)",
		Overview: "Go has no inheritance — instead, embedding a struct or interface in another struct promotes its methods and fields to the outer type, achieving code reuse through composition without the fragility of inheritance hierarchies.",
		Question: "You want `AdminUser` to have all the methods of `User` plus additional admin methods, without copy-pasting all of `User`'s implementation. Go has no `extends`. How does embedding solve this?",
		Explanation: "`type User struct { Name string; Email string }; func (u User) Greet() string { return \"Hello \" + u.Name }`. `type AdminUser struct { User; Role string }`. `AdminUser` automatically gets the `Name`, `Email` fields and the `Greet()` method promoted from `User`. `admin.Greet()` calls `User.Greet()` transparently. You can override: define `Greet()` on `AdminUser` — it shadows the embedded one. Access the embedded type explicitly: `admin.User.Greet()`. Embedding an interface in a struct: useful for method forwarding and creating mock/decorator types. Multiple embedding: `type ReadWriter struct { Reader; Writer }` — promotes all methods from both. Embedding ≠ inheritance: `AdminUser` is NOT a `User` for type assertion purposes — you can't pass `AdminUser` where `User` is expected (unless both satisfy the same interface). This forces you to program to interfaces, not concrete types.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Core Language",
		Name:     "Go generics (type parameters, constraints)",
		Overview: "Go 1.18 introduced generics — functions and types parameterised by type parameters constrained by interfaces; they eliminate repetitive code for container types and algorithms without sacrificing type safety or using `interface{}`.",
		Question: "You write a `Max` function for `int`. Then you need one for `float64`, `string`. You end up with three identical functions. Before Go 1.18 you'd use `interface{}` and lose type safety. How do generics solve this?",
		Explanation: "`func Max[T constraints.Ordered](a, b T) T { if a > b { return a } return b }`. `constraints.Ordered` is a built-in constraint satisfied by all types that support `<`, `>` (integers, floats, strings). Call: `Max(3, 5)` — T inferred as `int`. `Max(\"apple\", \"banana\")` — T inferred as `string`. Generic types: `type Stack[T any] struct { items []T }; func (s *Stack[T]) Push(v T) { s.items = append(s.items, v) }`. Constraints are interfaces: `type Number interface { ~int | ~float64 }` — `~int` means 'any type whose underlying type is int' (includes `type MyInt int`). `any` = `interface{}` — no constraint. Use cases: container data structures (Stack, Queue, Set), utility functions (Map, Filter, Reduce over slices), and reducing code duplication without reflection. Trade-off: generics increase compile time and binary size; prefer concrete types where generics would add complexity without clear benefit.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Runtime",
		Name:     "Go garbage collector and GC tuning",
		Overview: "Go uses a concurrent, tri-colour mark-and-sweep GC that runs mostly in parallel with the application — stop-the-world pauses are typically sub-millisecond; GOGC controls the trade-off between GC frequency and memory headroom.",
		Question: "Your Go service has high tail latency spikes that correlate with GC runs. The default `GOGC=100` causes frequent GC cycles. How does GOGC work and what can you tune?",
		Explanation: "Go's GC is triggered when the heap grows by GOGC% since the last collection (default: 100% — double the heap size triggers a GC). Lower GOGC (e.g., 50): GC runs more frequently — lower memory usage, more CPU overhead. Higher GOGC (e.g., 200): GC runs less frequently — more memory used, less CPU overhead, lower tail latency. Go 1.19 added `GOMEMLIMIT` — a hard memory limit that overrides GOGC to prevent OOM: `GOMEMLIMIT=512MiB`. The GC is tri-colour concurrent mark-and-sweep: during the mark phase, the GC marks all live objects while the application runs (concurrent), with a very short STW to set up and finalise. Avoid GC pressure: pool frequently allocated objects with `sync.Pool`, prefer reusing buffers (`bytes.Buffer`) over creating new ones, reduce pointer-heavy data structures (the GC scans all pointers). Profile with `pprof`: `go tool pprof` heap profile shows allocation hot spots.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Runtime",
		Name:     "Go scheduler (M:N threading, GOMAXPROCS)",
		Overview: "Go's M:N scheduler maps G goroutines onto M OS threads across P logical processors (GOMAXPROCS); when a goroutine blocks on a system call, its thread is released and another goroutine runs — maximising CPU utilisation without user involvement.",
		Question: "Your Go program uses 8 CPU cores but `GOMAXPROCS=1` limits it to one core. Changing it to 8 makes CPU-bound code 8× faster. What does GOMAXPROCS control exactly, and why doesn't it affect I/O-bound code the same way?",
		Explanation: "The Go scheduler has three entities: G (goroutine), M (OS thread, mapped to a CPU core), P (logical processor — a context that holds a run queue of goroutines). `GOMAXPROCS` sets the number of P's — the maximum number of goroutines that can execute Go code simultaneously. For CPU-bound work: more P's = more goroutines run in true parallel = faster. `GOMAXPROCS=runtime.NumCPU()` is the default since Go 1.5. For I/O-bound work: goroutines block on I/O, the scheduler detaches them from their P and runs other goroutines — all P's stay busy regardless of how many goroutines are waiting on I/O. Network polling: Go's netpoller (epoll/kqueue) handles all network I/O asynchronously — a goroutine calling `conn.Read()` is parked until data arrives without blocking its OS thread. System calls that do block an OS thread (blocking file I/O, CGo) cause the scheduler to create a new OS thread to keep the P running.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Runtime",
		Name:     "Go race detector",
		Overview: "The race detector (`go run -race`, `go test -race`) instruments memory accesses at compile time and detects concurrent unsynchronised reads and writes to the same variable at runtime — reporting the exact goroutines and stack traces involved.",
		Question: "Two goroutines increment a shared counter without a mutex. Tests pass silently. In production, the counter gives wrong results. How do you catch this data race before it reaches production?",
		Explanation: "`go test -race ./...` — runs all tests with the race detector enabled. If two goroutines access the same memory location concurrently and at least one is a write without synchronisation, the race detector prints a detailed report: goroutine stack traces, the conflicting accesses, and the file/line numbers. The race detector adds ~2× runtime overhead and ~5–10× memory overhead — run it in CI, not production (unless traffic is low). It only catches races that actually occur during the run — a race that needs specific timing might not trigger. Use the Go race detector religiously: `go build -race` for integration tests, `go test -race` for unit tests. Common races: incrementing a shared counter, appending to a shared slice, reading a map while another goroutine writes to it (Go maps are not safe for concurrent access — even concurrent reads panic if a write is occurring). Fix: `sync.Mutex`, `sync/atomic`, `sync.Map`, or channel communication.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Runtime",
		Name:     "Go profiling with pprof",
		Overview: "pprof is Go's built-in profiler — it collects CPU profiles (which functions consume CPU), heap profiles (which functions allocate memory), goroutine dumps, and trace data — accessible via `net/http/pprof` endpoints or direct file collection.",
		Question: "Your Go service's CPU usage is high but you don't know which function is causing it. How do you identify the hot path in a running production service without restarting it?",
		Explanation: "Import `_ \"net/http/pprof\"` — this registers profiling endpoints on your HTTP server at `/debug/pprof/`. Collect a CPU profile: `go tool pprof http://localhost:6060/debug/pprof/profile?seconds=30` — samples the call stack 100 times per second for 30 seconds. `(pprof) top` — shows functions sorted by CPU time. `(pprof) web` — opens a flame graph in the browser (requires graphviz). Heap profile: `/debug/pprof/heap` — shows memory allocations by function. Goroutine dump: `/debug/pprof/goroutine` — all goroutine stack traces, essential for diagnosing goroutine leaks. `go test -bench=. -cpuprofile=cpu.prof` — profile benchmarks. Flame graphs: `go tool pprof -http :8080 cpu.prof` — interactive flame graph. Trace: `net/http/pprof/trace` gives a detailed execution trace viewable with `go tool trace`. Typical findings: excessive JSON marshalling, reflect-heavy code, unintended memory allocations in hot paths.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Testing",
		Name:     "Go table-driven tests",
		Overview: "Table-driven tests define a slice of test cases as a struct (input + expected output) and loop over them — this pattern eliminates duplicated test boilerplate, makes adding new cases trivial, and produces clear failure messages with the case name.",
		Question: "You have a `sum(a, b int) int` function with 10 test cases. Writing a separate `TestSum*` function for each is verbose. Go's testing idiom handles all 10 in a single test function — how?",
		Explanation: "`func TestSum(t *testing.T) { tests := []struct{ name string; a, b, want int }{ {\"positive\", 1, 2, 3}, {\"negative\", -1, -2, -3}, {\"zero\", 0, 0, 0} }; for _, tc := range tests { t.Run(tc.name, func(t *testing.T) { got := sum(tc.a, tc.b); if got != tc.want { t.Errorf(\"sum(%d,%d) = %d, want %d\", tc.a, tc.b, got, tc.want) } }) } }`. `t.Run` creates a sub-test with its own name — failures show `TestSum/positive` not just `TestSum`. Sub-tests run in parallel: `t.Parallel()` inside `t.Run` — tests run concurrently (must capture the loop variable). Adding a new test case = adding one struct literal. `t.Errorf` continues the test; `t.Fatalf` stops it immediately. Use `testify/assert` for richer assertions (`assert.Equal(t, want, got)`). Benchmarks follow the same table pattern with `b *testing.B` and `b.ResetTimer()`.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Tooling",
		Name:     "Go modules and versioning (go.mod, go.sum)",
		Overview: "Go modules (`go.mod`) declare the module path and dependencies with exact versions; `go.sum` contains cryptographic checksums for every dependency version — together they guarantee reproducible, tamper-proof builds.",
		Question: "You upgrade a dependency and your colleague's machine builds fine but production breaks because it pulled a different patch version. How do Go modules guarantee everyone uses the exact same dependency versions?",
		Explanation: "`go.mod` declares: module name, minimum Go version, and direct/indirect dependencies with their minimum versions. `go.sum` contains a SHA-256 hash of every dependency's zip file and `go.mod` — the Go toolchain verifies these hashes before using cached modules. Minimum Version Selection (MVS): if package A requires lib@v1.2 and package B requires lib@v1.3, Go uses v1.3 — the minimum version that satisfies all requirements. It never automatically upgrades beyond what's needed. `go get package@v1.5.0` — pins to a specific version. `go mod tidy` — adds missing and removes unused dependencies. `go mod vendor` — copies all dependencies into a `vendor/` folder for offline builds. Major version suffixes: `import \"example.com/lib/v2\"` — v2+ are treated as different modules, enabling major version upgrades without breaking code that uses v1. Replace directive: `replace example.com/lib => ../local-lib` — use a local fork for development.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Patterns",
		Name:     "io.Reader and io.Writer composition",
		Overview: "io.Reader (one method: `Read(p []byte) (n int, err error)`) and io.Writer (`Write(p []byte) (n int, err error)`) are Go's most pervasive interfaces — any source or sink of bytes implements them, enabling composable pipelines without knowing the concrete type.",
		Question: "You want to read an HTTP response body, decompress it with gzip, parse it as JSON, and log the bytes read — all without loading the full body into memory. How does composing io.Reader implementations achieve this?",
		Explanation: "`gzipReader, _ := gzip.NewReader(resp.Body)` — wraps the HTTP body (a `io.ReadCloser`, which is an `io.Reader`) with gzip decompression transparently. `teeReader := io.TeeReader(gzipReader, logWriter)` — reads from gzipReader and simultaneously writes every byte to logWriter. `json.NewDecoder(teeReader).Decode(&result)` — parses JSON by reading from teeReader. The full pipeline: `resp.Body → gzip.Reader → TeeReader → json.Decoder`, all streaming without buffering the whole body. Every reader in the chain implements `io.Reader` and wraps another — the standard library is full of these: `bufio.NewReader` (buffered), `io.LimitReader` (limited bytes), `cipher.StreamReader` (decrypt on the fly), `base64.NewDecoder`, `compress/zlib`. Writing to stdout, a file, a TCP connection, or a byte buffer — all via `io.Writer`. `io.Copy(dst, src)` copies in 32 KB chunks with one line of code.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Patterns",
		Name:     "sync.Once",
		Overview: "sync.Once guarantees that a function is executed exactly once, even if called from multiple goroutines simultaneously — used for lazy singleton initialisation without the overhead of a mutex on every access.",
		Question: "An expensive resource (database connection pool, config file parse) should be initialised once at startup and reused. Multiple goroutines might call the init function simultaneously. How do you guarantee exactly one initialisation?",
		Explanation: "`var once sync.Once; var db *sql.DB; func getDB() *sql.DB { once.Do(func() { db, _ = sql.Open(...) }); return db }`. The function passed to `once.Do` is called exactly once — subsequent calls to `once.Do` on the same `Once` value return immediately without calling the function, even if called from thousands of goroutines simultaneously. The zero value of `sync.Once` is ready to use — no initialisation needed. `once.Do` blocks until the function completes — if goroutine A is executing the init function, goroutine B's `once.Do` call blocks until A finishes, then returns. `sync.Once` is the idiomatic way to implement the singleton pattern in Go. Compare to `init()` functions (run automatically at package load, order is complex in large programs) — `sync.Once` gives you explicit, lazy, tested initialisation. Cannot be reset — once done, always done.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Patterns",
		Name:     "Go channels as fan-out and fan-in pipelines",
		Overview: "Fan-out distributes work from one channel to multiple worker goroutines in parallel; fan-in merges results from multiple goroutines into a single channel — together they form composable concurrent pipelines.",
		Question: "You have 1,000 URLs to fetch. Fetching sequentially takes 1,000 seconds. You want 20 concurrent workers. How do channels compose into a producer-worker-results pipeline?",
		Explanation: "Pipeline stages: (1) Producer goroutine: `jobs := make(chan string, 100); go func() { for _, url := range urls { jobs <- url }; close(jobs) }()`. (2) Fan-out: spawn 20 workers, each reading from the same `jobs` channel. `results := make(chan Result, 100); for i := 0; i < 20; i++ { go func() { for url := range jobs { results <- fetch(url) } }() }`. (3) Close results after all workers finish: `go func() { wg.Wait(); close(results) }()`. (4) Fan-in: collect from `results`: `for r := range results { process(r) }`. This is the classic worker pool pattern. `for url := range jobs` exits when the channel is closed — no sentinel value needed. `close(results)` after all workers finish lets the range loop exit. For more complex fan-in (merging from multiple distinct channels): `select` on all channels in a loop. `golang.org/x/sync/semaphore` provides a weighted semaphore for limiting concurrency without explicit worker goroutines.",
	},
	{
		Category: "Golang",
		Layer:    "Go — Tooling",
		Name:     "Go build constraints and cross-compilation",
		Overview: "Build constraints limit which files compile based on OS, architecture, or custom tags; Go's cross-compilation support allows building binaries for any target OS/arch from any host — a single `go build` command produces a Linux ARM64 binary from a macOS machine.",
		Question: "Your code has OS-specific implementations (Windows uses the Registry API, Linux reads /etc/os-release). You also need to build a Linux Docker image binary from your Mac. How does Go handle both?",
		Explanation: "Build constraints: in file `config_linux.go` — the `_linux` suffix means this file only compiles on Linux (Go understands `_GOOS` and `_GOARCH` suffixes). Or use a build tag at the top of the file: `//go:build linux && amd64`. Fallback: `config.go` with no constraint compiles on all platforms. This is how `syscall` and `os` packages have platform-specific implementations. Cross-compilation: `GOOS=linux GOARCH=arm64 go build -o myapp-linux-arm64 .` — produces a Linux ARM64 binary from any host. No C compiler needed (unless using CGo). All dependencies are compiled in. `GOOS` values: `linux`, `darwin`, `windows`, `freebsd`. `GOARCH` values: `amd64`, `arm64`, `arm`, `386`, `riscv64`. This is why building Go apps for Docker is trivial: `GOOS=linux GOARCH=amd64 go build -o app . && docker build .` — build the static binary locally, `COPY` into a `scratch` (empty) Docker image for a minimal production container.",
	},
}
